# Phase 06 Plan: Jiaoan Shicao Skill

**Milestone:** v1.3 Jiaoan Shicao Skill
**Created:** 2026-05-31
**Mode:** mvp

## Goal

Create the second concrete portable skill in this repository: `jiaoan-shicao`, a markdown-first workflow for practical lesson plans, teaching activity designs, and project-based lesson-plan rendering through the Presto `jiaoan-shicao` Typst template.

## Scope

- Add `skills/jiaoan-shicao/SKILL.md` as the canonical skill source.
- Add a reusable `jiaoan-shicao-full.md` Markdown template that demonstrates the supported intermediate structure.
- Add script code that calls the Presto `presto-template-jiaoan-shicao` binary, writes Typst output, compiles PDF with Typst, and can compare against reference Typst/PDF fixtures.
- Bundle `calendar.json` as a support resource for date and schedule context.
- Validate against the provided `jiaoan-shicao-full.md`, `jiaoan-shicao-full.typ`, and `jiaoan-shicao-full.pdf` fixture set.

## Tasks

- [x] Define the skill contract, trigger wording, inputs, outputs, verification, and safety boundaries.
- [x] Implement scriptable Markdown-to-Typst/PDF rendering and support-resource copying.
- [x] Verify generated Typst and PDF against the provided fixture artifacts.

## Validation

The phase is complete when the script can generate Typst identical to the provided reference and compile a PDF through Typst while keeping the support resource available for authoring workflows.
