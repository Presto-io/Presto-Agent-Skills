---
phase: 43-editable-pptx-renderer-and-pagination
plan: 11
type: execute
wave: 11
depends_on:
  - 43-10
files_modified:
  - skills/school-pptx/scripts/pptx_model.py
  - skills/school-pptx/scripts/pptx_paginate.py
  - skills/school-pptx/scripts/pptx_emit.py
  - skills/school-pptx/scripts/pptx_objects.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-08
  - PPTX-10
  - VER-03
must_haves:
  truths:
    - "R43-C04：contents 分页、frozen typography 与 emitter 必须消费同一 body font、margins、line spacing、paragraph spacing 和 effective geometry；合法 5 项目录必须自动扩为多张物理页，每页完整 text frame 高度不超过 118.125pt 槽高。"
    - "R43-C05 / D-04：fragment.heading 与 paragraph、每个 list item、code 正文构成的实际发射段落序列必须全部计高；heading 在每个派生 fragment 重复后，任何成功页的完整 reopen text frame 都不得超过目标槽。"
    - "R43-W04：dedicated code 使用 manifest `code.text_budget.font_size_max=14pt` 作为唯一受控字号，并在 PhysicalSlide 中冻结；planner measurement、emitter argument 与 reopen run 必须严格相等，emitter 不得回退到 10pt。"
    - "R43-W05：Phase 43 aggregate 必须实际运行 contents、heading+paragraph/list/code 与 dedicated-code 容量向量；required/called 顺序完全相等且无动态 skip，任一向量重新出现成功裁切或字号漂移时 aggregate 非零。"
    - "所有 aggregate evidence producer 只保存本次实际解析、计划、重开、hash 与前后快照值；joined equality、embedded hash、projection equality、old-target preservation 均由原始值比较得到，禁止写死 expected hash 或 True。"
    - "Phase 44 的 public `verify --workdir`、runtime adapters 和 PowerPoint/WPS 人工视觉 UAT 不在本计划实现或宣称完成。"
  artifacts:
    - path: "skills/school-pptx/scripts/pptx_model.py"
      provides: "paginator 与 emitter 共用的 frozen fragment paragraph-sequence 投影。"
    - path: "skills/school-pptx/scripts/pptx_paginate.py"
      provides: "contents 与 mixed fragments 的完整段落序列计高，以及 dedicated-code frozen 14pt typography。"
    - path: "skills/school-pptx/scripts/pptx_objects.py"
      provides: "严格按共享 paragraph sequence 发射 heading、paragraph、list item 与 code run。"
    - path: "skills/school-pptx/scripts/pptx_emit.py"
      provides: "机械消费 body/code frozen typography，并对缺失 dedicated-code 计划值 fail closed。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "整帧容量 public/reopen vectors、真实 producer evidence、AST/source guard 与扩展后的固定 aggregate。"
  key_links:
    - from: "skills/school-pptx/scripts/pptx_model.py"
      to: "skills/school-pptx/scripts/pptx_paginate.py"
      via: "同一 paragraph sequence 决定每个 fragment 的完整 frame measurement"
      pattern: "fragment_paragraph_sequence|heading|paragraph|list|code"
    - from: "skills/school-pptx/scripts/pptx_model.py"
      to: "skills/school-pptx/scripts/pptx_objects.py"
      via: "emitter 按同一 paragraph sequence 创建实际 paragraphs/runs"
      pattern: "fragment_paragraph_sequence|add_fragment_text_frame"
    - from: "skills/school-pptx/scripts/pptx_paginate.py"
      to: "skills/school-pptx/scripts/pptx_emit.py"
      via: "selected_font_sizes 与 typography.code/body 是唯一字号和 text-frame 参数来源"
      pattern: "selected_font_sizes|typography.body|typography.code|PPTX_PLAN_FONT_MISSING"
    - from: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      to: "PHASE_43_GATE_ORDER"
      via: "frame-capacity-consistency 作为 R43-C04/C05/W04/W05 的 blocking gate"
      pattern: "frame-capacity-consistency|R43-C04|R43-C05|R43-W04|R43-W05"
---

<objective>
关闭 Phase 43 post-execution verification 中剩余的两个确定性容量 blocker 与两个 typography/evidence warning。

Purpose: 让 paginator 对“将要发射的完整段落序列”计高，并让 Phase 43 aggregate 由 public render、frozen plan、PPTX reopen 与源码审计共同证明不存在成功裁切、专用 code 字号漂移或常量回填假证据。
Output: 共用 paragraph-sequence 契约、contents/fragment-heading 完整计高、dedicated-code frozen typography、`frame-capacity-consistency` blocking gate，以及 producer 级 AST/source guard。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-09-PLAN.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-09-SUMMARY.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-10-PLAN.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-10-SUMMARY.md
@skills/school-pptx/templates/standard-school.manifest.yaml
@skills/school-pptx/scripts/pptx_model.py
@skills/school-pptx/scripts/pptx_paginate.py
@skills/school-pptx/scripts/pptx_emit.py
@skills/school-pptx/scripts/pptx_objects.py
@skills/school-pptx/scripts/verify_pptx_renderer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 以实际段落序列统一 contents 与 fragment-heading 整帧计高（R43-C04/C05）</name>
  <files>skills/school-pptx/scripts/pptx_model.py, skills/school-pptx/scripts/pptx_paginate.py, skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-10-SUMMARY.md
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
  </read_first>
  <action>在 `pptx_model.py` 增加不依赖 `python-pptx` 的单一纯 helper `fragment_paragraph_sequence(fragment)`（可用 frozen paragraph projection 表达），顺序严格为：非空 `fragment.heading` 一段；`kind == "code"` 时 `fragment.text` 一段且 role=`code`；有 `fragment.items` 时每个 item 各一段且保持原顺序；其余非空 `fragment.text` 一段且 role=`paragraph`。helper 不做 Markdown reparse、delimiter normalization、strip、join 或分页，只描述 `add_fragment_text_frame()` 实际将创建的段落序列。修改 `pptx_objects.add_fragment_text_frame()` 只遍历该 helper：heading/paragraph/list item 走现有 rich runs，code 仍直接写一个 `Consolas` run；每个实际 paragraph 精确应用 frozen `line_spacing` 与 `paragraph_spacing_points`，不得在 objects 层增加或删除段落。

在 `pptx_paginate.py` 增加 `_measure_paragraph_sequence(...)`，对 `fragment_paragraph_sequence()` 中每个实际 paragraph 分别调用 CJK-aware `TextMeasure`，使用同一 frozen font、effective width、role（code 或 paragraph），并将 `display_lines * font * line_spacing` 与每个 emitted paragraph 的一次 `paragraph_spacing_points` 相加；text run 内的显式换行只增加 display lines，不得误算为额外 paragraph spacing。`_fragment_height()` 改为该 helper 的薄封装，不再只测 `fragment.text/items`，也不得忽略 `fragment.heading`。margins 只通过 `_effective_geometry()` 从可用 frame width/height 扣除一次，不得再次加入 fragment height。

重写 `_contents_fragments()` 的权重来源：先取得 `_selected_body_typography(contents.body)`，因此受控字号严格为 `font_size_max=26pt`，再以已经编号的实际 item paragraph、frozen `line_spacing=1.2`、`paragraph_spacing_points=2pt`、零 margins 和 effective geometry 计高；`ordered_contiguous_partition(..., count_first=True)` 的每项 weight 必须包含该 item 发射后的完整 paragraph 高度。`build_deck_plan()` 继续为每张 contents 页冻结 `selected_font_sizes.body=26pt` 和全部 `typography.body.*`，禁止 `_contents_fragments()` 使用 18pt min 或原始 slot height。合法 5 项 contents 向量应在 118.125pt body 槽内自动扩为至少 2 页，编号连续且每页标题仍严格为 `目录`。

修订 `_simple_slide_fragments()`、`_text_fragments()` 与 `_code_fragments()` 的容量判断，使每个派生 fragment 都以完整 sequence 计高。带 heading 的 code 在每个 code fragment 中为重复 heading 预留高度后才按完整 source line 分组；paragraph/list 在完整 block 超页时继续按既有 sentence/list-item 边界拆分，并在每个候选 fragment 上重复 heading 后重测，直到每页 sequence 高度不超过 capacity；单个语义单元在 minimum/受控字号下仍不可容纳时产生既有 bounded overflow diagnostic，绝不以 exit 0 保留超高页。不得改变 D-01～D-05 的源顺序、完整 list item、code source-line boundary、heading 无“续”重复或 parser-plan-PPTX 文本保真。

在 `verify_pptx_renderer.py` 新增并注册可单独调用的 `frame-capacity-consistency` gate（本任务先实现 contents 与 fragment-heading vectors）：(1) public render 一个空 authored contents slide + 5 个后续 `##`，断言 exit=0、contents physical pages&gt;1、frozen/reopen body font 均为 26pt、可见编号 1..5 连续；(2) 分别 public render `###` heading + paragraph、`###` heading + list、`### 源码` + 4×40 中文 code 三个合法 title-content 向量，断言成功时每个派生 fragment 均重复同一 heading。gate 必须从 reopen shape 的实际 paragraph/run 文本、font、margins、line spacing 与 space-after 重新构造 paragraph sequence，用同一 `TextMeasure` 重算完整 frame height，并逐页断言 `reopened_full_height_points &lt;= effective_content_height_points`；同时断言 plan sequence 与 reopen sequence 逐项相等，code parser/frozen/reopen 按 physical order 连接后逐字符相等。返回实际 raw paragraph texts/roles 或其 SHA-256、实际 font/spacing/margins、每页 display/full/effective heights、physical indices、public exit/output bytes 和 artifact paths，不返回固定 `PASS` 或写死 equality boolean。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/pptx_model.py skills/school-pptx/scripts/pptx_paginate.py skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py frame-capacity-consistency &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py mixed-fragment-capacity</automated>
  </verify>
  <acceptance_criteria>
    - R43-C04 planning assertion: `_contents_fragments()` 使用 frozen 26pt/1.2/2pt/零 margins 与 effective geometry；5 项合法目录计划为多物理页，每页完整 paragraph-sequence 高度不超过 118.125pt，编号 1..5 连续且无 `续`。
    - R43-C04 reopen assertion: public render 退出 0，contents plan font、emitted run font、reopen run font 均为 26pt；每个 reopen paragraph 的 text/spacing 与 frozen sequence 一致，完整 frame 重测不超槽。
    - R43-C05 sequence assertion: shared helper 对 heading+paragraph、heading+每个 list item、heading+code 产生与 `add_fragment_text_frame()` 完全相同的段落数、顺序、role 与文本；无 heading fragment 不增加空段。
    - R43-C05 capacity assertion: 三类带 heading public vectors 的每张派生页都重复 heading，完整 reopen frame 高度不超过 effective slot；原 4×40 中文 code 反例不得再以两张各 146pt 的页面退出 0。
    - Fidelity assertion: paragraph/list/code 保持源顺序与既有语义拆分；code 只在完整 source line 间分页，parser、全部 frozen fragments 与全部 reopen code runs 逐字符相等，soft wrap 不写回 newline。
    - Evidence assertion: gate 返回本次实际 paragraphs/hashes、字号、spacing、heights、退出码、输出字节数与 artifact paths；所有 equality 由这些 raw values 在 gate/aggregate 中比较，不写死 True。
  </acceptance_criteria>
  <done>R43-C04/C05 由共享段落序列、public 扩页和逐页 reopen 完整高度证据关闭。</done>
</task>

<task type="auto">
  <name>Task 2: 冻结 dedicated-code 受控字号并机械发射（R43-W04）</name>
  <files>skills/school-pptx/scripts/pptx_paginate.py, skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-03-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-10-PLAN.md
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
  </read_first>
  <action>在 `build_deck_plan()` 为 `layout == "code"` 建立显式 dedicated-code typography：从 manifest `layouts.code.slots[id=code].text_budget` 选择唯一受控 `font_size_max=14pt`，并在所有 code 物理页写入 `selected_font_sizes=(('code', 14.0),)`；同时以 `typography.code.*` 冻结实际使用的 margins、line spacing 与 paragraph spacing，供 code paginator 与 emitter 共同消费。`_simple_slide_fragments()` / `_code_fragments()` 对 dedicated code 必须使用该 14pt frozen typography 和 effective code geometry 计算 soft-wrap display lines、物理页数与 capacity，不得另取 min=10pt 或重新选择字号。若 dedicated code 有 `fragment.heading`，分页时必须计入 emitter 实际保留的 heading 区域/段落高度；无 heading 时不得预留虚假空间。

修改 `pptx_emit.emit_deck()` 的专用 code 分支，删除 `dict(physical.selected_font_sizes).get("code", budget["font_size_min"])` 回退。通过现有 `_frozen_body_typography()` 的通用化版本或专用 `_frozen_code_typography()` 严格读取 `selected_font_sizes.code` 与全部 `typography.code.*`；缺失或非数值时分别抛 `PPTX_PLAN_FONT_MISSING` / `PPTX_PLAN_TYPOGRAPHY_MISSING`，不得发射。扩展 `add_literal_text()` 以机械应用 frozen margins、line spacing 与 paragraph spacing；emitter 不导入 paginator、不读取 manifest min/max 决定 code run 字号，也不改变 frozen page count。heading 若继续使用独立 `school-pptx:code-heading` shape，其冻结/派生高度必须与 paginator 计量完全相同，code shape 的剩余 geometry 必须为正。

扩展 `frame-capacity-consistency` gate，增加无 heading 与有 `###` heading 的 dedicated-code public vectors，使用足以触发分页的多行/宽行中文 code。对每个 vector 记录并断言：public exit=0；plan `selected_font_sizes.code`、emitter 输入与每个 reopen `school-pptx:code` run 都为 14pt；plan physical page count 等于 reopen code shape count；parser text、按 physical order 连接的 frozen code text、reopen run text逐字符相等；从实际 reopen code frame geometry/margins/paragraph properties 重测的 full height 不超过实际剩余 code geometry。增加缺失 frozen font/typography 负例，精确断言两个 stable fail-closed codes。gate evidence 返回实际 plan/reopen fonts、page counts、geometry、heights、text hashes、退出码和 artifacts，不返回固定 equality 结论。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/pptx_paginate.py skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py frame-capacity-consistency &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - R43-W04 font assertion: dedicated-code plan、paginator measurement、emitter argument 与全部 reopen code runs 严格为 14pt；不再出现 plan 空字号与 reopen 10pt。
    - Frozen-boundary assertion: dedicated code 的字号和 typography 出现在 `PhysicalSlide.to_projection()`；删除任一 frozen font/typography 字段时 emitter 精确返回 `PPTX_PLAN_FONT_MISSING` / `PPTX_PLAN_TYPOGRAPHY_MISSING`，不得读取 manifest fallback 后继续。
    - Capacity assertion: dedicated-code physical page count 等于 reopen code shape count，每页完整 code frame（有 heading 时使用扣除同源 heading 后的实际 geometry）重测不超槽；超量合法 code 自动增加物理页。
    - PPTX-10 assertion: code 保持单个 editable monospace run/页，parser、frozen fragments、reopen runs 按 physical order 连接后逐字符相等，soft wrap 不增加 authored newline。
    - Emitter isolation assertion: `pptx_emit.py` 不导入 paginator/measure，不调用 font selection，也不通过 `.get("code", ...)` 或 manifest min/max 回退决定 dedicated code 字号。
  </acceptance_criteria>
  <done>R43-W04 由 frozen 14pt、planner/emitter/reopen equality 与完整 code-frame capacity 证据关闭。</done>
</task>

<task type="auto">
  <name>Task 3: 扩展固定 aggregate 并禁止 evidence producer 常量回填（R43-W05）</name>
  <files>skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-08-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-10-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-10-SUMMARY.md
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
  </read_first>
  <action>把 `frame-capacity-consistency` 加入 `PHASE_43_GATE_ORDER`，固定放在既有 `mixed-fragment-capacity` 之后、`emit-structure` 之前；同步更新 `PHASE_43_REQUIRED_GATES`、argparse choices、called-order/exact-set assertions，保持元素唯一且 `dynamic_skips=0`。扩展 `GAP_COVERAGE`，精确增加 `R43-C04 -> frame-capacity-consistency`、`R43-C05 -> frame-capacity-consistency`、`R43-W04 -> frame-capacity-consistency`、`R43-W05 -> (frame-capacity-consistency, mixed-fragment-capacity, media-descriptor-binding)`；`REQUIREMENT_COVERAGE.PPTX-08` 必须包含新 gate，`PPTX-10` 必须包含 dedicated-code vector。aggregate 从 `frame-capacity-consistency` 的 raw plan/reopen fonts、paragraph/text hashes、page counts、full/effective heights 和 public exits 重新比较并阻断，不得仅信任 producer 返回的 `joined_equality`/`PASS`。

修订 `mixed_fragment_capacity_gate()` 与 `media_descriptor_binding_gate()` 的 evidence producer：`joined_equality` 不再写死 `True`，改为返回 parser/frozen/reopen 实际文本或各自 SHA-256/长度，aggregate 自行比较；`embedded_hash` 必须来自 `_embedded_media_hashes()` 实际提取结果，不能赋 `original_hash`；runtime-missing 返回 `projection_before_hash` 与 `projection_after_hash`，不能写死 `frozen_projection_equal=True`；symlink/add-picture fault 返回旧目标 before/after SHA-256 或 bytes length/hash，不能写死 `old_target_preserved=True`。若保留便捷 boolean，其值必须由同一 raw operands 的 `==`/`!=` 比较赋得，且 aggregate 仍从 raw operands 重算。所有 evidence 保持 bounded，不把 PPTX bytes、完整 projection 或用户绝对路径写入公共输出。

把 `_assert_gap_outcome_source_derived()` 扩展为 producer 级 AST/source guard（可更名但保留 aggregate 调用）：定位 `mixed_fragment_capacity_gate`、`media_descriptor_binding_gate`、`frame_capacity_consistency_gate` 与 `run_phase_43` 四个函数；对 `joined_equality`、`embedded_hash`、`frozen_projection_equal`、`old_target_preserved`、`gap_outcome_audit` 的赋值/return value 建立结构断言，拒绝 `True` literal、expected hash 直接回填、固定 projection equality、固定 old-target preservation，要求 observed 字段来自实际 reopen/package/snapshot 变量或 Compare/哈希调用。增加 guard 自测：对最小变异 source（把上述字段改成 `True` 或 expected hash）运行 checker 必须失败；生产 source 必须通过。`gap_outcome_audit` 增加 R43-C04/C05/W04/W05 的实际 gate evidence，registry 的 `required`/`called`/`unique` 也从真实 registry/called list 推导，不写死成功值。

完整 aggregate 必须在隔离 TemporaryDirectory 中实际黑盒执行 public shell，并保留 canonical render、Phase 41/42 regression、双产物 cleanliness 与 Phase 44 scope assertions。删除/skip/reorder `frame-capacity-consistency`，把 contents 恢复 18pt measurement、忽略 heading、恢复 dedicated code 10pt fallback，或将 producer evidence 改成常量，任一变异都必须让 `phase-43` 非零。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py frame-capacity-consistency &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py mixed-fragment-capacity &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py media-descriptor-binding &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py phase-43</automated>
  </verify>
  <acceptance_criteria>
    - Coverage assertion: aggregate 实际执行 contents 5 项、heading+paragraph/list/code、dedicated code（含 14pt 与 full-frame capacity）所有 vectors；R43-C04/C05/W04/W05 均映射到实际 called gate。
    - Registry assertion: `PHASE_43_GATE_ORDER`、required set 与 called tuple 完全相等且唯一，`frame-capacity-consistency` 位于固定位置，`dynamic_skips==0`；删除、skip、重复或 reorder 任一 gate 均失败。
    - Raw evidence assertion: mixed code 返回 parser/frozen/reopen 实际 hashes/lengths；media 返回实际 embedded hash list、projection before/after hash、old target before/after hash；aggregate 从这些 raw values 重算 equality/inequality。
    - AST/source guard assertion: guard 覆盖三个 producer 与 `run_phase_43`，生产 source 通过；将 `joined_equality`、`frozen_projection_equal`、`old_target_preserved` 改为 `True` 或将 `embedded_hash` 改为 expected/original hash 的变异 source 均被拒绝。
    - False-green assertion: 5 项 contents 单页超高、任一 heading vector 超高、dedicated code plan/reopen 字号不等或任一 full height 超 effective height 时，individual gate 与 `phase-43` aggregate 都非零。
    - Boundary assertion: canonical public render 仍只发布同 stem `.md/.pptx`，Phase 41/42 回归通过；未添加 Phase 44 public verify、runtime adapters 或 manual viewer PASS 声明。
  </acceptance_criteria>
  <done>R43-W05 由扩展 coverage、raw evidence 重算和 producer 级 AST/source guard 关闭。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| 成功裁切 | 合法 contents 或带 heading mixed frame 退出 0，但实际完整 text frame 超出受控 geometry | 共享 paragraph sequence；public render + plan + reopen 逐页 full/effective height blocking assertions。 |
| 计划/发射字号漂移 | dedicated code 用 14pt 分页却以 10pt 发射，破坏 frozen-plan 边界 | frozen `selected_font_sizes.code`/typography；emitter 缺值 fail closed；reopen 14pt equality。 |
| 计高重复或漏算 | margins/paragraph spacing 被重复扣除，或 code 内换行被误作多个 paragraph spacing | effective geometry 只扣 margins 一次；sequence-level 每个实际 paragraph 只加一次 space-after；显式换行只增加 display lines。 |
| aggregate false green | gate 被遗漏、动态 skip，或 producer 用 expected/True 伪造 observed evidence | fixed exact registry/called order；raw values 重算；producer 级 AST/source guard 与变异自测。 |
| evidence 泄漏/膨胀 | raw evidence 输出完整 PPTX、projection 或绝对路径 | evidence 仅保存 bounded 文本 hashes、长度、数值和隔离 artifacts；公共 CLI 继续执行 8KiB/no-traceback 边界。 |
| 资源耗尽 | 极长 paragraph/list/code 触发无界拆分或测量状态 | 保留 `MAX_TEXT_BYTES`、`MAX_LOGICAL_LINES`、`MAX_PARTITION_STATES` 与 bounded diagnostic；新增向量只使用确定性有限 fixture。 |
</threat_model>

<verification>
- `export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l"`
- `SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}"`
- `"$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/pptx_model.py skills/school-pptx/scripts/pptx_paginate.py skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/verify_pptx_renderer.py`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py frame-capacity-consistency`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py mixed-fragment-capacity`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py media-descriptor-binding`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py phase-43`
- `git diff --check -- .planning/phases/43-editable-pptx-renderer-and-pagination/43-11-PLAN.md`
</verification>

<success_criteria>
- R43-C04：5 项合法 contents 由 26pt frozen/emitted typography 自动扩页，所有 reopen full-frame heights 不超过 118.125pt。
- R43-C05：fragment.heading 与 paragraph、每个 list item、code 正文及每段 spacing 全量计高，三类 public/reopen vectors 无成功超槽页。
- R43-W04：dedicated-code planner、frozen plan、emitter 与 reopen 字号全部为 14pt，页数、文本与完整 frame capacity 一致。
- R43-W05：固定 aggregate 实际执行全部新增容量 vectors；producer 只产出实际值，aggregate 从 raw values 重算，AST/source guard 可阻断 expected/True 回填。
- PPTX-08、PPTX-10 与 Phase 43 goal 的自动化证据恢复可信；Phase 44 边界保持不变。
</success_criteria>
