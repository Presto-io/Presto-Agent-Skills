---
phase: 43-editable-pptx-renderer-and-pagination
plan: 10
type: execute
wave: 10
depends_on:
  - 43-09
files_modified:
  - skills/school-pptx/scripts/pptx_paginate.py
  - skills/school-pptx/scripts/pptx_emit.py
  - skills/school-pptx/scripts/pptx_objects.py
  - skills/school-pptx/scripts/pptx_render.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-05
  - PPTX-08
  - PPTX-10
  - VER-03
must_haves:
  truths:
    - "R43-C03 / D-03: mixed-fragment code 的分页测量与 native run 发射来自同一个 frozen target-slot 排版契约；title-content.body 固定消费 manifest 的 24pt，two-column.left_body/right_body 固定消费 manifest 的 22pt，margins/line spacing/paragraph spacing 同源，任何物理页的重测 display height 不得超过 effective content height。"
    - "R43-C03 / PPTX-08: 合法 mixed-fragment code 超出冻结槽位预算时必须增加物理页，不得以退出 0 的单页成品静默裁切；parser、frozen fragment 与 reopen code run 文本继续逐字符相等。"
    - "R43-W03: 合法相对或绝对图片的格式、像素与尺寸校验及 python-pptx add_picture 发射绑定同一个 traversal-root descriptor 逐分量打开后读出的 bounded immutable bytes；校验完成后替换 authored path 不得让第二次路径打开引入 Pillow 竞态。"
    - "运行期媒体缺失不修改 frozen plan：parse 时存在但 emit 前删除的媒体生成 editable placeholder 与结构化 MEDIA_MISSING diagnostic；render_command 合并该 error 后仍发布同 stem Markdown/PPTX best-effort 双产物并退出非零。"
    - "R43-W02: gap_outcome_audit 只从本次实际 gate 返回的 exit、output_bytes、selected font、display height、artifact path/hash 与旧目标保全 evidence 推导；不得硬编码 True、固定向量数或未验证的成功字段。"
    - "Phase 43 aggregate 保持固定顺序、required/called 完全相等、无动态 skip；PPTX-08 与 VER-03 分别由 mixed-fragment-capacity 和 media-descriptor-binding 直接阻断。"
  artifacts:
    - path: "skills/school-pptx/scripts/pptx_paginate.py"
      provides: "按实际发射字号与 effective content geometry 测量，并冻结每个正文 target slot 的字号、margins、line spacing 与 paragraph spacing。"
    - path: "skills/school-pptx/scripts/pptx_emit.py"
      provides: "普通 body slot 只消费 frozen font size，缺失计划值时 fail closed，不回退到 manifest font_size_max。"
    - path: "skills/school-pptx/scripts/pptx_objects.py"
      provides: "descriptor/bytes 绑定的图片加载、Pillow 校验与 BytesIO picture emission。"
    - path: "skills/school-pptx/scripts/pptx_render.py"
      provides: "合并运行期媒体 diagnostics，并按既有三态状态机发布 best-effort 双产物与非零退出。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "mixed-fragment 容量、媒体校验后替换与真实 aggregate outcome audit 的 blocking gates。"
  key_links:
    - from: "skills/school-pptx/scripts/pptx_paginate.py"
      to: "skills/school-pptx/scripts/pptx_emit.py"
      via: "PhysicalSlide.selected_font_sizes 为 body/left_body/right_body 提供唯一字号来源"
      pattern: "selected_font_sizes|body|left_body|right_body"
    - from: "skills/school-pptx/scripts/pptx_objects.py"
      to: "python-pptx add_picture"
      via: "同一 held descriptor 的 bounded bytes 经 Pillow 验证后以 BytesIO 发射"
      pattern: "os.open|BytesIO|Image.open|add_picture"
    - from: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      to: "gap_outcome_audit"
      via: "从 run_named_gate 返回的实际嵌套 evidence 提取并断言，不重写常量"
      pattern: "mixed-fragment-capacity|media-descriptor-binding|gap_outcome_audit"
---

<objective>
关闭 Phase 43 最终验证确认的字号错配、aggregate 假证据与图片校验后二次打开三个缺口。

Purpose: 让 PPTX-08 的模板预算与 VER-03 的 bounded public failure 都由实际物理输出和故障注入证据证明，而不是仅证明 gate 被调用。
Output: 冻结正文槽字号、descriptor/bytes 绑定图片发射、两个定向 blocking gates，以及由真实 gate evidence 推导的 Phase 43 aggregate audit。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-09-PLAN.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-09-SUMMARY.md
@skills/school-pptx/templates/standard-school.manifest.yaml
@skills/school-pptx/scripts/pptx_model.py
@skills/school-pptx/scripts/pptx_paginate.py
@skills/school-pptx/scripts/pptx_emit.py
@skills/school-pptx/scripts/pptx_objects.py
@skills/school-pptx/scripts/pptx_render.py
@skills/school-pptx/scripts/verify_pptx_renderer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 冻结正文 target-slot 实际字号并按同值分页发射（R43-C03）</name>
  <files>skills/school-pptx/scripts/pptx_paginate.py, skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-09-SUMMARY.md
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
  </read_first>
  <action>在 `pptx_paginate.py` 建立单一正文槽排版选择 helper（例如 `_selected_body_typography(slot)`），对 `overflow: paginate` 的普通 body target 选择 manifest `font_size_max` 作为实际字号：当前受控 manifest 中 `title-content.body=24.0`、`two-column.left_body=22.0`、`two-column.right_body=22.0`；同时冻结 text-frame 排版参数为受控值 `margin_left/right/top/bottom=0pt`、`line_spacing=1.2`、`paragraph_spacing=2.0pt`，并通过 `PhysicalSlide.slot_values`（或等价 frozen plan 字段）按 target slot 保存，禁止 emitter 自行取默认值。paginator 的 `TextMeasure` 与 `pptx_objects.add_fragment_text_frame()` 必须消费同一组 frozen font/margins/spacing；effective content width 严格为 `slot_width_points - margin_left - margin_right`，effective content height 严格为 `slot_height_points - margin_top - margin_bottom`，code soft-wrap、block packing 与容量判断都使用 effective content geometry，不得拿原始 slot height 直接比较。把所选字号和排版参数显式传入 `_fragment_height()`、`_code_fragments()`、`_simple_slide_fragments()` 与 `_two_column_fragments()`；构造每张 `PhysicalSlide` 时按该页真实 target slots 写入 `selected_font_sizes` 与 frozen typography，普通页使用 `body`，双栏分别使用 `left_body`/`right_body`，专用 code/table 继续保留其既有受控字号语义。修订 `pptx_emit.emit_deck()` 与 `pptx_objects.add_fragment_text_frame()`，必须精确读取并应用对应 frozen key；缺失字号或任一排版参数时抛稳定 `PptxEmitError("PPTX_PLAN_FONT_MISSING", ...)` 或 `PPTX_PLAN_TYPOGRAPHY_MISSING` 并 fail closed，不得回退到 manifest/Python-pptx 默认值、重新选择字号或改变 margins/spacing。新增并注册 `mixed-fragment-capacity` gate：分别黑盒渲染 title-content 与 two-column 合法向量，每个向量同时含普通 rich text 和 4 行×40 个中文字符的 fenced code；断言 public exit=0、title-content code run=24pt、two-column code run=22pt，reopen gate 从 text frame 回读相同 margins、line spacing 与 paragraph spacing，并用同一 `TextMeasure`、effective content width/height 重测，要求 `display_height_points &lt;= effective_content_height_points`。超量向量必须实际增加物理页。证据一致性按 physical order 收集全部 frozen code fragments 与全部 reopen code runs，使用单个 `\n` 恢复源行边界后比较 `parser_code == "\n".join(frozen_fragments) == "\n".join(reopened_runs)`，并逐页断言 `fragment.text == run.text`；不得要求分页后的任一单 fragment 等于完整 parser code。gate 必须返回每个向量的 `public_exit`、`physical_pages`、`selected_font_sizes`、`frozen_typography`、`reopened_typography`、`effective_content_width_points`、`display_height_points`、`effective_content_height_points`、逐页 equality、joined equality、`output_bytes` 与两个公开 artifact paths，供 Task 3 直接消费。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/pptx_paginate.py skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py mixed-fragment-capacity &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - R43-C03 font assertion: title-content 与 two-column 的 paginator measurement、`PhysicalSlide.selected_font_sizes`、emitter argument 和 reopen code run 分别严格为 24pt 与 22pt，不再出现 16pt/24pt 或 15pt/22pt 分叉。
    - PPTX-08 capacity assertion: 4×40 中文 code 合法向量按模板预算扩为多物理页；每页以相同 frozen margins/line spacing/paragraph spacing 计算 effective content width/height，重测 display height 不超过 effective content height，任何超高仍退出 0 都使 gate 失败。
    - PPTX-10 fidelity assertion: 按 physical order 连接全部 frozen fragments 与 reopen runs 后逐字符等于 parser code，连接符严格为单个 `\n`；每页 `fragment.text == run.text`，soft wrap 不写回额外 newline，普通 bold/highlight 保持 native runs。
    - Frozen-boundary assertion: emitter 缺失 target-slot frozen 字号或 margins/spacing 时返回 `PPTX_PLAN_FONT_MISSING`/`PPTX_PLAN_TYPOGRAPHY_MISSING`，不得读取 manifest/Python-pptx fallback、调用 paginator或修改 plan。
    - Evidence assertion: gate 返回真实数值、退出码、输出字节数和 artifact paths；不得只返回 `PASS`/`True`。
  </acceptance_criteria>
  <done>R43-C03 被同源 frozen 字号、实际扩页与 reopen 高度证据机械关闭。</done>
</task>

<task type="auto">
  <name>Task 2: 将图片校验与 add_picture 绑定同一 descriptor/bytes（R43-W03）</name>
  <files>skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/pptx_render.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-05-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-09-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-09-SUMMARY.md
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_render.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
  </read_first>
  <action>在 `pptx_emit._media_path()` 删除 `resolve()`，但必须保留 Phase 42/既有 renderer 对合法绝对媒体路径的支持。把媒体引用规范化为结构化的 `traversal_root + path_parts`（或等价 frozen-free runtime reference）：相对 authored path 拒绝空分量、`.`、`..` 后，以 Markdown 父目录 `media_root` 的 held `O_DIRECTORY | O_NOFOLLOW` fd 为 traversal root；绝对 authored path 保留其绝对语义，以文件系统 root `/` 的 held `O_DIRECTORY | O_NOFOLLOW` fd 为 traversal root，并把去除 anchor 后的各分量作为 path parts；两类路径都不得调用 `resolve()`、不得预先跟随 symlink。`pptx_objects.py` 删除 `path.is_file()` 安全判定，以 descriptor-bound helper（例如 `_load_validated_image(traversal_root_fd, path_parts)`）替代 `_safe_image_size(path)` + `add_picture(str(path))`。helper 从 traversal root 逐分量使用 descriptor-relative `os.open(..., O_NOFOLLOW)`：中间分量要求普通目录，最终分量要求普通文件；`lstat/fstat` 与 open descriptor 是权威，相对路径父级 symlink、相对最终分量 symlink、绝对路径任一中间 symlink及绝对最终分量 symlink都必须拒绝，但合法绝对 PNG 必须成功。最终 fd 立即 `fstat` 验证 `MAX_MEDIA_BYTES`，从同一 held fd 循环读取最多 `MAX_MEDIA_BYTES + 1` bytes 后关闭；以 `BytesIO(payload)` 在 `warnings.catch_warnings()` 内完成 `Image.open`、format allowlist、size/pixel limit 与 `verify()`，返回 immutable payload、width、height 和 SHA-256。明确运行期 ENOENT 语义：若 media 在 parse 时存在但 emit 前文件或任一目录分量消失，objects/emitter 不修改 `PhysicalDeckPlan`，而是创建本地 editable missing-media placeholder，并由 `emit_deck()` 返回或抛送结构化 runtime diagnostic（稳定 code `MEDIA_MISSING`、severity error、logical/physical index、bounded generic message/fix，不含绝对路径）。修订 `pptx_render.render_command()`，把 emitter runtime media diagnostics 合并到既有 parser/plan error set 与 affected slides；随后仍按 D-17 走既有 best-effort 双产物发布，输出同 stem `.md/.pptx`、明确异常与 `MEDIA_MISSING`、退出非零，不把 runtime missing 伪装成成功，也不重建或修改 frozen plan。ELOOP、symlink、非目录中间分量、非普通最终文件返回稳定 bounded media/path domain code。`add_contain_picture()` 的 contain geometry 只使用 validated width/height，并把新的 `BytesIO(payload)` 传给 `slide.shapes.add_picture()`，绝不再次传 path；保留 crop 四值为 0。对 descriptor open/read 与 add_picture 的第三方异常建立稳定映射：DecompressionBombError/Warning -&gt; `PPTX_MEDIA_PIXEL_LIMIT`，UnidentifiedImageError、decode/stream `OSError`、SyntaxError、ValueError -&gt; `PPTX_MEDIA_FORMAT_INVALID`，超字节 -&gt; `PPTX_MEDIA_SIZE_LIMIT`，未知异常仍由既有 `PPTX_OBJECT_INVALID` 边界兜底；不得包含 path、repr 或 Pillow 原文。新增默认 `None` 的 module hooks：payload 验证后/`add_picture` 前替换 hook，以及 parse 完成后/emit 前删除 hook；生产默认均为 `None` 且测试后 finally 恢复。新增并注册 `media-descriptor-binding` gate：先准备合法 1x1 PNG，在验证后 hook 中把 authored path 原子替换为声明 20000x10000 的 bomb PNG，断言命令仍使用原 validated bytes，embedded media hash 等于原 payload且不等于 replacement hash、crop=0、同 stem 双产物存在。增加显式绝对 PNG public 成功向量，断言 exit=0、native picture/hash/crop 正确；增加相对路径中间 symlink、相对最终 symlink 与绝对路径 symlink public 失败向量，断言稳定 bounded code、不读取 target、不发布 Markdown且旧 PPTX byte-identical。增加“parse 时存在、emit 前删除”故障注入向量，断言成品含 editable missing-media placeholder、同 stem Markdown/PPTX 均发布、输出含稳定 `MEDIA_MISSING` 与受影响逻辑页、exit != 0、frozen plan projection 前后相等。保留 add_picture 故障注入向量，强制 Pillow/stream 异常并断言非零 stable code、`output_bytes &lt; 8192`、无 traceback/path/Pillow 原文、旧 PPTX byte-identical。gate 返回相对/绝对成功证据、hook 状态、public exit/code、validated/embedded/replacement hashes、三类 symlink 结果、runtime-missing diagnostic/placeholder/artifact inventory、output_bytes、old_target_preserved 与 artifact paths，供 Task 3 消费。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/pptx_render.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py media-descriptor-binding &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py object-error-bounded</automated>
  </verify>
  <acceptance_criteria>
    - R43-W03 source assertion: `_media_path()` 不调用 `resolve()`，production picture path 不再出现 `path.is_file()` 或 `_safe_image_size(path)` 后 `add_picture(str(path))`；相对路径从 Markdown 父目录 fd、绝对路径从 `/` fd 开始逐分量 no-follow traversal，校验和发射只消费同一最终 held descriptor 读出的 immutable bytes。
    - Prior-contract assertion: 显式绝对 PNG public 向量成功退出 0、生成 native picture 且 embedded hash/crop 正确；绝对路径 symlink 向量 fail closed，不因修复竞态而拒绝全部合法绝对路径。
    - Race assertion: 校验后把 authored path 替换为 bomb PNG 不会改变 embedded media；validated hash == embedded hash 且 != replacement hash，命令无裸 `PIL.Image.DecompressionBombError`、traceback 或绝对路径。
    - Bounded fallback assertion: add_picture 故障注入只产生 allowlisted stable code，公开输出低于 8KiB，旧 PPTX byte-identical且 Markdown 不发布。
    - PPTX-05 assertion: 正常/竞态成功成品仍为 native picture relationship、contain 比例、crop left/top/right/bottom 全 0，不栅格化整页。
    - Runtime-missing assertion: parse 时存在、emit 前删除向量生成 editable placeholder，同 stem Markdown/PPTX 均发布，输出稳定 `MEDIA_MISSING` 与受影响页，exit != 0，且 frozen plan projection 前后相等。
    - Safety assertion: 相对路径中间/最终 symlink 与绝对路径 symlink 黑盒向量均 fail closed；不联网、不读取引用路径之外的文件，所有 traversal/final fd 在成功/失败路径关闭，payload 大小受 `MAX_MEDIA_BYTES` 限制。
  </acceptance_criteria>
  <done>R43-W03 由单次 descriptor 绑定与校验后替换故障注入证据关闭。</done>
</task>

<task type="auto">
  <name>Task 3: 从真实 gate evidence 构造 blocking gap outcome audit（R43-W02）</name>
  <files>skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-08-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-08-SUMMARY.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-09-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-09-SUMMARY.md
    - .planning/REQUIREMENTS.md
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/scripts/school-pptx.sh
  </read_first>
  <action>把 `mixed-fragment-capacity` 与 `media-descriptor-binding` 注册到 `GATES`、`PHASE_43_GATE_ORDER`、`PHASE_43_REQUIRED_GATES` 与 argparse choices；固定顶层 gate 数从 18 增为 20，保持唯一、exact called order 和 `dynamic_skips=0`。扩展 `GAP_COVERAGE` 精确加入 `R43-C03: ("mixed-fragment-capacity",)`、`R43-W03: ("media-descriptor-binding",)`、`R43-W02: ("mixed-fragment-capacity", "media-descriptor-binding")`，同时保留既有历史与 R43-C01/C02/W01 映射。把 `REQUIREMENT_COVERAGE` exact set 明确扩为 `{"PPTX-03", "PPTX-04", "PPTX-05", "PPTX-08", "PPTX-10", "VER-03"}`：`PPTX-05` 必须映射 `media-descriptor-binding`、`editable-objects`、`emit-structure`；`PPTX-08` 必须直接包含 `mixed-fragment-capacity`；`PPTX-10` 必须保留 `code-literal-roundtrip`；`VER-03` 必须直接包含 `media-descriptor-binding`。重写 `run_phase_43()` 的 `gap_outcome_audit`：从 `evidence["mixed-fragment-capacity"]["mixed-fragment-capacity"]` 与 `evidence["media-descriptor-binding"]["media-descriptor-binding"]` 取值，先以 `require()` 校验 title-content/two-column 的真实 exit、frozen/reopened 字号与排版参数、物理页、`display_height_points &lt;= effective_content_height_points`、逐页/连接后 text equality、输出边界与 artifact inventory；再校验相对与绝对 PNG 成功证据、media hook/hash binding、相对中间/最终 symlink 与绝对 symlink stable failure code、runtime `MEDIA_MISSING` 的非零 exit/placeholder/同 stem 双产物/frozen projection equality、`output_bytes &lt; 8192`、old target preserved 与路径 inventory。audit 只复制这些已校验值，禁止字面量 `True`、`failure_vectors: 4` 或固定成功计数。增加 source/AST regression，若 `gap_outcome_audit` assignment 中出现未源自 gate evidence 的成功布尔/向量常量则失败。aggregate evidence 中三个新 gap 各自包含 gate 名和完整派生证据，并继续证明 canonical render、Phase 41/42 regression 与 Phase 44 scope fence。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py mixed-fragment-capacity &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py media-descriptor-binding &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py phase-43</automated>
  </verify>
  <acceptance_criteria>
    - Registry assertion: Phase 43 required/called order 精确包含 20 个唯一 gate；两个新 gate 实际执行，删除、跳过、重复或重排任一项均非零。
    - R43-W02 evidence assertion: `gap_outcome_audit` 的所有 success/failure 字段均从本次 gate evidence 读取并经 `require()` 校验；源码中不存在硬编码 `public_success: True`、`bounded_output: True` 或固定 `failure_vectors`。
    - Gap coverage assertion: aggregate 精确覆盖 R43-C03、R43-W02、R43-W03，并保留历史 C/W 与 R43-C01/C02/W01 映射；每个映射只引用实际 called gates。
    - Requirement assertion: exact set 包含 PPTX-05；PPTX-05 映射 `media-descriptor-binding` + `editable-objects` + `emit-structure`，PPTX-08 直接受 `mixed-fragment-capacity` 阻断，PPTX-10 保留 `code-literal-roundtrip`，VER-03 直接受 `media-descriptor-binding` 阻断。
    - Scope assertion: 不新增 public `verify --workdir`、SKILL.md、runtime adapter、README/compatibility matrix、PowerPoint/WPS UAT 或任何 Phase 44 声明。
  </acceptance_criteria>
  <done>R43-W02 被真实数值 evidence、固定 registry 和 requirement-direct blocking audit 关闭。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| mixed-code 静默裁切 | 16pt 规划、24pt 发射导致合法成功 PPTX 超槽，教师在 viewer 中丢失可见代码 | 单一 frozen target-slot 字号；按 24pt/22pt 规划并发射；每页 reopen display-height blocking gate。 |
| emitter 再决策 | 缺失 frozen 字号时回退 manifest，重新制造 plan/emitter 漂移 | `PPTX_PLAN_FONT_MISSING` fail closed；AST/source gate 禁止 fallback 与 paginator import。 |
| 图片 TOCTOU / symlink replacement | 校验后路径替换为 bomb/恶意内容，第二次打开泄漏 Pillow 异常或发射未验证 bytes | `O_NOFOLLOW` held fd、fstat、bounded single read、immutable bytes、BytesIO add_picture；校验后替换 hash gate。 |
| 合法绝对媒体回归 | 只允许 media_root 相对路径会破坏既有绝对图片输入契约 | 相对路径从 Markdown 父目录 fd、绝对路径从 `/` fd 逐分量 no-follow；显式绝对 PNG 成功 gate。 |
| parse/emit 间媒体消失 | parser 已接受媒体但发射时 ENOENT，若只画 placeholder 会被错误报告为成功 | emitter 返回结构化 `MEDIA_MISSING`，render 合并 error 后发布 best-effort 双产物并退出非零；plan projection 保持不变。 |
| 恶意图片资源耗尽 | 大文件、超像素、bomb warning/error 消耗内存或 CPU | `MAX_MEDIA_BYTES + 1` 上限、像素上限、warning 提升、allowlisted domain code；High 失败阻断。 |
| 第三方异常泄漏 | Pillow/python-pptx repr、traceback、绝对路径进入公开 CLI | 精确 exception mapping、通用中文消息、8KiB 输出上限、路径/禁词审计与旧目标保全。 |
| aggregate false green | gate 被调用但 audit 写死成功，未验证的属性被报告为已关闭 | outcome 只从实际 nested evidence 派生；require 数值/路径/hash；AST 拒绝成功常量。 |
| 测试钩子滥用 | 故障注入 hook 在生产环境产生副作用 | hook 默认 `None`、仅模块内显式赋值、finally 恢复；aggregate 断言 canonical render 未触发测试替换。 |
</threat_model>

<verification>
- `export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l"`
- `SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}"; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml'`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py mixed-fragment-capacity`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py media-descriptor-binding`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py object-error-bounded`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py phase-43`
- `git diff --check`
</verification>

<success_criteria>
- R43-C03：mixed title-content/two-column code 按实际 24pt/22pt 与同一 frozen 排版参数规划和发射，超量增加物理页，每页 display height 不超过 effective content height，代码文本逐字符保真。
- R43-W03：相对图片从 Markdown 父目录 fd、绝对图片从 `/` fd 逐分量 no-follow 并只读一次；合法绝对 PNG 成功，绝对 symlink fail closed，校验后 path 替换不改变 embedded media。
- Runtime missing：parse 时存在、emit 前删除产生 editable placeholder、稳定 `MEDIA_MISSING`、同 stem best-effort 双产物与非零退出，且 frozen plan 不变。
- R43-W02：aggregate outcome audit 完全由两个新 gate 的真实 exit/font/height/path/hash evidence 推导，无硬编码成功值或固定向量数。
- PPTX-05、PPTX-08、PPTX-10、VER-03 的直接与回归 gates 全部通过；Phase 41/42 aggregate 无回归。
- Phase 44 的 public verify、runtime/UAT/discoverability 工作未被引入或宣称完成。
</success_criteria>

<source_audit>
| Source | Coverage |
|---|---|
| 43-VERIFICATION R43-C03 | Task 1 冻结实际 target-slot 字号、扩页与 reopen display-height gate |
| 43-VERIFICATION R43-W03 | Task 2 相对/绝对 traversal-root descriptor、校验后替换与 bounded add_picture fault gate |
| 43-VERIFICATION R43-W02 | Task 3 从实际 gate evidence 派生 aggregate outcome，拒绝成功常量 |
| CONTEXT D-03 | Task 1 保持 code 源行与 soft-wrap 文本不变，同时按真实显示字号计高 |
| CONTEXT D-17/D-19 | Task 2 runtime missing 结构化诊断、best-effort 双产物非零发布，以及不可恢复 symlink/object failure 的旧 PPTX 保全 |
| Prior media contract | Task 2 显式绝对 PNG 成功与绝对路径 symlink 失败向量，避免绝对路径功能回归 |
| ROADMAP Phase 43 criteria 1-3 | public render 可重复；native media/code 可编辑；长 code 按预算扩页 |
| REQUIREMENTS PPTX-05/PPTX-08/PPTX-10/VER-03 | Task 1/2 定向 gates，Task 3 direct requirement coverage |
| Phase 44 boundary | 所有任务显式排除 runtime notes、discoverability、public verify 与 viewer UAT |
</source_audit>
