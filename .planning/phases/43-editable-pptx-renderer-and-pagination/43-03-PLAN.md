---
phase: 43-editable-pptx-renderer-and-pagination
plan: 03
type: execute
wave: 3
depends_on:
  - 43-02
files_modified:
  - skills/school-pptx/scripts/pptx_emit.py
  - skills/school-pptx/scripts/pptx_objects.py
  - skills/school-pptx/scripts/pptx_ooxml.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-01
  - PPTX-02
  - PPTX-03
  - PPTX-04
  - PPTX-05
  - PPTX-06
  - PPTX-07
  - PPTX-08
  - PPTX-09
  - PPTX-10
  - PPTX-11
  - PPTX-12
  - SKILL-03
must_haves:
  truths:
    - "D-11: 表格数据本体是 native PPTX table，不允许 text-box matrix 替代。"
    - "D-12/D-13/D-14: timeline node 与 gallery card 是 native editable group；axis 独立；空 caption/table-name 文本框仍存在且无 prompt。"
    - "D-15: bold/highlight 是同一连续文本框中的 native runs；行内 span offsets 含 Markdown delimiters，归一化必须删除 **/== 而不损失或重排可见文本。"
    - "D-16: 仅有 authored notes 的物理页访问 notes_slide；同一逻辑页全部派生页备注一致，无备注页没有 notes relationship。"
    - "模板五张 seed slide 与其中两份 notes relationship 在发射前受控移除；成品 closing 恰好一个并使用 manifest part path。"
    - "PPTX-12 诚实采用 transition mode none；本阶段不生成不稳定 transition，none 仍为有效输出。"
  artifacts:
    - path: "skills/school-pptx/scripts/pptx_emit.py"
      provides: "从冻结计划机械发射模板一致 PPTX，并执行 staged package structural validation。"
    - path: "skills/school-pptx/scripts/pptx_objects.py"
      provides: "原生富文本、table、contain picture、group、code、placeholder 与 notes helpers。"
    - path: "skills/school-pptx/scripts/pptx_ooxml.py"
      provides: "仅 seed slide removal 与 run highlight 的窄 OOXML 兼容层。"
  key_links:
    - from: "skills/school-pptx/scripts/pptx_emit.py"
      to: "skills/school-pptx/scripts/pptx_model.py"
      via: "只读 PhysicalDeckPlan；不得重新分页"
      pattern: "PhysicalDeckPlan"
    - from: "skills/school-pptx/scripts/pptx_emit.py"
      to: "skills/school-pptx/templates/standard-school.pptx"
      via: "按 slide layout partname bootstrap，先移除 5 张 seed"
      pattern: "slideLayout|remove_seed"
    - from: "skills/school-pptx/scripts/pptx_objects.py"
      to: "skills/school-pptx/templates/standard-school.manifest.yaml"
      via: "所有 geometry/font/theme token 来自 manifest"
      pattern: "geometry|text_budget|scheme_color"
---

<objective>
把冻结物理页计划机械地发射成可重开、非空、原生可编辑的 PPTX 对象。

Purpose: 满足 editability、template consistency、notes/media/native object 契约，同时隔离 python-pptx public API 缺口。
Output: emitter、原生对象 helper、窄 OOXML helper 与完整 fixture 结构回归门。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
@skills/school-pptx/templates/standard-school.manifest.yaml
@skills/school-pptx/templates/standard-school.pptx
@skills/school-pptx/scripts/markdown_contract.py
@skills/school-pptx/scripts/pptx_model.py
@skills/school-pptx/scripts/pptx_paginate.py
@skills/school-pptx/fixtures/school-pptx-full.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现模板 bootstrap 与隔离 OOXML helper</name>
  <files>skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/pptx_ooxml.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
    - skills/school-pptx/templates/standard-school.pptx
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/scripts/template_report.py
  </read_first>
  <action>使用 `python-pptx 1.0.2` public API 打开受控 template，并按 slide layout partname 建映射；不得按 localized name 或 `slide_layouts[n]`。在 `pptx_ooxml.py` 封装唯一 seed removal helper，通过 `_sldIdLst` 与 relationship 删除模板全部 5 张 seed slide，并验证 zero slides 后才发射；封装幂等 `set_run_highlight(run, scheme_color)`，按 schema 顺序创建 `<a:highlight><a:schemeClr>`，不得硬编码 RGB。bootstrap 后追加 plan slides，closing 使用 manifest 的 `slideLayout7.xml`。启动时检查 `python-pptx>=1.0.2,<2` 与 Pillow；缺失/不兼容抛 bounded domain error，不输出 traceback；Phase 44 的 aggregate readiness command 不在此实现。</action>
  <verify>
    <automated>python3 skills/school-pptx/scripts/verify_pptx_renderer.py ooxml-bootstrap</automated>
  </verify>
  <acceptance_criteria>
    - Seed assertion: 输入模板 5 slides/2 notes relationships，bootstrap 后 0 slides/0 seed relationships，保存重开成功。
    - Layout assertion: 所有 layout 由 OPC part path 解析；错误/缺失 part 非零失败，不 fallback 到索引。
    - Highlight assertion: helper 二次调用幂等、scheme token 来自 manifest、保留既有 bold/text，ZIP XML 可见且可重开。
    - Dependency assertion: 缺失/版本不兼容产生 `PPTX_DEPENDENCY_MISSING` 或稳定等价码，无 pip install、无 traceback。
  </acceptance_criteria>
  <done>模板可安全清空种子页并按机器 part path 为原生对象发射做好准备。</done>
</task>

<task type="auto">
  <name>Task 2: 实现原生文本、表格、媒体、组合与备注对象</name>
  <files>skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/pptx_ooxml.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_ooxml.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
  </read_first>
  <action>实现 `add_rich_text`，先基于 Phase 42 `inline_spans()` 的 authored-string offsets 归一化：span start/end 包含 `**`/`==` 分隔符，必须一次删除 delimiter、生成连续 plain/bold/highlight segment，验证 adjacent/mixed spans；不重新解析 Markdown，nested/overlap 用稳定 diagnostic 拒绝。按 plan 字号发射 text/code，开启 word_wrap、关闭 autofit，代码一个连续等宽文本框并保持源换行。用 `add_table()` 创建每页 native table，显式 row/column size、重复 header 和空/非空 table-name shape。图片以固有宽高比 contain 计算后 `add_picture()`，crop 四值为 0；缺失媒体只生成本地 native placeholder，不联网、不猜路径。timeline 每 node 用 `add_group_shape()` 包含 marker/time/title/description，axis 组外；gallery 每 card 组合 picture + 永远存在 caption textbox。仅 notes intent 为真时访问 `slide.notes_slide` 并写同一备注；无 notes 时绝不访问。</action>
  <verify>
    <automated>python3 skills/school-pptx/scripts/verify_pptx_renderer.py editable-objects</automated>
  </verify>
  <acceptance_criteria>
    - D-11 assertion: 每个 table physical page 恰有 native `<a:tbl>`，header/data 行结构符合 plan，无 textbox matrix。
    - D-12/D-13 assertion: timeline/gallery 重开后仍是 `<p:grpSp>` native children，axis 独立，picture 可选且 caption 可编辑。
    - D-14 assertion: 空 caption/table-name shape 名稳定、text 为空、无“单击添加”提示。
    - D-15 assertion: `**`/`==` 不进入 visible text；bold `b=1` 与 scheme highlight 共存且纯文本顺序完整。
    - D-16 assertion: notes relationships 精确匹配 plan，全有全无，无备注 slide 不创建 notes part。
    - Media assertion: picture 关系有效、宽高比保持、crop 全 0；缺失媒体不读取 authored path 之外的文件。
  </acceptance_criteria>
  <done>全部受支持内容以原生、可编辑、模板控制的 PPTX 对象表达。</done>
</task>

<task type="auto">
  <name>Task 3: 完成 staged PPTX 结构验证与 full fixture 门</name>
  <files>skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/fixtures/school-pptx-full.md
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/pptx_ooxml.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
  </read_first>
  <action>在 emitter 内增加 staged package validator：先限制 ZIP entry 数、压缩/解压大小与关系数量，再用 zipfile/安全 XML 检查 content types、slide/layout/media/notes relationships、无 `../` target、无 external relationship，最后用 `python-pptx` reopen；失败抛 `PPTX_PACKAGE_INVALID`，不触碰最终路径。扩展 `full_fixture_structure_gate`，从 canonical fixture 构造 plan、发射到 TemporaryDirectory，断言非空、物理页数、无 seed 文本、closing 一次且末页、layout part 映射、native text/table/picture/group/highlight/notes/code round-trip、gallery≤4、无 full-slide screenshot。记录内部 transition mode 为 `none`，不写 transition XML；视觉/编辑体验的 PowerPoint/WPS UAT 明确留给 Phase 44。</action>
  <verify>
    <automated>python3 skills/school-pptx/scripts/verify_pptx_renderer.py emit-structure</automated>
  </verify>
  <acceptance_criteria>
    - PPTX-01 assertion: canonical fixture 产生可重开、非空 PPTX，slide count 与 plan 完全一致。
    - PPTX-02/03 assertion: 可见文本、代码、timeline label、caption 均在 native text/run，整页 picture shortcut 不存在。
    - PPTX-04..11 assertion: native table、contain picture、gallery/timeline expansion、续表规则、code、notes 均有 ZIP + reopen 证据。
    - PPTX-12 assertion: transition mode 明确为 `none`，absence 不失败，未生成复杂动画/转场。
    - Boundary assertion: 不新增 public CLI、不发布 delivery artifact、不执行 LibreOffice/PowerPoint/WPS 人工视觉验收。
  </acceptance_criteria>
  <done>完整 fixture 的 staged PPTX 在发布前已经通过结构、可编辑对象与包完整性门。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| 恶意 PPTX ZIP/XML | ZIP bomb、关系穿越、外部关系、XXE、畸形 package | entry/size/relationship 上限，拒绝 `..` 与 External，安全解析 + reopen；任一 High 阻断发射。 |
| 恶意媒体 | 超大图、解码 bomb、伪装格式、路径逃逸或联网回退 | 只使用 canonical resolved file；Pillow 像素/字节上限与格式 allowlist；禁 URL/CWD/sibling fallback；缺失用本地 shape。 |
| 私有 OOXML API | 删除错误关系、破坏 bold/highlight 或 package | helper 极小且幂等；保存、ZIP XML、reopen 三重测试；不手写 OPC package。 |
| notes/seed 泄漏 | 模板 seed 内容或既有 notes 被带入公开 deck | bootstrap 必须从 5 清到 0，结构 gate 搜索 seed text/relationship；High 失败阻断。 |
| 资源耗尽 | 数千 shape/group/table cell 或超大压缩包 | 上游 block 上限、每页对象预算、package size cap、bounded diagnostics。 |
| 诊断泄漏 | 图片路径、XML、source text 或 traceback 暴露 | domain error 仅 code/位置/fix，不 dump XML/媒体/源文；负例断言。 |
| 文件发布/TOCTOU | 本计划 staged 文件被误当 final 或符号链接替换 | 只写 verifier-owned TemporaryDirectory，不接受 public out-dir；最终 descriptor/atomic replace 在 Plan 04 实现。 |
</threat_model>

<verification>
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py emit-structure`
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py pagination`
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py contract-model`
</verification>

<success_criteria>
- canonical fixture 可发射为非空、可重开、无 seed、一个 closing 的 PPTX。
- D-11..D-16 全部由对象结构和 round-trip 证据落实。
- 所有对象 native/editable，contain 无裁剪，notes 精确，transition 诚实为 none。
</success_criteria>

<source_audit>
| Source | Coverage |
|---|---|
| ROADMAP criteria 1-4 | Task 3 non-empty/结构，Task 2 editable objects 与 pagination materialization |
| CONTEXT D-11..D-16 | Task 2 逐项实现与验收 |
| RESEARCH 5 seed slides | Task 1 清除 5 张及 2 notes relationship，Task 3 搜索泄漏 |
| RESEARCH closing mapping | Task 1/3 按 `slideLayout7.xml` relationship 断言 |
| RESEARCH inline delimiter offsets | Task 2 明确 authored offsets 含 delimiters 并做 adjacent/mixed tests |
| UI-SPEC Editable/Media/Notes | Task 2/3 结构 evidence；手工 viewer UAT 延期 Phase 44 |
</source_audit>
