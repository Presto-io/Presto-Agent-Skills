---
phase: 43-editable-pptx-renderer-and-pagination
plan: 04
type: execute
wave: 4
depends_on:
  - 43-03
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - skills/school-pptx/references/markdown-contract.md
  - skills/school-pptx/scripts/pptx_render.py
  - skills/school-pptx/scripts/school-pptx.sh
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-01
  - PPTX-02
  - PPTX-13
  - VER-03
  - SKILL-03
must_haves:
  truths:
    - "D-17/D-18: invalid Markdown 或缺失媒体可发布同名、无警告页/横幅/水印的结构有效 best-effort PPTX，但命令必须非零并明确异常、受影响逻辑页和非成功状态。"
    - "D-19: staged PPTX 在同目录完成 ZIP/XML + reopen 校验后才单文件原子替换；不可恢复失败保留旧 PPTX且无公开 .bak。"
    - "D-20/D-21: 标准成功产物固定为同 stem 的 Markdown + PPTX 两文件，Markdown 字节原样保留；这有意修订 PPTX-13 的显式复制旧假设。"
    - "双文件无法形成整体原子事务：先各自暂存并验证，再原子发布 Markdown，最后原子发布 PPTX 作为 deck-ready 提交点；文档与 CLI 不宣称跨文件原子性。"
    - "render 直接 import canonical parse_document，shell 只做薄 dispatch；所有实现 skill-local，不调用 sibling skill scripts。"
  artifacts:
    - path: "skills/school-pptx/scripts/pptx_render.py"
      provides: "render 状态机、bounded dependency/error summary、安全双产物 staging 与 PPTX-last publication。"
    - path: "skills/school-pptx/scripts/school-pptx.sh"
      provides: "公开 `render --input --out-dir [--stem]` 薄分发。"
    - path: "skills/school-pptx/references/markdown-contract.md"
      provides: "D-20/D-21 双产物、best-effort 与原子性诚实边界。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "Phase 43 结构回归、发布安全与 Phase 41/42 regression aggregate gate。"
  key_links:
    - from: "skills/school-pptx/scripts/school-pptx.sh"
      to: "skills/school-pptx/scripts/pptx_render.py"
      via: "render dispatch 并原样传播退出码"
      pattern: "render"
    - from: "skills/school-pptx/scripts/pptx_render.py"
      to: "skills/school-pptx/scripts/markdown_contract.py"
      via: "in-process canonical parse_document，不经 JSON/subprocess"
      pattern: "parse_document"
    - from: "skills/school-pptx/scripts/pptx_render.py"
      to: "skills/school-pptx/scripts/pptx_emit.py"
      via: "validated staged package 后执行 Markdown-first/PPTX-last commit"
      pattern: "os.replace|publish"
---

<objective>
交付可重复 render 命令、诚实 best-effort 语义和安全、干净的标准双产物发布边界。

Purpose: 完成用户可调用的 Phase 43 工作流，同时保证异常非零、旧 PPTX 保全和 caller-owned 路径安全。
Output: render CLI、双产物契约文档、descriptor-confined publisher 与 Phase 43 aggregate structural regression gate。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
@.planning/phases/42-markdown-contract-and-full-fixture/42-03-SUMMARY.md
@skills/school-pptx/references/markdown-contract.md
@skills/school-pptx/scripts/markdown_contract.py
@skills/school-pptx/scripts/school-pptx.sh
@skills/school-pptx/scripts/pptx_paginate.py
@skills/school-pptx/scripts/pptx_emit.py
@skills/school-pptx/scripts/verify_markdown_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修订双产物与异常公开契约</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, skills/school-pptx/references/markdown-contract.md, skills/school-pptx/scripts/school-pptx.sh</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - skills/school-pptx/references/markdown-contract.md
    - skills/school-pptx/scripts/school-pptx.sh
  </read_first>
  <action>在任何 CLI 代码改动之前，先修订 canonical 规划契约：将 `.planning/ROADMAP.md` 的 Phase 43 Success Criteria 5 改为标准成功交付固定包含同 stem 的 Markdown + PPTX 两个文件，同时明确 manifest、logical JSON、diagnostics、logs、debug、evidence 与临时文件不进入公开目录；不得保留“仅显式请求才复制 Markdown”的旧规则。再修订 `.planning/REQUIREMENTS.md`：把 PPTX-13 改为同一固定双产物规则，Markdown bytes 原样保留，manifest、logical JSON、diagnostics、logs、debug、evidence 与临时文件不进入公开目录；同步该需求的 Traceability 映射和文档更新时间，使 ROADMAP Success Criteria 5、PPTX-13 与 D-20/D-21 一致。然后在 markdown-contract 增加 `render --input &lt;reviewed.md&gt; --out-dir &lt;delivery-dir&gt; [--stem &lt;name&gt;]`，明确成功标准固定为 `&lt;stem&gt;.md` + `&lt;stem&gt;.pptx`，Markdown 按输入 bytes 保存，且不再表达“显式请求才复制 Markdown”。说明三个状态：成功退出 0；结构有效 best-effort 发布两文件但退出非零；不可恢复时不替换旧 PPTX。明确两个独立文件没有跨文件原子事务，固定先 Markdown、后 PPTX，PPTX 是 deck-ready commit；异常 PPTX 无警告页/水印，调用 agent 必须复述异常和受影响页。扩展 shell usage/commands 并新增独立 render dispatch 到 `${SCHOOL_PPTX_PYTHON:-python3} pptx_render.py`，保留 validate/example/template-report/info 行为，shell 不实现业务逻辑。</action>
  <verify>
    <automated>skills/school-pptx/scripts/school-pptx.sh --help | grep -F 'render --input &lt;reviewed.md&gt; --out-dir &lt;delivery-dir&gt; [--stem &lt;name&gt;]' &amp;&amp; rg -n '^  5\..*(Markdown.*PPTX|PPTX.*Markdown)' .planning/ROADMAP.md &amp;&amp; rg -n '^.*\*\*PPTX-13\*\*:.*(Markdown.*PPTX|PPTX.*Markdown)' .planning/REQUIREMENTS.md &amp;&amp; rg -n '(&lt;stem&gt;\.md.*&lt;stem&gt;\.pptx|&lt;stem&gt;\.pptx.*&lt;stem&gt;\.md)' skills/school-pptx/references/markdown-contract.md &amp;&amp; ! rg -n 'explicitly requested Markdown|only.*explicit.*Markdown|仅显式请求.*Markdown|显式请求才复制 Markdown' .planning/ROADMAP.md .planning/REQUIREMENTS.md skills/school-pptx/references/markdown-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - D-20/D-21 assertion: `.planning/ROADMAP.md` Phase 43 Success Criteria 5、`.planning/REQUIREMENTS.md` 的 PPTX-13 和 `skills/school-pptx/references/markdown-contract.md` 均明确同 stem Markdown + PPTX 两文件固定构成标准成功产物。
    - Canonical requirement assertion: ROADMAP Success Criteria 5、PPTX-13、Traceability、REQUIREMENTS 更新时间和 markdown-contract 已同步，三处均不再出现“显式请求才复制 Markdown”的旧规则。
    - Clean output assertion: 三处 canonical 文档均明确除固定 `.md/.pptx` 外，manifest、logical JSON、diagnostics、logs、debug、evidence 与临时文件保持隐藏或仅位于验证工作目录。
    - Atomicity assertion: 文档不使用“成对原子/事务”表述，明确 Markdown-first、PPTX-last crash window。
    - D-17/D-18 assertion: best-effort 文案包含非零、异常、受影响页、修正后覆盖/手工修改建议，且禁止成功措辞与可见警告污染。
    - Dispatcher regression: 既有四个命令 usage/dispatch 不变，render 退出码不被 shell 改写。
  </acceptance_criteria>
  <done>用户和 agent 获得与 D-17..D-21 一致、对原子性诚实的公开命令契约。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 render 状态机与描述符约束发布</name>
  <files>skills/school-pptx/scripts/pptx_render.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/markdown_contract.py
  </read_first>
  <action>实现 argparse 与三态 orchestration：读取输入原始 bytes；in-process 调 `load_manifest`/`parse_document`；保留 canonical errors 继续安全 paginate/emit；staged PPTX 结构有效且无 errors 时成功，结构有效但有 errors 时 best-effort 非零，不可恢复时不发布 PPTX。创建 renderer-owned held-descriptor publisher：验证 out-dir/stem，拒绝空 stem、分隔符、`.`/`..`、input/output identity、directory collision、symlink final、root exchange；用 `O_NOFOLLOW|O_EXCL` 在同目录创建固定前缀 temp，写/fsync 两文件，发射器校验 staged PPTX 后重新核对 `(st_dev,st_ino)`，先 `os.replace` Markdown、最后 `os.replace` PPTX。只清理当前 descriptor 下命令自有普通 temp，不 glob/rmtree/跟随链接；不生成 `.bak`、JSON、log、manifest/evidence。输出严格按 UI-SPEC 成功/best-effort/unrecoverable 顺序，复用 200 条 bounded diagnostics，列 affected logical slides，无 traceback；依赖缺失也走不可恢复 bounded error。</action>
  <verify>
    <automated>python3 -m py_compile skills/school-pptx/scripts/pptx_render.py &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py cli-publication</automated>
  </verify>
  <acceptance_criteria>
    - VER-03 positive: canonical fixture render 退出 0，目录命令自有产物恰为同 stem `.md/.pptx`，PPTX 非空可重开，Markdown bytes 完全相同。
    - D-17/D-18 negative: 缺失媒体/invalid Markdown 发布同名结构有效 PPTX 并非零，输出明确非成功、异常数和受影响页，无 warning slide/banner/watermark。
    - D-19 unrecoverable: staged corruption、依赖缺失或 package failure 不替换旧 PPTX，命令 temp 全清，无 `.bak`。
    - Publication safety: destination/root symlink exchange、目录碰撞、stem traversal、input/output identity 均 fail closed，caller sentinel hash 不变。
    - Honest pair assertion: fault injection 在两次 replace 间可观察 Markdown 新/PPTX 旧窗口，CLI/文档从不宣称整体原子；正常完成以 PPTX-last 提交。
  </acceptance_criteria>
  <done>render 可安全发布成功或诚实 best-effort 双产物，并在不可恢复失败时保护旧 PPTX。</done>
</task>

<task type="auto">
  <name>Task 3: 关闭 Phase 43 结构、安全与回归门</name>
  <files>skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/scripts/verify_markdown_contract.py
    - skills/school-pptx/scripts/school-pptx.sh
    - skills/school-pptx/scripts/pptx_render.py
    - skills/school-pptx/fixtures/school-pptx-full.md
  </read_first>
  <action>扩展 `phase-43` aggregate registry，按固定顺序实际运行 contract-model、pagination、ooxml-bootstrap、editable-objects、emit-structure、cli-publication、best-effort、publication-safety、determinism、phase_41_42_regression。黑盒调用 public render，两次运行比较 physical-plan 投影和 semantic package inventory，不强求 PPTX ZIP bytes 一致。断言成功 public root 除 caller sentinel 外命令自有文件恰为两件；异常非零且无成功措辞；所有故障注入无 traceback、输出长度有界、旧 PPTX/无关文件保全、无 temp/log/debug/manifest/JSON。运行 Phase 41 template-report 与 Phase 42 fixture-example。做 import/call graph audit，新增运行时代码全部位于 school-pptx/scripts 且无 sibling skill 调用。不要创建 Phase 44 public `verify --workdir`、dependency aggregate、runtime adapter、README/compatibility matrix、manual PowerPoint/WPS UAT。</action>
  <verify>
    <automated>python3 skills/school-pptx/scripts/verify_pptx_renderer.py phase-43</automated>
  </verify>
  <acceptance_criteria>
    - Coverage assertion: aggregate registry 名称唯一且无 skip，遗漏任一 contract/pagination/object/publication/security/regression gate 即非零。
    - Requirements assertion: PPTX-01..13、VER-03、SKILL-03 的每项结构/行为证据均可由 gate 名与断言定位。
    - Decision assertion: D-01..D-21 每项至少被一个执行 gate 直接断言，而非只出现在文档。
    - Security assertion: ASVS L1 High 风险全部 blocking；恶意 Markdown/media/PPTX、ZIP/XML、路径穿越、symlink/TOCTOU、atomic replace、诊断泄漏、资源耗尽均有负例。
    - Scope assertion: Phase 44 的 VER-04..10、SKILL-01/02/04/05/06、aggregate public verify、runtime docs 和 manual viewer UAT 未被实现或宣称完成。
  </acceptance_criteria>
  <done>Phase 43 所有功能、决策、安全与前序回归都有一条可重复的结构自动门。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| 恶意 Markdown/YAML/media | 解析绕过、外部读取、图片 bomb、资源耗尽 | 复用 canonical limits；媒体只读 resolved file、格式/像素/字节上限、无网络 fallback；best-effort 仍非零。 |
| 恶意 PPTX ZIP/XML | staged package bomb、关系穿越、外部关系或恶意 XML | Plan 03 validator 在 publish 前 blocking；fault injection 证明无效包不替换 final。 |
| 路径穿越/stem 注入 | `../`、绝对 stem、分隔符覆盖任意文件 | stem allowlist + descriptor-relative fixed sibling names；负例确保 outside sentinel 不变。 |
| symlink/TOCTOU | out-dir/final/temp 在检查和 replace 间被交换 | held no-follow dir fd、`fstat` identity、`O_EXCL|O_NOFOLLOW`、replace 前再次核对；hook 精确交换测试。 |
| 原子替换误承诺 | 双文件 crash window 导致版本暂时不一致 | 两文件独立 staging/replace，Markdown-first/PPTX-last，明确不保证 pair transaction；PPTX 单文件原子且旧文件在不可恢复失败保留。 |
| 诊断泄漏 | traceback、绝对内部路径、Markdown/媒体/XML 内容流出 | 200 条/长度上限，稳定 code/location/fix/affected slide，禁止 source/XML dump；aggregate 搜索。 |
| 破坏性清理 | glob/rmtree 删除 caller-owned 文件或链接目标 | 只追踪本次创建 temp 名与 fd，普通文件检查后 unlink；sentinel/collision/交换测试。 |
| 依赖供应链 | render 自动 pip install 未审计包 | 绝不自动安装；只做版本检测和 bounded error，Phase 44 再做 aggregate readiness。 |
</threat_model>

<verification>
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py phase-43`
- aggregate 内必须调用 `skills/school-pptx/scripts/school-pptx.sh render`、Phase 41 template-report、Phase 42 fixture-example。
- 检查成功目录只有命令自有 `<stem>.md`/`<stem>.pptx`，故障后无 `.tmp-*`/`.bak`/JSON/log/manifest/debug。
</verification>

<success_criteria>
- 用户可重复把 finalized Markdown 渲染为非空、可编辑、模板一致 PPTX，并收到同 stem Markdown。
- invalid/best-effort 始终非零且明确异常；不可恢复失败保留旧 PPTX。
- D-01..D-21、PPTX-01..13、VER-03、SKILL-03 全覆盖且结构自动门通过。
- Phase 44 的公共 aggregate verify、runtime 文档、dependency readiness 与手工 PowerPoint/WPS UAT 保持未实现。
</success_criteria>

<source_audit>
| Source | Coverage |
|---|---|
| ROADMAP Phase 43 goal/criteria | Tasks 1-3 提供 render、editable PPTX、分页摘要、干净双产物与非零 invalid |
| PPTX-01..12 | Plan 02/03 + Task 3 aggregate；本计划 requirements 复验用户入口与 native output |
| PPTX-13 | Task 1 依据 D-20/D-21 修订公开契约，Task 2/3 断言固定双产物与无 evidence |
| VER-03 | Task 1/2 public render + positive/negative exit status |
| SKILL-03 | Task 3 skill-local import/call graph audit |
| CONTEXT D-17..D-21 | Task 1 文档契约，Task 2 状态机/发布，Task 3 故障与文案 gate |
| RESEARCH blockers | 双文件非事务与 PPTX-last 顺序、bounded dependency、descriptor publication、semantic determinism 全覆盖 |
| UI-SPEC | Render States、Failure/Publication、Verification Contract 自动结构部分全覆盖；visual handoff 延期 Phase 44 |
</source_audit>
