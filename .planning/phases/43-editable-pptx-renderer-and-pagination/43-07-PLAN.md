---
phase: 43-editable-pptx-renderer-and-pagination
plan: 07
type: execute
wave: 7
depends_on:
  - 43-06
files_modified:
  - skills/school-pptx/templates/standard-school.manifest.yaml
  - skills/school-pptx/references/template-contract.md
  - skills/school-pptx/scripts/template_report.py
  - skills/school-pptx/scripts/pptx_model.py
  - skills/school-pptx/scripts/pptx_paginate.py
  - skills/school-pptx/scripts/pptx_emit.py
  - skills/school-pptx/scripts/pptx_objects.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-03
  - PPTX-04
  - PPTX-08
must_haves:
  truths:
    - "W-02/W-04/W-05: target slot、目录可见编号和每个表格行的 EMU 高度必须是 frozen plan 字段；emitter 只能消费，不得重新选择栏位、编号或平均行高。"
    - "W-03: cover title、subtitle 与 canonical metadata descriptor 均由 document metadata 冻结后写入 manifest-owned title/subtitle slots，不再只发射 title。"
    - "PhysicalDeckPlan 的 `to_projection()` 必须包含新增字段，重复 planning equality 和 semantic determinism 保持稳定。"
  artifacts:
    - path: "skills/school-pptx/templates/standard-school.manifest.yaml"
      provides: "cover.subtitle 的 72 字符、2 行 manifest-owned canonical 预算。"
    - path: "skills/school-pptx/references/template-contract.md"
      provides: "封面 subtitle descriptor 的字段顺序、排除项与 overflow 契约。"
    - path: "skills/school-pptx/scripts/template_report.py"
      provides: "受控 cover.subtitle 预算的 template-report 验证与证据输出。"
    - path: "skills/school-pptx/scripts/pptx_model.py"
      provides: "target_slot、row_heights_emu、slot_values 等 immutable emitter contract。"
    - path: "skills/school-pptx/scripts/pptx_paginate.py"
      provides: "双栏归属、目录显示编号、表格逐行高度、封面槽值的唯一决策点。"
    - path: "skills/school-pptx/scripts/pptx_emit.py"
      provides: "不重做布局决策的 mechanical slot/row emission。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "W-02..W-05 专项 frozen-plan/reopen 回归。"
  key_links:
    - from: "skills/school-pptx/templates/standard-school.manifest.yaml"
      to: "skills/school-pptx/scripts/template_report.py"
      via: "template-report 验证 cover.subtitle 的 72 字符、2 行受控预算并输出证据"
      pattern: "cover|subtitle|max_chars|max_lines"
    - from: "skills/school-pptx/scripts/pptx_paginate.py"
      to: "skills/school-pptx/scripts/pptx_model.py"
      via: "构造 target_slot、row_heights_emu 与 slot_values frozen fields"
      pattern: "target_slot|row_heights_emu|slot_values"
    - from: "skills/school-pptx/scripts/pptx_emit.py"
      to: "skills/school-pptx/scripts/pptx_model.py"
      via: "只读取 frozen fields 选择 manifest slot 并设置 native row heights"
      pattern: "fragment.target_slot|fragment.row_heights_emu|physical.slot_values"
---

<objective>
修复内容完整性和 frozen plan 到 emitter 的机械边界。

Purpose: 让双栏、目录、表格和封面在成品中忠实落实分页器已决定的内容归属与模板预算。
Output: 扩展的 frozen models、唯一 pagination decisions、机械 emitter、W-02/W-03/W-04/W-05 专项门。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-02-SUMMARY.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 frozen model 并冻结双栏与封面槽位（W-02/W-03）</name>
  <files>skills/school-pptx/templates/standard-school.manifest.yaml, skills/school-pptx/references/template-contract.md, skills/school-pptx/scripts/template_report.py, skills/school-pptx/scripts/pptx_model.py, skills/school-pptx/scripts/pptx_paginate.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/references/markdown-contract.md
    - skills/school-pptx/scripts/template_report.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/references/template-contract.md
    - skills/school-pptx/fixtures/school-pptx-full.md
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
  </read_first>
  <action>先统一 manifest-owned 封面契约：把 `standard-school.manifest.yaml` 的 `cover.subtitle.text_budget.max_chars` 从 48 调整为 72，保留 `max_lines: 2`、现有 geometry、18..28 pt shrink 范围和 binary template 不变；在 `template-contract.md` 明写 subtitle 值固定为两行，第一行为 YAML `subtitle`，第二行只按非空值顺序拼接 `school`、`department`、`course`、`author`、`date`，字段间使用 ` · `，`program`/`presenter` 不进入此槽。canonical fixture 第一行 14 字、第二行 51 字，连同换行总长 66，必须在 72 字符/2 行预算内成功。让 `template_report.py` 验证并报告受控 `cover.subtitle` 预算恰为 72 字符/2 行，既有 `manifest_renderer_contract_gate` 断言报告、manifest 与文档契约一致。随后给 frozen `BlockFragment` 增加 `target_slot: str | None`，给 `PhysicalSlide` 增加 `slot_values: tuple[tuple[str, str], ...]`，并在 `to_projection()` 明确序列化。实现 `_two_column_fragments()` 时严格消费 `markdown-contract.md` D-06 已冻结的 logical `blocks` 源码顺序，不按 `###` heading 重新分区、不按高度二分：第 1/2 块分别进入 `left_body`/`right_body`，第 3/4 块重复该配对，依此类推；奇数末块进入 `left_body`，同一 pair 的 `right_body` 为空 payload。每个 pair 的左右侧分别按对应 manifest slot budget 分页，再按 page index 配对成物理页，所有 fragment 显式携带 `target_slot`；不得在 emitter 推断左右栏。cover 的 `slot_values` 固定为 `title=document_title` 和上述完整两行 `subtitle`。若任一独立非 canonical fixture 的完整组合超过 72 字符或 2 行，产生 `COVER_METADATA_OVERFLOW` error 且 render 非零，不做字符截断、字段丢弃或成功降级；best-effort 可仅保留 authored subtitle，但不得伪装为完整 descriptor。所有新增 tuple 保持 frozen/hash/equality 语义。在 `verify_pptx_renderer.py` 同任务内实现并注册 `frozen-slot-content`，使本任务 automated 首次调用时 gate 已存在，覆盖 canonical 5-block 双栏 slot 序列 `left_body,right_body,left_body,right_body,left_body`、block inventory、cover descriptor、overflow 负例与 projection determinism。</action>
  <verify>
    <automated>python3 -m py_compile skills/school-pptx/scripts/template_report.py skills/school-pptx/scripts/pptx_model.py skills/school-pptx/scripts/pptx_paginate.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py manifest_renderer_contract_gate &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py frozen-slot-content</automated>
  </verify>
  <acceptance_criteria>
    - W-02 plan assertion: canonical 5-block two-column plan 的 target slot 序列精确为 `left_body,right_body,left_body,right_body,left_body`，每个源 block 恰出现一次且顺序不变，奇数末块只进入左栏。
    - W-02 budget assertion: 左右栏分别使用各自 manifest geometry/text budget 形成物理页；projection 中目标栏位显式存在。
    - W-03 budget assertion: manifest、template contract 与 template-report evidence 对 `cover.subtitle` 均为 `max_chars=72`、`max_lines=2`；canonical descriptor 总长精确为 66，且 `program`/`presenter` 不进入该槽。
    - W-03 plan assertion: cover `slot_values` 含非空 title/subtitle，canonical fixture 的 school、department、course、author、date 均可在 frozen subtitle descriptor 中定位，planning 不产生 `COVER_METADATA_OVERFLOW`。
    - Overflow assertion: 独立非 canonical 超长 metadata fixture 返回非零和 `COVER_METADATA_OVERFLOW`，不字符截断、不丢弃完整字段后伪装成功；canonical fixture 不用于该负例。
    - Determinism assertion: 同输入连续两次 `to_projection()` 完全相等，dataclass mutation 继续抛出 `FrozenInstanceError`。
  </acceptance_criteria>
  <done>W-02/W-03 的内容归属在 pagination 阶段冻结，不再留给 emitter 猜测。</done>
</task>

<task type="auto">
  <name>Task 2: 冻结目录显示编号与表格逐行 EMU 高度（W-04/W-05）</name>
  <files>skills/school-pptx/scripts/pptx_model.py, skills/school-pptx/scripts/pptx_paginate.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/fixtures/school-pptx-full.md
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
  </read_first>
  <action>给 `BlockFragment` 增加 `row_heights_emu: tuple[int, ...]` 并加入 projection。`_contents_fragments()` 在全局分区完成后把每页 `items` 冻结为可见字符串 `f"{absolute_index}. {entry}"`，absolute index 从 1 连续递增，同时保留 `number_start/number_end` metadata 供核验；emitter 不再根据 metadata 生成编号。`_table_fragments()` 从 manifest 派生唯一预算 `content_budget_emu = table.geometry.height - table_name.geometry.height`，并先断言 table 与 table_name 的 x/y/width 关系符合既有契约且派生高度为正；不得引用 manifest 中不存在的 `content_budget` 字段。保留所选 font 对 header 和每条 data row 的 `_wrapped_cell` 最大高度，每行按 `ceil(measured_points * 12700)` 转为正整数 EMU；每页 fragment 的 `row_heights_emu` 必须与 `(header, *rows[start:end])` 一一对应，重复 header 使用相同 header height。若 ceil 高度合计超过 `content_budget_emu`，分页器必须重分页，或选择 manifest 允许的候选字号后重新测量并再次逐行 ceil；仍无法容纳时产生 `TABLE_ROW_OVERFLOW` 并使 render 非零。若合计严格小于预算，使用 `divmod(content_budget_emu - measured_total_emu, row_count)`，按稳定行序分配 quotient 和前 remainder 行各 1 EMU；若合计恰等于预算则不分配。最终每行高度不得低于其 ceil 测量高度，且 frozen `row_heights_emu` 总和不得超过 `content_budget_emu`。不得把高度塞入自由字符串 metadata 或让 emitter 重新平均分配。在 `verify_pptx_renderer.py` 同任务内实现并注册 `frozen-numbering-row-heights`，覆盖可见编号、预算派生、row height/projection/header、逐行 ceil 下界、等于预算不分配及正 slack 稳定分配断言，确保 automated 首次调用时 gate 已存在。</action>
  <verify>
    <automated>python3 -m py_compile skills/school-pptx/scripts/pptx_model.py skills/school-pptx/scripts/pptx_paginate.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py frozen-numbering-row-heights &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py pagination</automated>
  </verify>
  <acceptance_criteria>
    - W-04 plan assertion: canonical contents plan 的可见 items 从 `1. ` 连续到最后一项，跨页没有重置/重复/跳号，页标题仍严格为 `目录`。
    - W-05 model assertion: `content_budget_emu` 精确等于 `table.geometry.height - table_name.geometry.height` 且为正；每个 table fragment 满足 `len(row_heights_emu) == len(rows)`、所有高度为正整数且不低于 `ceil(measured_points * 12700)`，frozen 高度总和不超过派生预算；超预算输入经重分页或允许字号重新测量后仍不可容纳时返回非零 `TABLE_ROW_OVERFLOW`。
    - Header assertion: 所有续页第一行文本等于原 header，第一项 row height 等于首张表格 header height。
    - Frozen boundary assertion: row height 和可见编号都出现在 `to_projection()`；emitter 无需测量文本或读取 `number_start` 决定可见内容。
  </acceptance_criteria>
  <done>W-04/W-05 的最终可见编号和表格几何成为 immutable plan contract。</done>
</task>

<task type="auto">
  <name>Task 3: 机械发射 frozen slots/heights 并做成品回读（W-02..W-05）</name>
  <files>skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
  </read_first>
  <action>把 `emit_deck()` 的通用 body 逻辑改为按 `fragment.target_slot` 分组，并仅从 manifest 取该 slot 的 geometry/text budget；禁止 `two-column -> left_body` 硬编码。cover 遍历 `physical.slot_values` 写入 `title`/`subtitle` manifest slots，shape names 固定为 `school-pptx:cover-title`、`school-pptx:cover-subtitle`。contents 直接发射 fragment.items 中已编号文本。修改 `add_table(..., row_heights_emu=fragment.row_heights_emu)`，逐行设置 frozen 高度并拒绝长度不匹配，删除 `table_height / len(rows)` 平均高度逻辑；对象 helper 不调用测量器。在 `verify_pptx_renderer.py` 同任务内实现并注册 `frozen-plan-emission`；专项 reopen 门检查双栏两 shape 均存在且文本集合等于 plan、cover subtitle 含 canonical metadata、目录可见 `1.` 到 N 连续、每个 native table row.height 精确等于 plan EMU。加入源码审计，`pptx_emit.py` 不 import `pptx_paginate`/`pptx_measure`，不包含左右栏选择、目录编号计算或平均行高公式。</action>
  <verify>
    <automated>python3 -m py_compile skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py frozen-plan-emission &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py emit-structure</automated>
  </verify>
  <acceptance_criteria>
    - W-02 reopen assertion: 每张 canonical two-column 物理页同时存在 renderer-owned left/right native text shape，源内容无丢失、重复或跨栏漂移。
    - W-03 reopen assertion: cover title 与 subtitle shape 均非空，subtitle 可回读 canonical subtitle 及 metadata descriptor。
    - W-04 reopen assertion: contents 可见文本按计划从 1 连续编号，所有页 title 为 `目录` 且没有 `续`。
    - W-05 reopen assertion: 每张 native table 的各 row.height 与对应 frozen `row_heights_emu` 逐项相等，不再全部平均相等（除非测量向量本身相等）。
    - PPTX-03/04/08 assertion: 上述对象均 native/editable，物理页数量和每页 plan mapping 一致，emitter source audit 证明未重新分页/测量/分栏。
  </acceptance_criteria>
  <done>W-02..W-05 由 frozen projection 与成品 native object 的逐项 equality 关闭。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| emitter 重做布局 | 分栏、编号或行高与审阅计划漂移 | 所有决定进入 frozen fields；source audit 禁 paginator/measure 和重算公式。 |
| 内容丢失/重复 | 双栏分区或多页配对漏 block | block identity/order inventory，要求每个源 block 恰出现一次。 |
| 表格溢出 | EMU 取整或平均分配低估测量高度 | 冻结正整数测量高度；总高预算断言；不可行时 bounded `TABLE_ROW_OVERFLOW`。 |
| metadata 注入 | 超长封面元数据越过 subtitle budget | 72 字符/2 行 manifest 契约；完整五字段组合超限即非零 `COVER_METADATA_OVERFLOW`，不字符截断、字段丢弃或新增 geometry。 |
| 模型兼容回归 | 新字段未进入 projection 导致 determinism gate 漏检 | projection schema/equality/FrozenInstanceError 专项断言。 |
</threat_model>

<verification>
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py frozen-slot-content`
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py frozen-numbering-row-heights`
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py frozen-plan-emission`
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py emit-structure`
</verification>

<success_criteria>
- W-02/W-03/W-04/W-05 均有 plan projection 与 reopen PPTX 的机械 equality 证据。
- canonical 封面 descriptor 以 66 字符完整落入 72 字符/2 行预算并成功；独立超长 fixture 才触发 `COVER_METADATA_OVERFLOW` 非零负例。
- PPTX-03、PPTX-04、PPTX-08 的 blocked 原因被 native objects、预算字段和 emitter source audit 明确关闭。
- emitter 仍只消费 PhysicalDeckPlan，不导入分页/测量模块或作布局选择。
</success_criteria>
