---
phase: 43-editable-pptx-renderer-and-pagination
plan: 06
type: execute
wave: 6
depends_on:
  - 43-05
files_modified:
  - skills/school-pptx/scripts/pptx_objects.py
  - skills/school-pptx/scripts/pptx_emit.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-03
  - PPTX-10
must_haves:
  truths:
    - "C-02/D-03: code fragment 的 authored text（含 ==、**、连续星号、空格和换行）逐字符进入单一可编辑 monospace text frame，任何代码路径不得调用 inline_spans 或 rich-text delimiter normalization。"
    - "代码软换行只影响分页测量，不向成品可回读文本插入额外换行；emitter 不重新解析 Markdown 或改变 PhysicalDeckPlan。"
  artifacts:
    - path: "skills/school-pptx/scripts/pptx_objects.py"
      provides: "literal code text helper，与 rich text helper 完全分离。"
    - path: "skills/school-pptx/scripts/pptx_emit.py"
      provides: "code fragment 到 literal monospace helper 的机械 wiring。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "C-02 逐字符 reopen 专项回归。"
  key_links:
    - from: "skills/school-pptx/scripts/pptx_emit.py"
      to: "skills/school-pptx/scripts/pptx_objects.py"
      via: "code layout 只调用 add_literal_text；普通正文继续调用 add_rich_text/add_plain_lines"
      pattern: "add_literal_text|layout == .code."
---

<objective>
关闭代码页 Markdown 富文本归一化导致的静默文本变异。

Purpose: 使 PPTX-03/PPTX-10 对代码内容和编辑性的要求从“有 monospace shape”提升为逐字符可往返。
Output: literal code emitter、机械 emitter wiring、C-02 修复前失败修复后通过的 reopen 回归。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-02-SUMMARY.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增不解释 Markdown delimiter 的 literal code helper（C-02）</name>
  <files>skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/pptx_ooxml.py
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
  </read_first>
  <action>在 `pptx_objects.py` 增加 `add_literal_text(slide, geometry, text, *, font_size, name, font_name="Consolas")`。该函数创建一个 textbox，调用 `_configure_text_frame()`，只创建一个 paragraph/run 并把 `run.text = text` 原样赋值，设置显式字号和 monospace 字体；不得调用 `normalize_rich_text()`、`inline_spans()`、`add_rich_text()` 或 `set_run_highlight()`，不得 split/join 行、strip 尾随空格或规范化换行。保留现有 rich text 路径给普通标题/正文的 bold/highlight。在 `verify_pptx_renderer.py` 同任务内实现并注册首次可调用的 `code-literal-roundtrip` helper-level gate：直接用 literal helper 写入含 `==`、`**`、连续星号、空格和换行的 native textbox，reopen 后逐字符比较，并以 AST/调用图源审计证明 helper 不触达 delimiter parser；Task 2 再扩展同一 gate 到 parser-plan-emitter 全链路。</action>
  <verify>
    <automated>python3 -m py_compile skills/school-pptx/scripts/pptx_objects.py &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - C-02 helper assertion: 同任务注册且可调用的 `code-literal-roundtrip` 证明 `add_literal_text` 的函数体不引用 `normalize_rich_text`、`inline_spans`、`add_rich_text` 或 highlight helper。
    - Literal vector assertion: `if a == b == c\nreturn **value**\nx = ***raw***\n  keep  spaces  ` 写入后通过 python-pptx reopen 回读与输入逐字符相等。
    - Editability assertion: 向量位于一个 `school-pptx:code` native text shape，所有 run 使用 monospace 字体且不是图片/整页截图。
  </acceptance_criteria>
  <done>代码对象拥有与富文本完全分离的 literal 写入原语。</done>
</task>

<task type="auto">
  <name>Task 2: 将 frozen code fragment 机械发射并升级逐字符回归（C-02）</name>
  <files>skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/fixtures/school-pptx-full.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
  </read_first>
  <action>在 `pptx_emit.emit_deck()` 中为 `physical.layout == "code"` 建立专用分支：从 frozen code fragments 按计划顺序取 `heading` 与 `text`，代码正文不经过通用 `lines` + `add_plain_lines()`；如果需显示 `###` heading，使用独立普通 rich-text shape/manifest 子区，代码 shape 只接收 `fragment.text` 原值。调用 `add_literal_text(..., font_size=selected/manifest code budget, name="school-pptx:code")`，保持 `word_wrap=True`、`auto_size=NONE`。扩展 Task 1 已注册的 `code-literal-roundtrip`：构造包含 `==`、`**`、`***`、空行、行首/行尾空格、超宽单行的 Markdown，比较 parser block text、PhysicalDeckPlan fragment text 和 reopen PPTX code shape text 三者完全相等；另断言超宽行没有被 emitter 插入 newline，普通正文的 bold/highlight 回归仍通过。</action>
  <verify>
    <automated>python3 -m py_compile skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip &amp;&amp; python3 skills/school-pptx/scripts/verify_pptx_renderer.py editable-objects</automated>
  </verify>
  <acceptance_criteria>
    - C-02 wiring assertion: code branch 不调用 `add_plain_lines`/`add_rich_text` 处理代码正文，且 `pptx_emit.py` 仍不 import/call paginator。
    - PPTX-10 round-trip assertion: parser text == frozen fragment text == reopen `school-pptx:code` text，包括所有测试 delimiter、原始换行和空格。
    - Soft-wrap assertion: 超宽源码行在 reopen 文本中仍为一行，显示换行只由 text frame 承担。
    - PPTX-03 regression: 普通正文 `**bold**`/`==highlight==` 仍生成 native styled runs，代码中的相同字符不生成 bold/highlight run。
  </acceptance_criteria>
  <done>C-02 及其阻塞的 PPTX-03/PPTX-10 由逐字符 parser-plan-PPTX 证据关闭。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| 静默代码变异 | delimiter 被删除或被解释成样式，教学代码语义改变 | literal helper 独立调用图；parser-plan-reopen 三方 equality。 |
| 控制字符/超长行 | 文本写入异常或资源耗尽 | 复用 Phase 42 输入/行长上限与 paginator bounded diagnostic；不新增解析器。 |
| emitter 重新布局 | 发射阶段 split/join 或插入换行 | 只消费 frozen fragment.text；源审计禁止 paginator import；超宽行回归。 |
| 修复破坏普通 rich text | 正文 bold/highlight 丢失 | editable-objects 回归同时断言普通 run 样式和代码 literal。 |
</threat_model>

<verification>
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip`
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py editable-objects`
</verification>

<success_criteria>
- C-02 测试向量在 parser、frozen plan、reopen PPTX 间逐字符一致。
- PPTX-03/PPTX-10 的代码对象保持 native、editable、monospace，且不误加 rich-text 样式。
- 超宽行和普通正文 rich-text 行为无回归。
</success_criteria>
