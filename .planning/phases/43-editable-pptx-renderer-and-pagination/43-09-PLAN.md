---
phase: 43-editable-pptx-renderer-and-pagination
plan: 09
type: execute
wave: 9
depends_on:
  - 43-08
files_modified:
  - skills/school-pptx/scripts/pptx_emit.py
  - skills/school-pptx/scripts/pptx_objects.py
  - skills/school-pptx/scripts/pptx_paginate.py
  - skills/school-pptx/scripts/pptx_render.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-03
  - PPTX-04
  - PPTX-10
  - VER-03
must_haves:
  truths:
    - "R43-C01：非 code 布局在同一 body slot 混排普通 rich text 与 fenced code 时，共用一个不重叠的可编辑 text frame；code fragment 由独立 monospace run 逐字符写入且不经过 delimiter normalization，普通 bold/highlight runs 保持不变。"
    - "R43-C02：合法仅表头 table 的 public render 必须退出 0，并生成单页、单行、可编辑 native PPTX table；冻结 fragment 的 rows 与 row_heights_emu 都恰好含一项，空 table-name placeholder 可回读。"
    - "R43-W01：Pillow DecompressionBombError/Warning 精确映射为 PPTX_MEDIA_PIXEL_LIMIT，识别与 verify/decode 失败精确映射为 PPTX_MEDIA_FORMAT_INVALID；公开失败诊断 bounded 且无 traceback、Pillow 原文或绝对路径。"
    - "Phase 43 aggregate 将三条 gap 的定向回归注册为 blocking gates，并在 gap_coverage 与 blocked requirement coverage 中精确记录实际调用。"
    - "本计划不实现 Phase 44 的 verify、runtime adapter、README/兼容矩阵或人工 viewer UAT。"
  artifacts:
    - path: "skills/school-pptx/scripts/pptx_emit.py"
      provides: "按 fragment.kind 路由 code literal 与普通 rich text 的机械发射。"
    - path: "skills/school-pptx/scripts/pptx_paginate.py"
      provides: "header-only table 的确定性分页和行高计划。"
    - path: "skills/school-pptx/scripts/pptx_objects.py"
      provides: "Pillow 异常到 PptxObjectError 的 bounded 媒体边界。"
    - path: "skills/school-pptx/scripts/pptx_render.py"
      provides: "render 公共失败路径的稳定错误输出。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "C-01/C-02/W-01 定向黑盒门和 Phase 43 aggregate registry。"
  key_links:
    - from: "skills/school-pptx/scripts/pptx_emit.py"
      to: "skills/school-pptx/scripts/pptx_objects.py"
      via: "专用 code layout 继续调用 add_literal_text；非 code body slot 调用按 fragment 生成 paragraph/run 的单 text-frame helper，其中 code run 直接赋 fragment.text，普通 run 才执行 rich-text normalization"
      pattern: "fragment.kind.*code|add_fragment_text_frame|add_literal_text"
    - from: "skills/school-pptx/scripts/pptx_paginate.py"
      to: "skills/school-pptx/scripts/pptx_objects.py"
      via: "header-only table 仍携带 frozen row_heights_emu 并进入 native add_table"
      pattern: "data_heights|row_heights_emu|add_table"
    - from: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      to: "skills/school-pptx/scripts/school-pptx.sh"
      via: "定向 vectors 与 aggregate 均黑盒调用公开 render"
      pattern: "run_public_render|GAP_COVERAGE|PHASE_43_GATE_ORDER"

---

<objective>
关闭 43-VERIFICATION.md 中唯一剩余的 R43-C01、R43-C02、R43-W01，并让公共 render 与 Phase 43 aggregate 对这些合法/可达路径给出可机器核验的结果。

Purpose: 恢复 fenced code 的逐字符可编辑性，定义零数据行 table 的稳定语义，并封装 Pillow 安全异常，避免 VER-03 被未捕获异常绕过。
Output: 三条定向回归、更新后的 gap/requirement registry，以及通过完整 aggregate 的证据。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-08-SUMMARY.md
@skills/school-pptx/scripts/pptx_emit.py
@skills/school-pptx/scripts/pptx_objects.py
@skills/school-pptx/scripts/pptx_paginate.py
@skills/school-pptx/scripts/pptx_render.py
@skills/school-pptx/scripts/verify_pptx_renderer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 按 fragment.kind 保真发射所有布局中的 fenced code</name>
  <files>skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-06-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
  </read_first>
  <action>保留现有专用 `layout == "code"` / `add_literal_text` 分支。在 `pptx_objects.py` 新增单 text-frame mixed-fragment helper（固定命名 `add_fragment_text_frame`）：它只创建一个覆盖既有 body slot geometry 的 textbox，并按 frozen fragment 顺序创建 paragraph/run；heading、paragraph、list item 的 run 继续复用 `normalize_rich_text` 所得 bold/highlight spans，`fragment.kind == "code"` 则在独立 paragraph 中只创建一个 run，直接令 `run.text = fragment.text`，设置 `Consolas` 与 body slot 的 frozen/manifest 字号，不得对 code 调用 `normalize_rich_text`、`inline_spans`、`add_rich_text` 或 `add_plain_lines`，也不得 strip/split/join 或插入换行。`pptx_emit.py` 的普通 body/title-content/two-column 分支按 target slot 分组后，每槽仅调用一次该 helper；不建立共享 geometry 的额外 code shape，不修改 `PhysicalDeckPlan`、`BlockFragment`、manifest geometry 或 paginator。扩展 `code-literal-roundtrip`：同一合法 `title-content` body 同时包含普通 `**bold**`/`==highlight==` 与 fenced code，并另以 `two-column` 覆盖 code；黑盒 public render、重开 PPTX 后按 body shape 与 code paragraph/run 核对 parser text == frozen fragment text == code run text，断言单槽 shape 数为 1、paragraph 顺序与 fragment 顺序一致、普通 bold/highlight 仅出现在普通 runs、code run 为 monospace 且无 bold/highlight，超宽源码行未被写入额外 newline。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - C-01 source assertion：非 code layout 每个 target slot 只创建一个 body text frame；helper 的 code 分支直接赋 `fragment.text` 且不触达 delimiter normalization/rich-text line join，emitter 不导入 paginator。
    - PPTX-03/PPTX-10 round-trip assertion：同一 `title-content` body 的普通 rich text 与 fenced code，以及 `two-column` 的 fenced code，经 public render/reopen 后 code run 逐字符等于 parser/frozen plan，单槽仅一个 native editable textbox 且无 shape 重叠代理。
    - Style/soft-wrap assertion：code run 为 monospace、无 bold/highlight，普通 run 的 bold/highlight 保持 native 样式；超宽源码行在 code run 中仍保持单行。
    - Public assertion：成功向量退出 0、目录仅含同 stem `.md/.pptx`；成功摘要可回显用户传入的 input/out 路径，但不得泄漏仓库、uv cache 或 renderer 内部临时路径，且无 traceback/sidecar。
  </acceptance_criteria>
  <done>R43-C01 已由非 code 布局的 parser-plan-emitter-reopen 向量关闭。</done>
</task>

<task type="auto">
  <name>Task 2: 处理 header-only table 并封装 Pillow 媒体异常</name>
  <files>skills/school-pptx/scripts/pptx_paginate.py, skills/school-pptx/scripts/pptx_objects.py, skills/school-pptx/scripts/pptx_emit.py, skills/school-pptx/scripts/pptx_render.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/scripts/pptx_objects.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/pptx_render.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-02-SUMMARY.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-05-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
  </read_first>
  <action>在 `_table_fragments` 中唯一实现 header-only 成功语义：当 `data_heights == []` 时不调用空分区，也不计算空 `loads` 的均值；直接令 `ranges=((0, 0),)`，保留测得的 header height，经 `_allocate_row_heights((header_height,), content_budget_emu, fixed_prefix=1)` 得到恰好一项的正整数 `row_heights_emu`，生成恰好一个 `rows=(headers,)` 的 `BlockFragment`/物理页。不得产生 `TABLE_HEADER_ONLY` diagnostic。emitter 必须继续通过 `add_table` 发射恰好一行的 editable native `<a:tbl>`，并保留内容为空、可重开的 `school-pptx:table-name` placeholder。为 `_safe_image_size` 使用 `warnings.catch_warnings()` 将 `PIL.Image.DecompressionBombWarning` 提升为异常，并包住 `Image.open`、`image.size`、`image.verify`/必要 decode：`DecompressionBombError` 与 `DecompressionBombWarning` 只映射 `PPTX_MEDIA_PIXEL_LIMIT`；`UnidentifiedImageError` 以及 open/verify/decode 的 `OSError`、`SyntaxError`、`ValueError` 只映射 `PPTX_MEDIA_FORMAT_INVALID`；已有 `PptxObjectError` 原样重抛。`pptx_emit` allowlist 保留这两个 stable code，`pptx_render` 只打印 code 与通用修复建议。扩展 `table-header-only` gate，黑盒断言 public render 退出 0、单物理页、frozen `rows`/`row_heights_emu` 长度均为 1、重开 native `<a:tbl>` 的 `rows==1` 且回读 row height 等于 frozen 值、空 table-name placeholder 存在且文本为空。扩展 `object-error-bounded` 为四类独立向量：bomb error、bomb warning 均精确返回 `PPTX_MEDIA_PIXEL_LIMIT`；unidentified 与 verify/decode failure 均精确返回 `PPTX_MEDIA_FORMAT_INVALID`；每个失败向量退出非零、旧 PPTX sentinel byte-identical、不发布 Markdown，输出低于 8KiB 且不含 Pillow 原文、traceback、输入/workdir/repo/cache/内部临时绝对路径。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/pptx_paginate.py skills/school-pptx/scripts/pptx_objects.py skills/school-pptx/scripts/pptx_emit.py skills/school-pptx/scripts/pptx_render.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py table-header-only &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py object-error-bounded</automated>
  </verify>
  <acceptance_criteria>
    - C-02 pagination assertion：合法仅表头 table 的 public render 退出 0 且仅生成一个物理页；frozen fragment 的 `rows==1`、`row_heights_emu==1`，不产生 diagnostic 或空 ranges/loads 除零。
    - PPTX-04 assertion：header-only 成品含一行 editable `<a:tbl>`（不是 text-box 矩阵），其 row height 等于 frozen 值；空 table-name placeholder 可回读、文本为空且无 prompt text。
    - W-01 exact-code assertion：DecompressionBombError/Warning 分别精确返回 `PPTX_MEDIA_PIXEL_LIMIT`，UnidentifiedImageError/verify-decode failure 分别精确返回 `PPTX_MEDIA_FORMAT_INVALID`。
    - VER-03 failure assertion：四个 Pillow 失败向量均非零、旧目标 byte-identical、不发布 Markdown，诊断低于 8KiB 且无 traceback、Pillow 原文、输入/workdir/repo/cache/internal-temp 绝对路径。
  </acceptance_criteria>
  <done>R43-C02 与 R43-W01 的合法/恶意媒体公开路径均 bounded 且有确定性结果。</done>
</task>

<task type="auto">
  <name>Task 3: 将三条新回归设为 Phase 43 aggregate blocking gates</name>
  <files>skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-08-PLAN.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-08-SUMMARY.md
    - .planning/REQUIREMENTS.md
    - skills/school-pptx/scripts/school-pptx.sh
  </read_first>
  <action>把 Task 1/2 的定向 gates 以固定名称注册到 `GATES`，并更新 `PHASE_43_GATE_ORDER`、required set、argparse choices 与 called-order exact assertions；不得动态 skip、重复或用历史同名标签掩盖新向量。更新 `GAP_COVERAGE` 为 `R43-C01`、`R43-C02`、`R43-W01`（同时保留既有历史 C-01/C-02/W-01..W-06 的明确映射，必要时用 review_id 与 verification id 分开），并使 evidence 记录每条 gap 实际调用的 gate。更新 `REQUIREMENT_COVERAGE`，确保 PPTX-03、PPTX-04、PPTX-10、VER-03 各有直接定向 gate；aggregate 在隔离 TemporaryDirectory 中黑盒执行 public shell，验证 canonical render、Phase 41/42 regression、双产物边界仍通过。增加输出审计：所有三条 gap 的失败向量均非零/成功向量按契约退出，输出 bounded、无 traceback 与绝对路径；不得修改 REQUIREMENTS/ROADMAP/STATE，且 scope assertion 继续证明 Phase 44 public verify/runtime/UAT 未提前加入。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py phase-43</automated>
  </verify>
  <acceptance_criteria>
    - Registry assertion：`PHASE_43_GATE_ORDER`、required set、called tuple 完全相等且唯一；三条新定向 gate 均实际执行，删除/skip/reorder 任一项均失败。
    - Gap coverage assertion：aggregate evidence 精确包含 R43-C01、R43-C02、R43-W01，每项至少映射一个实际 gate，不以旧 C-01/C-02/W-01 标签替代。
    - Requirement assertion：PPTX-03、PPTX-04、PPTX-10、VER-03 分别列出直接回归 gate，并在 aggregate 中成功完成。
    - Public boundary assertion：canonical public render 仍成功生成同 stem `.md/.pptx`；成功摘要只允许回显用户请求的 input/out artifact 路径，不得泄漏仓库、uv cache 或 renderer internal temp；所有失败诊断均无 traceback/任何绝对路径且低于 8KiB，Phase 44 边界未泄漏。
  </acceptance_criteria>
  <done>Phase 43 aggregate 对 R43-C01/C02/W01 与四个 blocked requirements 形成 blocking、可追溯的最终门。</done>
</task>

</tasks>

<threat_model>
| Threat | Risk | Mitigation and verification |
|---|---|---|
| fenced code delimiter mutation | 合法代码被静默改写，破坏 PPTX-03/PPTX-10 | fragment.kind 路由、parser-plan-reopen 三方逐字符 equality。 |
| header-only table empty partition | 合法输入触发除零并泄漏 traceback | 显式零 data rows 成功分支、单页单行 native table 与 frozen/reopen row-height 黑盒回归。 |
| Pillow decompression bomb leakage | 第三方异常绕过 allowlist，泄漏路径和内部信息 | `_safe_image_size` 捕获并映射，public CLI 输出与 sentinel 回归。 |
| aggregate false green | 新 gap 未实际执行或被历史标签掩盖 | fixed registry、exact called order、R43 gap coverage 和直接 requirement coverage。 |
</threat_model>

<verification>
- 完整可复制验证：`export PYTHONPATH="$HOME/.cache/uv/archive-v0/3UaCURZW721Q9fJj:$HOME/.cache/uv/archive-v0/V3BbfFztllgFv38f:$HOME/.cache/uv/archive-v0/g8iDxaNqOVKE-2u5:$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l"; SCHOOL_PPTX_PYTHON="${SCHOOL_PPTX_PYTHON:-python3}"; "$SCHOOL_PPTX_PYTHON" -c 'import pptx, PIL, lxml, yaml' &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py code-literal-roundtrip &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py table-header-only &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py object-error-bounded &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py phase-43`
</verification>

<success_criteria>
- R43-C01 fenced code 在非 code 布局仍逐字符可编辑、monospace、无 rich-text delimiter 变异。
- R43-C02 header-only table 由公开命令成功生成单页、单行 native table，frozen rows/row heights 与重开结果完全一致，空 table-name placeholder 可编辑。
- R43-W01 Pillow bomb error/warning 与识别/decode 失败分别转换为精确 allowlisted public code，旧目标不被替换且失败诊断无 traceback、Pillow 原文或绝对路径。
- aggregate 以固定 registry 实际执行三条 gap 定向门，并精确覆盖 PPTX-03、PPTX-04、PPTX-10、VER-03。
</success_criteria>
