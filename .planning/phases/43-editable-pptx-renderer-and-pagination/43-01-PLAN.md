---
phase: 43-editable-pptx-renderer-and-pagination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/school-pptx/templates/standard-school.manifest.yaml
  - skills/school-pptx/references/template-contract.md
  - skills/school-pptx/scripts/template_report.py
  - skills/school-pptx/scripts/pptx_model.py
  - skills/school-pptx/scripts/pptx_paginate.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
autonomous: true
requirements:
  - PPTX-03
  - PPTX-04
  - PPTX-05
  - PPTX-07
  - PPTX-09
  - PPTX-13
  - SKILL-03
must_haves:
  truths:
    - "D-14: manifest 与 template-contract 明确保留空图注和空表名编辑槽；gallery.caption.empty_slot 不再是 hide。"
    - "closing 通过 OOXML part path 映射到实际结束页使用的 ppt/slideLayouts/slideLayout7.xml，而不是 cover layout 或数字索引。"
    - "gallery 1/2/3/4 项卡片、timeline 节点子区域、table_name、run 高亮 theme scheme token 全部由 manifest 拥有，发射器无需私有几何或 RGB。"
    - "物理页计划是冻结、可比较、可序列化的中间模型；分页模块不导入 python-pptx，发射阶段不得回写分页决定。"
    - "PPTX-13 的旧措辞已被识别为待 Plan 04 按 D-20/D-21 修订的双产物契约，本计划只建立机器契约，不伪称两个文件可整体原子提交。"
  artifacts:
    - path: "skills/school-pptx/templates/standard-school.manifest.yaml"
      provides: "Phase 43 所需空槽、分项几何、高亮 token 与正确 closing part-path 机器契约。"
    - path: "skills/school-pptx/scripts/pptx_model.py"
      provides: "冻结的 fragment、物理页、整副 deck 与 renderer diagnostic 数据结构。"
    - path: "skills/school-pptx/scripts/pptx_paginate.py"
      provides: "不依赖 PPTX 对象的 CJK 感知测量与后续分页公共 primitive。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "Phase 43 任务级 gate registry，首批覆盖 manifest/model/measurement。"
  key_links:
    - from: "skills/school-pptx/scripts/template_report.py"
      to: "skills/school-pptx/templates/standard-school.manifest.yaml"
      via: "校验新增 subregion/preset/token、closing 关系与预算完整性"
      pattern: "table_name|item_presets|inline_styles|slideLayout7"
    - from: "skills/school-pptx/scripts/pptx_paginate.py"
      to: "skills/school-pptx/scripts/pptx_model.py"
      via: "只构造 frozen physical plan，不创建 PPTX shape"
      pattern: "PhysicalDeckPlan|PhysicalSlide"
---

<objective>
先消除会迫使渲染器硬编码视觉规则的模板契约缺口，再建立确定性物理页模型和保守测量底座。

Purpose: 阻断 D-14、closing 映射、分项几何与主题高亮 token 漂移，并把后续算法限制在纯计划层。
Output: 经 template-report 校验的 manifest/参考文档、冻结物理模型、CJK 测量 primitive 和首批独立 gate。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
@.planning/phases/41-standard-template-and-manifest/41-CONTEXT.md
@.planning/phases/42-markdown-contract-and-full-fixture/42-CONTEXT.md
@.planning/phases/42-markdown-contract-and-full-fixture/42-03-SUMMARY.md
@skills/school-pptx/references/template-contract.md
@skills/school-pptx/references/markdown-contract.md
@skills/school-pptx/templates/standard-school.manifest.yaml
@skills/school-pptx/templates/standard-school.pptx
@skills/school-pptx/scripts/template_report.py
@skills/school-pptx/scripts/markdown_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修订 Phase 43 模板机器契约</name>
  <files>skills/school-pptx/templates/standard-school.manifest.yaml, skills/school-pptx/references/template-contract.md, skills/school-pptx/scripts/template_report.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-CONTEXT.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/templates/standard-school.pptx
    - skills/school-pptx/references/template-contract.md
    - skills/school-pptx/scripts/template_report.py
  </read_first>
  <action>在 manifest 中把 `gallery.caption.empty_slot` 改为 `preserve`；为 table 增加具有 geometry、text_budget、`empty_slot: preserve` 的 `table_name` 子区域；为 gallery 定义 1/2/3/4 项的卡片、picture、caption 几何预设；为 timeline 定义 manifest 所有的 axis/node subdivision 与节点文字预算；新增 `inline_styles.highlight.scheme_color` 并限制为模板 theme scheme 名称；把 `closing.pptx_layout` 校正为 `ppt/slideLayouts/slideLayout7.xml`。扩展 `template_report.validate_manifest()`，逐项检查新字段类型、非负几何、父区域内包含、preset 完整且不重叠越界、text budget 上下限、受控 scheme token、closing part-path 存在，并把证据写入原有 JSON/Markdown report。同步 template-contract 说明动态创建的空文本框仍由 manifest 子区域控制，不要求修改二进制模板，也不得在代码中复制坐标。</action>
  <verify>
    <automated>report_dir="$(mktemp -d "${TMPDIR:-/tmp}/school-pptx-43-01-report.XXXXXX")" &amp;&amp; trap 'rm -rf -- "$report_dir"' EXIT HUP INT TERM &amp;&amp; skills/school-pptx/scripts/school-pptx.sh template-report --theme standard-school --out-md "$report_dir/report.md" --out-json "$report_dir/report.json"</automated>
  </verify>
  <acceptance_criteria>
    - D-14 assertion: report 证明空 caption/table_name 都是 preserve，且 table_name 拥有机器预算与区域。
    - Mapping assertion: closing evidence 的 part path 严格为 `ppt/slideLayouts/slideLayout7.xml`。
    - Geometry assertion: gallery 1/2/3/4 presets 与 timeline subdivisions 均完整、位于父槽内且无非法重叠。
    - Theme assertion: highlight 只引用 manifest theme scheme token，不新增 renderer RGB。
    - Regression assertion: 11 个受控布局和已有 placeholder/geometry tolerance 校验继续通过。
  </acceptance_criteria>
  <done>渲染所需全部视觉事实均有一个可校验的 manifest 来源，D-14 与 closing 阻断点关闭。</done>
</task>

<task type="auto">
  <name>Task 2: 建立冻结物理页模型与 CJK 测量器</name>
  <files>skills/school-pptx/scripts/pptx_model.py, skills/school-pptx/scripts/pptx_paginate.py</files>
  <read_first>
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-RESEARCH.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-PATTERNS.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
  </read_first>
  <action>创建 frozen dataclass：`RenderDiagnostic`、`BlockFragment`、`PhysicalSlide`、`PhysicalDeckPlan`，保留 logical/physical index、layout、原 `##` title、fragment index、source line、notes intent、所选字号与受影响页映射，并提供稳定 JSON-compatible 投影但不写公开文件。创建 `TextMeasure`，按显式换行、East Asian Width、标点禁则、段落/列表/代码/单元格 margin 和 manifest 字号边界计算 display lines/height/break opportunities；Pillow 字体可用时只作为确定性增强，不可用时使用固定保守 fallback。提供纯标准库 `run_contract_model_self_check()`：构造两份相同 frozen plan 并断言相等、不可变和投影稳定，执行中文无空格、显式换行、emoji/组合字符、代码缩进、单元格 margin 与字号夹取的固定向量，返回可比较的确定性 evidence mapping，供本任务独立验证及 Task 3 gate 复用。禁止导入 `pptx`、调用 `fit_text()`、依赖 CWD 字体搜索或把字符计数当唯一分页权威。</action>
  <verify>
    <automated>python3 -m py_compile skills/school-pptx/scripts/pptx_model.py skills/school-pptx/scripts/pptx_paginate.py &amp;&amp; python3 -c 'import ast, pathlib, sys; path = pathlib.Path("skills/school-pptx/scripts/pptx_paginate.py"); tree = ast.parse(path.read_text(encoding="utf-8")); bad = [node for node in ast.walk(tree) if (isinstance(node, ast.Import) and any(alias.name == "pptx" or alias.name.startswith("pptx.") for alias in node.names)) or (isinstance(node, ast.ImportFrom) and node.module is not None and (node.module == "pptx" or node.module.startswith("pptx.")))]; assert not bad, "pptx_paginate.py must not import pptx or submodules"; sys.path.insert(0, str(path.parent)); import pptx_paginate; first = pptx_paginate.run_contract_model_self_check(); second = pptx_paginate.run_contract_model_self_check(); assert first == second and isinstance(first, dict) and first, "contract/model self-check evidence must be deterministic and non-empty"'</automated>
  </verify>
  <acceptance_criteria>
    - Model assertion: 相同输入可生成相等的 frozen plan 投影，且 emitter 无法通过模型 API 改写 fragment。
    - Measurement assertion: 中文无空格文本、显式换行、emoji/组合字符、代码缩进和单元格 margin 都有稳定测试向量。
    - Budget assertion: 所有字号均夹在 manifest min/max 内，PowerPoint autofit 不参与测量或分页。
    - Boundary assertion: 模块不读取媒体、不创建 PPTX、不写文件、不访问网络。
  </acceptance_criteria>
  <done>后续分页拥有确定性、CJK 感知且与 PPTX 发射解耦的模型和测量基础。</done>
</task>

<task type="auto">
  <name>Task 3: 建立契约与模型回归门</name>
  <files>skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - skills/school-pptx/scripts/verify_markdown_contract.py
    - skills/school-pptx/scripts/template_report.py
    - skills/school-pptx/scripts/pptx_model.py
    - skills/school-pptx/scripts/pptx_paginate.py
    - skills/school-pptx/templates/standard-school.manifest.yaml
  </read_first>
  <action>创建标准库驱动的 `verify_pptx_renderer.py` gate registry，首批注册 `manifest_renderer_contract_gate`、`model_determinism_gate`、`measurement_gate`。黑盒运行 template-report，并白盒断言 D-14 空槽、gallery/timeline 几何、highlight scheme、closing part path；复用并再次执行 Task 2 的 `run_contract_model_self_check()`，覆盖冻结模型相等性、不可变投影和 CJK fallback。以 AST/import graph 检查 `pptx_paginate.py`，拒绝 `import pptx`、`from pptx ...` 以及 `pptx` 的任何子模块导入，并把该断言纳入 `contract-model` gate；不得依赖模块 `__dict__` 名称检查。所有测试只在 TemporaryDirectory 写证据，限制 ZIP entry 数/单 entry 解压大小/总解压预算，输出无 traceback；任一高危模板 ZIP/XML、越界 geometry 或资源耗尽样例必须阻断。</action>
  <verify>
    <automated>python3 skills/school-pptx/scripts/verify_pptx_renderer.py contract-model</automated>
  </verify>
  <acceptance_criteria>
    - Registry assertion: 三个命名 gate 唯一、实际执行，任何跳过或失败使命令非零。
    - Import-boundary assertion: AST/import graph gate 拒绝 `pptx` 及任意子模块的 import，且 `contract-model` 实际执行该检查。
    - Seed evidence assertion: gate 从受控 template package 证明当前恰有 5 张 seed slide，供 Plan 03 移除测试使用。
    - Security assertion: 畸形 ZIP/XML、entry bomb、外部实体、越界 preset 与超限文本向量均有有界失败，无文件内容泄漏。
    - Scope assertion: 未新增 public `verify` 命令、runtime 文档或 PowerPoint/WPS 手工 UAT。
  </acceptance_criteria>
  <done>契约和纯模型具备可重复、资源有界、可阻断后续 wave 的 Phase 43 本地门。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| 恶意 manifest/YAML | 深层结构、巨大序列、非法数值导致资源耗尽或几何逃逸 | 复用 Phase 42 YAML 限制，新增 preset 数量/类型/范围/父区包含校验；任何 High 失败阻断 gate。 |
| 恶意模板 PPTX ZIP/XML | ZIP bomb、重复 entry、路径名穿越、XXE 或畸形关系 | 只读受控 skill-local template；检查 entry 名、数量、单项与总解压预算；使用禁外部实体解析器并有负例。 |
| 模板/manifest 篡改 | closing 指向 cover、子区域越界或 theme token 注入 | part-path、layout XML、scheme allowlist 和 evidence 交叉校验，失败非零。 |
| 资源耗尽 | CJK 测量的超长文本或分区状态爆炸 | 延续 2 MiB/2048 block 上游边界；测量器线性扫描、固定诊断上限、测试超限输入。 |
| 诊断泄漏 | 输出模板 XML、绝对路径或输入全文 | 仅稳定 code/location/fix，不打印 XML/源文本/traceback；gate 搜索敏感输出。 |
| 路径/符号链接/TOCTOU | report 输出或模板路径被交换 | 本计划不实现公开发布；测试只用隔离目录，template-report 原有安全边界不得弱化，High 风险留给 Plan 04 描述符发布门。 |
</threat_model>

<verification>
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py contract-model`
- `python3 skills/school-pptx/scripts/verify_markdown_contract.py fixture-example`
- 确认 `pptx_paginate.py` 不导入 `python-pptx`，manifest 报告覆盖新增字段与 closing part path。
</verification>

<success_criteria>
- D-14、closing、gallery/timeline 分项几何和高亮 token 均成为可验证机器契约。
- 物理页计划与测量 primitive 确定、冻结、CJK 感知且不依赖 PPTX 对象。
- Phase 41/42 回归继续通过；无 emitter、CLI 发布、Phase 44 aggregate verify/runtime docs/manual UAT 偷渡。
</success_criteria>

<source_audit>
| Source | Coverage |
|---|---|
| ROADMAP Phase 43 goal | Task 1 提供模板预算，Task 2 提供物理页计划底座 |
| CONTEXT D-14 | Task 1 明确 supersede hide 并保留空槽 |
| CONTEXT D-20/D-21 | must_haves 记录修订责任，实际公开契约留给 Plan 04 |
| RESEARCH blockers | Task 1 覆盖 closing/manifest gaps；Task 3 固化 5 张 seed 事实；Task 2 禁止 fit_text |
| UI-SPEC | Task 1 覆盖 Design System/Layout Ownership，Task 2 覆盖 Typography/Pagination shared rules |
</source_audit>
