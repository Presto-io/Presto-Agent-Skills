---
phase: 44-verification-gate-runtime-notes-and-uat
plan: 04
type: execute
wave: 4
depends_on:
  - 44-03
files_modified:
  - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md
  - skills/school-pptx/scripts/verify_school_pptx.py
autonomous: false
requirements:
  - VER-10
must_haves:
  truths:
    - "D-18：自动 OOXML reopen 只是前置条件；必须由人类在 Microsoft PowerPoint 或 WPS Presentation 至少一个真实桌面 viewer 打开 hash-matched canonical PPTX。"
    - "D-19：milestone UAT evidence 持久记录 viewer/version、OS、fixture SHA-256、PPTX SHA-256、带时区 timestamp、tester、evidence source 和逐项结果；reusable skill reference 不写项目 PASS。"
    - "D-20：必测项固定为 UAT-V01..V06 与 UAT-I01..I04，覆盖中文换行/孤行/溢出、contents/timeline balance、theme fidelity、bold/highlight、contain image/missing-media placeholder、notes、native table/code editability、group move/ungroup/member edit。"
    - "D-21：任何 PENDING/FAIL/BLOCKED、缺字段、hash mismatch 或非真实 viewer 都阻止 Phase 44/milestone acceptance；auto mode 不得填写 tester/observation/time/PASS，失败或阻断必须形成明确 gap。"
  artifacts:
    - path: ".planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md"
      provides: "hash-bound、初始 pending、由真实测试者逐项填写的 milestone viewer evidence。"
    - path: "skills/school-pptx/scripts/verify_school_pptx.py"
      provides: "只校验 UAT identity/schema/hash/status 完整性的 lint，不生成或推断人工 PASS。"
  key_links:
    - from: "public verification evidence"
      to: ".planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md"
      via: "fixture_sha256、pptx_sha256 和 relative evidence_source 精确绑定"
      pattern: "fixture_sha256|pptx_sha256|evidence_source"
    - from: ".planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md"
      to: "skills/school-pptx/references/visual-uat.md"
      via: "同一 10 项 ID、result enum 与 human sign-off contract"
      pattern: "UAT-V01|UAT-V02|UAT-V03|UAT-V04|UAT-V05|UAT-V06|UAT-I01|UAT-I02|UAT-I03|UAT-I04"
    - from: "human UAT result"
      to: "Phase 44 acceptance or explicit gap"
      via: "仅 all PASS + complete identity + matching hashes 可接受；其余状态 fail closed"
      pattern: "PENDING|PASS|FAIL|BLOCKED|gap"
---

<objective>
把自动验证通过的准确 canonical PPTX 交给真实 PowerPoint/WPS 人类测试者，并停在不可自动越过的视觉与编辑验收门。

Purpose: 证明 viewer 中实际中文排版、主题呈现、备注和原生对象编辑体验，而不是把 OOXML、截图或自动化断言冒充人工观察。
Output: hash-bound `44-UAT.md` pending form、真实 viewer 的 10 项人工结果、完整性 lint；若未全 PASS，则是明确 gap 而非 Phase 44 completion。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-CONTEXT.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-RESEARCH.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-UI-SPEC.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-03-PLAN.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-UI-SPEC.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
@skills/school-pptx/references/visual-uat.md
@skills/school-pptx/scripts/verify_school_pptx.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成 fresh canonical artifacts 与不可自动批准的 pending UAT 证据（D-18/D-19/D-21）</name>
  <files>.planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md, skills/school-pptx/scripts/verify_school_pptx.py</files>
  <read_first>
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-CONTEXT.md
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UI-SPEC.md
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-03-PLAN.md
    - skills/school-pptx/references/visual-uat.md
    - skills/school-pptx/scripts/verify_school_pptx.py
    - skills/school-pptx/fixtures/school-pptx-full.md
  </read_first>
  <action>在测试者指定且可保留到验收结束的 caller workdir 运行 public `school-pptx.sh verify --workdir &lt;uat-workdir&gt;`，必须先得到 8-gate/6-negative/21-gate 全通过的 current evidence。创建 `44-UAT.md`，frontmatter 初始 `status: pending`，viewer/version/OS/tested_at/tester 保持空字符串；从本次 `verification.json` 机械复制 canonical fixture/PPTX SHA-256 和相对 evidence source，并记录要打开的 workdir-relative delivery PPTX，不复制 PPTX 或 evidence 到正常 user delivery。

正文预建 UAT-V01..V06 与 UAT-I01..I04 十行，每行 Result=`PENDING`，slides/objects、Human observation、Evidence reference 留待人类填写；包含允许 viewer 精确枚举、四状态词汇、hash mismatch=BLOCKED、all-PASS sign-off 和自动化禁止声明。扩展 verifier 的 UAT lint 只能检查 schema/enum/identity/hash/ID completeness：它对初始表单返回明确 `UAT_PENDING`/非 acceptance，不得写入或默认 viewer/tester/time/observation/PASS。增加源码 guard，拒绝 auto-fill `PASS`、伪造测试者/版本/时间或以 OOXML status 映射人工结果。</action>
  <verify>
    <automated>sh -eu -c 'test -n "${SCHOOL_PPTX_UAT_WORKDIR:-}"; py="${SCHOOL_PPTX_PYTHON:-python3}"; uat=".planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md"; evidence="$SCHOOL_PPTX_UAT_WORKDIR/evidence/verification.json"; started_at="$(date +%s)"; skills/school-pptx/scripts/school-pptx.sh verify --workdir "$SCHOOL_PPTX_UAT_WORKDIR"; test -f "$evidence"; test -f "$uat"; "$py" -c '"'"'import json, pathlib, sys, yaml; sys.path.insert(0, "skills/school-pptx/scripts"); import verify_pptx_renderer; evidence_path, uat_path, started_at = pathlib.Path(sys.argv[1]), pathlib.Path(sys.argv[2]), int(sys.argv[3]); data = json.loads(evidence_path.read_text(encoding="utf-8")); top = ["dependency-readiness", "example-generation", "template-validation", "canonical-render", "structural-inspection", "phase-43-regression", "negative-cases", "evidence-integrity"]; neg = [("unknown-theme", "THEME_UNKNOWN"), ("unknown-layout", "LAYOUT_UNKNOWN"), ("missing-media", "MEDIA_MISSING"), ("unsupported-styling", "UNSUPPORTED_STYLE"), ("unresolved-review-marker", "REVIEW_MARKER_UNRESOLVED"), ("template-manifest-mismatch", "TEMPLATE_MANIFEST_MISMATCH")]; expected_negatives = [case_id for case_id, _ in neg]; expected43 = list(verify_pptx_renderer.PHASE_43_GATE_ORDER); registry = data["registry"]; negative_registry = data["negative_registry"]; phase43 = data["phase_43"]; phase43_registry = phase43["registry"]; negatives = data["negatives"]; artifacts = data["canonical"]["artifacts"]; uat = yaml.safe_load(uat_path.read_text(encoding="utf-8").split("---", 2)[1]); assert evidence_path.stat().st_mtime &gt;= started_at - 1 and data["status"] == "passed"; assert registry["required"] == top and registry["called"] == top and registry["unique"] is True and registry["dynamic_skips"] == 0; assert negative_registry["required"] == expected_negatives and negative_registry["called"] == expected_negatives and negative_registry["unique"] is True and negative_registry["dynamic_skips"] == 0; assert [(case["id"], case["observed_code"]) for case in negatives] == neg and all(case["exit_code"] != 0 and case["expected_code"] == case["observed_code"] for case in negatives); assert phase43["status"] == "passed" and phase43_registry["required"] == expected43 and phase43_registry["called"] == expected43 and phase43_registry["unique"] is True and phase43_registry["dynamic_skips"] == 0; fixture = next(item for item in artifacts if item["path"].endswith(".md")); pptx = next(item for item in artifacts if item["path"].endswith(".pptx")); assert len(fixture["sha256"]) == len(pptx["sha256"]) == 64 and uat["fixture_sha256"] == fixture["sha256"] and uat["pptx_sha256"] == pptx["sha256"]'"'"' "$evidence" "$uat" "$started_at"; capture="$(mktemp -d /tmp/school-pptx-uat-lint.XXXXXX)"; trap '"'"'rm -rf "$capture"'"'"' EXIT HUP INT TERM; set +e; "$py" skills/school-pptx/scripts/verify_school_pptx.py --check-uat "$uat" --evidence "$evidence" &gt;"$capture/stdout" 2&gt;"$capture/stderr"; lint_rc="$?"; set -e; test "$lint_rc" -ne 0; test "$(wc -c &lt;"$capture/stdout")" -le 8192; test "$(wc -c &lt;"$capture/stderr")" -le 8192; test ! -s "$capture/stderr"; test "$(tr -d '"'"'\r\n'"'"' &lt;"$capture/stdout")" = "UAT_PENDING"'</automated>
  </verify>
  <acceptance_criteria>
    - Prerequisite assertion: `SCHOOL_PPTX_UAT_WORKDIR` 非空且本次 public verify 成功后，current `verification.json` 与 `44-UAT.md` 均存在；evidence mtime 属于本次运行、status passed，8/6/21 registries 的 required/called 均按各自固定顺序精确相等、unique=true、dynamic_skips=0，六负例结果列表仍逐项满足 id/code/exit_code，fixture/PPTX hashes 均为 64 位且 UAT 引用与其逐字相等。任一前置断言失败都在运行 UAT lint 前令 automated verify 非零。
    - Pending identity assertion: `status: pending`，viewer/version/OS/tested_at/tester 空白，10 项 Result 全为 PENDING；没有自动 observation 或 sign-off。
    - Lint boundary assertion: 仅当前置断言全通过后单独运行 UAT lint；捕获其退出码和 bounded stdout/stderr，要求退出码非零、stderr 为空且去除换行后的 stdout 精确等于 stable code `UAT_PENDING`，不得以任意非零或其他错误代替；AST/source guard 对 auto PASS、fake identity/time、structure→human mapping mutations 全部失败。
    - Path assertion: evidence source 和 artifact 引用为 caller-workdir-relative 表达，planning form 不含 home/tmp 绝对路径、PPTX bytes、traceback 或 user raw text。
  </acceptance_criteria>
  <done>自动执行已准备准确输入与 pending form，并以 `UAT PENDING` 停止；VER-10 尚未完成。</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: 由真实测试者在 PowerPoint 或 WPS 完成 10 项 viewer UAT（D-18/D-20/D-21）</name>
  <files>.planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md</files>
  <read_first>
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UI-SPEC.md
    - skills/school-pptx/references/visual-uat.md
    - skills/school-pptx/fixtures/school-pptx-full.md
  </read_first>
  <action>暂停自动执行并把 hash-matched canonical PPTX 的 workdir-relative 路径交给实际人类测试者。测试者必须在桌面 Microsoft PowerPoint 或 WPS Presentation 中以编辑模式打开该文件，亲自填写 viewer 完整名称/version、OS/version、带时区 ISO 8601 timestamp、tester identity；逐项执行并填写 UAT-V01 中文换行/孤行/溢出、V02 contents balance、V03 timeline balance、V04 theme fonts/colors/decor/footer、V05 bold/highlight、V06 contain image 与 missing-media best-effort placeholder，I01 notes viewer surface/画布隔离、I02 native table edit、I03 native code edit/undo、I04 timeline/gallery group move/ungroup/member edit（使用测试副本，避免改 canonical hash）。每项填写实际 slides/objects、具体人工 observation、可选 evidence reference 和且只用 PASS/FAIL/BLOCKED；截图仅可辅助，不能代替 notes/edit/group 操作。代理不得代填、推断、观察预览后签署或把自动 gate 转为 PASS。</action>
  <verify>
    <manual>实际测试者确认已在记录的 PowerPoint/WPS 版本中打开与 frontmatter SHA-256 相符的 PPTX，完成 10 项操作并亲自填写身份、时间、逐项结果与观察。</manual>
  </verify>
  <acceptance_criteria>
    - Viewer assertion: viewer 必须精确为 Microsoft PowerPoint 或 WPS Presentation，version/OS/tester/tested_at 均由实际操作者填写且非空。
    - Coverage assertion: UAT-V01..V06/I01..I04 恰好各一项，均有具体 physical slides/objects 与 human observation，不以总评代替逐项证据。
    - Interaction assertion: I01 实际打开 notes surface，I02/I03 实际修改并撤销，I04 实际移动/取消组合/成员编辑；截图或只播放不满足。
    - Status assertion: 每项只能 PASS/FAIL/BLOCKED；任何未执行项保持 PENDING 并继续阻断，不允许代理补全。
  </acceptance_criteria>
  <done>只有真实测试者完成并填写全部 10 项后 checkpoint 才可恢复；自动执行本身永远不能完成本任务。</done>
</task>

<task type="auto">
  <name>Task 3: 校验人工 evidence，all-PASS 才接受，否则登记明确 gap（D-19/D-21）</name>
  <files>.planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md, skills/school-pptx/scripts/verify_school_pptx.py</files>
  <read_first>
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UI-SPEC.md
    - skills/school-pptx/references/visual-uat.md
    - skills/school-pptx/scripts/verify_school_pptx.py
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </read_first>
  <action>checkpoint 恢复后，运行 UAT lint：重新计算 evidence 指向的 fixture/PPTX SHA-256，与 `44-UAT.md` 精确比较；校验真实 viewer enum、完整 version/OS/tested_at/tester/evidence source、十个唯一 ID、每项 slides/objects + observation、Result enum、无 PENDING。lint 只能读取人类填写值，不改写结果。

仅当 identity/hash 完整且十项全部 PASS 时，允许将 UAT frontmatter `status` 由测试者/执行流程确认成 `passed` 并生成 acceptance-ready 结论；不得因自动验证通过补齐任何人类字段。任一 FAIL/BLOCKED/PENDING/hash mismatch/缺字段时保持 `failed` 或 `blocked/pending`，在 `44-UAT.md` 的 Gaps 区登记 requirement 至少 VER-10、viewer 环境、fixture/PPTX hashes、受影响 ID、观察和复验条件，并阻止 Phase 44/ROADMAP/REQUIREMENTS/milestone completion；修复产物后必须重新 public verify 并用新 hashes 复验。第二 viewer 可追加独立表，不能抵消第一 viewer failure。</action>
  <verify>
    <automated>${SCHOOL_PPTX_PYTHON:-python3} skills/school-pptx/scripts/verify_school_pptx.py --check-uat .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md --evidence "$SCHOOL_PPTX_UAT_WORKDIR/evidence/verification.json" &amp;&amp; git diff --check -- .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Identity assertion: viewer/version/OS/tester/time/source 完整，fixture/PPTX current hashes 重算匹配；任一 mismatch 精确 BLOCKED/non-zero。
    - All-pass assertion: 10 个唯一 ID 均有 observation/slides 且 Result=PASS 时 lint 才 exit 0；PENDING/FAIL/BLOCKED/缺项/重复项均非零。
    - Gap assertion: 任一非 PASS 都有 VER-10、viewer、hashes、affected IDs、observation、retest condition 的明确 gap，Phase/milestone 未完成。
    - Anti-forgery assertion: lint 不写 viewer/tester/time/observation/result，不从 OOXML/evidence gate/screenshot 推导 PASS；第二 viewer 结果独立。
  </acceptance_criteria>
  <done>VER-10 仅在真实人类 10/10 PASS 且 identity/hash lint 通过时完成；否则留下可复验 gap 并阻止 acceptance。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| Artifact 路径/身份漂移 | 人工打开旧 PPTX 或另一个同名文件 | current evidence relative source + fixture/PPTX SHA-256 重算；mismatch=BLOCKED。 |
| Symlink/TOCTOU | hash 后 artifact 被替换再交 viewer | 测试前重算 held artifact hash，测试期间 canonical 只读；任何变化需新 run/UAT。 |
| Evidence 泄漏 | UAT 写 home path、raw user text、traceback 或 PPTX bytes | 只写相对 source、hash、bounded observations；lint 拒绝 absolute path/traceback/oversized fields。 |
| 人工身份伪造 | agent 填 tester/version/time/PASS 或把结构 gate 当观察 | autonomous false + human checkpoint + AST/source anti-fill guard + manual sign-off。 |
| Fixture 污染 | 编辑操作修改 canonical deck，导致 hash 失效 | 所有 table/code/group 操作在测试副本完成；canonical hash 前后复核。 |
| FAIL 被掩盖 | 第二 viewer PASS 或总评覆盖已知 failure | viewer 表独立；任一 FAIL/BLOCKED 形成 gap，修复和 hash-bound retest 前不接受。 |
</threat_model>

<verification>
- `skills/school-pptx/scripts/school-pptx.sh verify --workdir "$SCHOOL_PPTX_UAT_WORKDIR"`
- `${SCHOOL_PPTX_PYTHON:-python3} skills/school-pptx/scripts/verify_school_pptx.py --check-uat .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md --evidence "$SCHOOL_PPTX_UAT_WORKDIR/evidence/verification.json"`
- 人工：在记录的 Microsoft PowerPoint 或 WPS Presentation 中打开 hash-matched PPTX 并完成 UAT-V01..V06、UAT-I01..I04。
- `git diff --check -- .planning/phases/44-verification-gate-runtime-notes-and-uat/44-UAT.md skills/school-pptx/scripts/verify_school_pptx.py .planning/phases/44-verification-gate-runtime-notes-and-uat/44-04-PLAN.md`
</verification>

<success_criteria>
- 自动执行先得到 fresh public PASS evidence，再创建 identity/hash-bound `PENDING` 表单并停止。
- 至少一个真实 PowerPoint/WPS 人类测试者完成全部 10 项视觉与编辑操作。
- 仅 identity/hash 完整且 10/10 人工 PASS 时 VER-10 和 Phase 44 可接受。
- 任一 PENDING/FAIL/BLOCKED/mismatch 都产生明确 gap 并阻止 Phase 44 与 v1.17 milestone completion。
</success_criteria>
