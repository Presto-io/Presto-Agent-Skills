---
phase: 44-verification-gate-runtime-notes-and-uat
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - skills/school-pptx/scripts/verify_school_pptx.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
  - skills/school-pptx/scripts/markdown_contract.py
  - skills/school-pptx/scripts/template_report.py
autonomous: true
requirements:
  - VER-04
  - VER-06
  - VER-07
  - VER-08
  - VER-09
must_haves:
  truths:
    - "D-01/D-04：public verify 实际按固定 8 gate 顺序运行全部 public flow，required==called、唯一、零动态 skip，并复用而非替代 Phase 43 固定 21-gate aggregate。"
    - "D-02/D-05：canonical 与六负例只在本次 workdir 的隔离 run tree 生成；fresh delivery 只含同 stem Markdown/PPTX，失败 evidence 不会被旧 PASS 覆盖。"
    - "D-03：fresh artifact 的结构 claim 由 reopen/hash/raw operands 重算并以 bounded relative evidence 发布，不复制常量或 Phase 43 旧 JSON。"
    - "D-08：负例 registry 固定且仅含 `unknown-theme`、`unknown-layout`、`missing-media`、`unsupported-styling`、`unresolved-review-marker`、`template-manifest-mismatch`，每项断言精确 stable code、非零、8KiB bound、无 traceback/false success、无 collateral write。"
    - "D-09：template/manifest mismatch 只 mutation workdir copies；committed template、manifest、fixture 与 user input hashes 不变。"
    - "D-10：transition evidence 只允许 `none|preserved|generated`，当前 observed `none` 合法且不得转为 warning/failure。"
  artifacts:
    - path: "skills/school-pptx/scripts/verify_school_pptx.py"
      provides: "八 gate 完整 orchestration、fresh structural evidence、六负例、evidence-integrity 与 mutation guards。"
    - path: "skills/school-pptx/scripts/markdown_contract.py"
      provides: "共享 validate/render 路径的 bounded `REVIEW_MARKER_UNRESOLVED` 诊断。"
    - path: "skills/school-pptx/scripts/template_report.py"
      provides: "隔离 template/manifest mismatch 的稳定 `TEMPLATE_MANIFEST_MISMATCH` 诊断或可规范化 observed field。"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "保持 Phase 43 21-gate authority，并为 fresh artifact inspection 提供受控 helper/机器证据。"
  key_links:
    - from: "skills/school-pptx/scripts/verify_school_pptx.py"
      to: "skills/school-pptx/scripts/school-pptx.sh"
      via: "example、template-report、render 都走用户同一 public shell contract"
      pattern: "example-generation|template-validation|canonical-render"
    - from: "skills/school-pptx/scripts/verify_school_pptx.py"
      to: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      via: "`phase-43-regression` 调用固定 21 gate 并再次验证 registry raw evidence"
      pattern: "phase-43|PHASE_43_GATE_ORDER|required|called|dynamic_skips"
    - from: "skills/school-pptx/scripts/markdown_contract.py"
      to: "unresolved-review-marker negative"
      via: "公共 validate/render 同源 diagnostic collector"
      pattern: "REVIEW_MARKER_UNRESOLVED|待补充|AI草稿"
    - from: "fresh delivery PPTX"
      to: "verification.json canonical.structure"
      via: "hash-bound bounded OOXML reopen and independent recomputation"
      pattern: "logical_to_physical|contents|notes|tables|code|groups|whole_slide_picture_count|transition_mode"
---

<objective>
把 Plan 1 的 fail-closed 骨架接成完整、可重复且不可能动态漏项的公共验证门。

Purpose: 用同一 public example/template/render 流程生成 fresh canonical artifacts，独立重开检查结构，复跑 Phase 43 的 21-gate authority，并以六个隔离负例和 integrity mutations 阻断 false green。
Output: 完整 8-gate `verify --workdir`、fresh structural JSON/Markdown、固定 6-case registry、两个缺失 stable diagnostics 和重复运行/聚合证据。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-CONTEXT.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-RESEARCH.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-PATTERNS.md
@.planning/phases/44-verification-gate-runtime-notes-and-uat/44-01-PLAN.md
@.planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
@skills/school-pptx/fixtures/school-pptx-full.md
@skills/school-pptx/templates/standard-school.manifest.yaml
@skills/school-pptx/templates/standard-school.pptx
@skills/school-pptx/scripts/school-pptx.sh
@skills/school-pptx/scripts/verify_school_pptx.py
@skills/school-pptx/scripts/verify_pptx_renderer.py
@skills/school-pptx/scripts/markdown_contract.py
@skills/school-pptx/scripts/template_report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接入 fresh public canonical flow、hash-bound structure 与 Phase 43 regression（D-01/D-02/D-03/D-04/D-05/D-10）</name>
  <files>skills/school-pptx/scripts/verify_school_pptx.py, skills/school-pptx/scripts/verify_pptx_renderer.py</files>
  <read_first>
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-CONTEXT.md
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-RESEARCH.md
    - .planning/phases/43-editable-pptx-renderer-and-pagination/43-VERIFICATION.md
    - skills/school-pptx/fixtures/school-pptx-full.md
    - skills/school-pptx/templates/standard-school.manifest.yaml
    - skills/school-pptx/scripts/verify_school_pptx.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/scripts/school-pptx.sh
  </read_first>
  <action>实现 `example-generation`、`template-validation`、`canonical-render` 三 gate，必须通过 public `school-pptx.sh example/template-report/render` 黑盒执行，并把最终 canonical same-stem pair 原子发布到 `delivery/`；断言 delivery 恰好两个文件，Markdown bytes/hash 与 example output 一致，PPTX 非空，所有 sidecars 留在 evidence/work。

实现 `structural-inspection`，只读取 evidence 命名且 SHA-256 绑定的 fresh delivery PPTX，并复用/最小抽取 Phase 43 的 bounded ZIP/XML helper：记录 13 logical、32 physical（未来 contract 有意变更前保持 baseline）、ordered layout part mapping、完整 logical→physical indices、source contents entries 与 reopen contents text/count；expected/observed notes indices/count；media relationships/embedded hashes/zero-crop native pictures；native tables 的 page/row/column/header；code source/frozen/reopen hash 和 native textbox/run properties；native group/timeline/gallery counts；按页面几何计算 `whole_slide_picture_count:0`；observed transition enum。所有 equality 在 aggregate 从 raw operands 重算，不接受 producer `passed:true` 或常量 counts。

实现 `phase-43-regression`，调用现有 `verify_pptx_renderer.py phase-43` 或稳定 `run_phase_43()`，验证准确 21-gate 顺序：`contract-model`、`pagination`、`frozen-slot-content`、`frozen-numbering-row-heights`、`ooxml-bootstrap`、`editable-objects`、`code-literal-roundtrip`、`mixed-fragment-capacity`、`frame-capacity-consistency`、`emit-structure`、`frozen-plan-emission`、`cli-publication`、`best-effort`、`publication-safety`、`publication-descriptor-race`、`table-header-only`、`object-error-bounded`、`media-descriptor-binding`、`template-reader-security`、`determinism`、`phase_41_42_regression`；再次断言 required==called、顺序、唯一、`dynamic_skips=0`，不只看 exit code。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; export SCHOOL_PPTX_PYTHON="$HOME/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/verify_school_pptx.py skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_school_pptx.py --self-test canonical</automated>
  </verify>
  <acceptance_criteria>
    - Public flow assertion: example/template-report/render 均从 shell public entry 实际调用，exit/output bytes/relative artifacts 被记录；delivery 仅同 stem `.md/.pptx`。
    - VER-06 assertion: evidence 含 logical/physical raw counts、ordered layout mapping、完整 logical-to-physical mapping、source/reopen contents operands，并由 aggregate 比较；canonical 为 13→32。
    - VER-07 assertion: notes/media/table/code/group/full-slide-picture 所有必需 raw fields 非空或明确为零，且重新打开 evidence hash 指定的 PPTX 独立复算一致。
    - Phase 43 assertion: 21 个名称与上述顺序精确一致，required/called 相等且唯一、`dynamic_skips=0`；删除/reorder/constant-result mutation 令顶层 gate 非零。
    - VER-09 assertion: `transition_mode` 只允许 `none|preserved|generated`，fresh canonical observed `none` 且 gate PASS。
  </acceptance_criteria>
  <done>fresh canonical artifacts、结构证据和 Phase 43 21-gate regression 都绑定到本次 raw operands，8-gate 前六项可真实执行。</done>
</task>

<task type="auto">
  <name>Task 2: 增加共享 review-marker 诊断与隔离 template mismatch stable code（D-08/D-09）</name>
  <files>skills/school-pptx/scripts/markdown_contract.py, skills/school-pptx/scripts/template_report.py, skills/school-pptx/scripts/verify_school_pptx.py</files>
  <read_first>
    - skills/school-pptx/references/markdown-contract.md
    - skills/school-pptx/references/template-contract.md
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/scripts/template_report.py
    - skills/school-pptx/scripts/verify_school_pptx.py
    - skills/school-pptx/scripts/verify_markdown_contract.py
    - skills/school-pptx/scripts/verify_template_manifest.py
  </read_first>
  <action>在 `markdown_contract.py` 共享 validate/render scanner 中只识别文档约定的 unresolved markers `{{待补充: ...}}` 与 `{{AI草稿: ...}}`，产生 stable `REVIEW_MARKER_UNRESOLVED`、locatable bounded message/fix；保持 fenced code opacity，不把普通花括号、代码模板或已解决正文误报。扩展现有 contract regression 验证 validate 与 render 都非零且 code 相同。

为 template-report 的 observed failures 增加稳定 `TEMPLATE_MANIFEST_MISMATCH` code，或在 verifier adapter 基于隔离 copy 的 report exit+failure fields 规范化为该 code；不得依赖中文 message substring，不改变 canonical template-report success semantics。fixture mutation 只复制 `standard-school.pptx` 与 manifest 到 `work/<run-id>/negatives/template-manifest-mismatch/`，修改一个 mapped geometry/placeholder fact并针对两份 copy 运行；前后计算 committed pair hashes。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; export SCHOOL_PPTX_PYTHON="$HOME/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3" &amp;&amp; "$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/markdown_contract.py skills/school-pptx/scripts/template_report.py skills/school-pptx/scripts/verify_school_pptx.py &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_school_pptx.py --self-test stable-diagnostics</automated>
  </verify>
  <acceptance_criteria>
    - Review marker assertion: 两种 documented markers 在普通 Markdown 中均由 validate/render 返回 `REVIEW_MARKER_UNRESOLVED`；相同文本在 fenced code、普通 `{}` 与已定稿文档不触发。
    - Mismatch assertion: isolated report 非零且 observed exact code 为 `TEMPLATE_MANIFEST_MISMATCH`，不靠自然语言匹配；canonical report 仍成功。
    - Source preservation assertion: canonical template、manifest、fixture 和 user input before/after SHA-256 完全相等，所有 mutation paths 位于单一 negative case root。
    - Bounded assertion: 两个失败路径 stdout+stderr 均小于 8192 bytes、无 traceback、绝对路径或 false success wording。
  </acceptance_criteria>
  <done>六负例所需的两个新 stable diagnostics 已进入真实公共路径，且 mismatch 不触碰 canonical assets。</done>
</task>

<task type="auto">
  <name>Task 3: 固定六负例 registry、完整 evidence-integrity 与双 fresh-run aggregate（D-03/D-04/D-05/D-08/D-09）</name>
  <files>skills/school-pptx/scripts/verify_school_pptx.py</files>
  <read_first>
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-RESEARCH.md
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-PATTERNS.md
    - .planning/phases/44-verification-gate-runtime-notes-and-uat/44-01-PLAN.md
    - skills/school-pptx/scripts/verify_school_pptx.py
    - skills/school-pptx/scripts/markdown_contract.py
    - skills/school-pptx/scripts/template_report.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
  </read_first>
  <action>实现固定 ordered negative registry，顺序和 expected code 精确为：`unknown-theme→THEME_UNKNOWN`、`unknown-layout→LAYOUT_UNKNOWN`、`missing-media→MEDIA_MISSING`、`unsupported-styling→UNSUPPORTED_STYLE`、`unresolved-review-marker→REVIEW_MARKER_UNRESOLVED`、`template-manifest-mismatch→TEMPLATE_MANIFEST_MISMATCH`。每 case 在专属 copied tree 执行 public validate/render/template-report，记录 exit、expected/observed code、capture byte count、traceback/false-success scans、case-local write inventory、caller/canonical sentinel before/after hashes；允许既有 render best-effort pair，但 status 必须 failed-as-expected。required dependency 缺失阻断 verify，不得把任一 case 记 skip。

完成 `evidence-integrity`：在 publish 前检查 schema version、8/6 两个 registry exact equality/order/unique、`dynamic_skips=0`、全部 gate/case status、relative paths、SHA-256 格式、bounded collections/diagnostics、transition enum、VER-04..VER-09 evidence coverage、JSON/Markdown同源字段及无 absolute path/traceback/raw bytes。加入 AST/source/mutation guards，至少把 hardcoded overall PASS、constant structural counts、missing/reordered/duplicate gate/case、absolute path、oversized diagnostic、stale evidence read 和 copied Phase 43 result 判为失败。最后 public verify 在两个全新 roots 连续成功，比较除 run id/timestamp 外的 deterministic semantic fields，并在同一 root 注入失败证明 current evidence 变 failed。</action>
  <verify>
    <automated>export PYTHONPATH="$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l" &amp;&amp; export SCHOOL_PPTX_PYTHON="$HOME/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3" &amp;&amp; export PYTHONPYCACHEPREFIX="/tmp/presto-pycache" &amp;&amp; "$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_school_pptx.py --self-test negatives-integrity &amp;&amp; d1="$(mktemp -d /tmp/school-pptx-v44-a.XXXXXX)" &amp;&amp; d2="$(mktemp -d /tmp/school-pptx-v44-b.XXXXXX)" &amp;&amp; skills/school-pptx/scripts/school-pptx.sh verify --workdir "$d1" &amp;&amp; skills/school-pptx/scripts/school-pptx.sh verify --workdir "$d2"</automated>
  </verify>
  <acceptance_criteria>
    - Negative registry assertion: required/called exact ordered equality、元素唯一、六项全执行且零 skip；任一 fixture/case 删除、renamed、unexpected success 或 wrong code 都使 verify 非零。
    - Per-case assertion: 六项均 `exit_code != 0`、exact code、output&lt;8192、no traceback、no false success、sentinel unchanged、writes confined to case root。
    - Integrity assertion: 8 个顶层 gate 全有 raw result；aggregate 从 raw outcomes 重算，以上每个 mutation 都令 `evidence-integrity` 和总状态失败。
    - Repeatability assertion: 两个 fresh public roots 都通过，delivery pair hashes 与 deterministic structure/registry fields 一致；所有 evidence paths 相对各自 root。
    - Failure freshness assertion: 后续故障 rerun 发布 current `status: failed`，旧 PASS 只能作为历史且不能被 public current path引用。
  </acceptance_criteria>
  <done>VER-04、VER-06..VER-09 由完整 public aggregate、固定六负例和 raw evidence integrity 共同关闭。</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block_on=high

| Threat | Risk | Mitigation and verification |
|---|---|---|
| Fixture 路径越界 | negative mutation 写到 canonical template/user input | 每 case 独立 copied root、relative target assertion、canonical before/after hashes。 |
| Symlink/TOCTOU | media/template/evidence 在检查与读取间被替换 | 复用 held descriptor、bounded ZIP/XML/media reader；identity change fail closed；race regression。 |
| 结构证据常量回填 | 固定 13/32、PASS 或 expected hash 掩盖 fresh artifact 退化 | fresh delivery hash binding、raw reopen operands、aggregate 重算、AST/source mutation guards。 |
| 负例动态漏项 | fixture discovery 或 dependency 条件导致 case 未运行 | 固定 ordered tuple + independent required set + exact called equality + zero dynamic skips。 |
| 用户文本/路径/traceback 泄漏 | 错误消息暴露 Markdown、home/temp path 或 stack | 8KiB capture cap、stable codes、path sanitizer、collection bounds、integrity scans。 |
| 资源耗尽 | 恶意 ZIP/XML、巨大 subprocess output、无限 case state | 复用 Phase 43 metadata/XML limits、timeouts、capture bounds、固定 8/6 registries。 |
</threat_model>

<verification>
- `export PYTHONPATH="$HOME/.cache/uv/archive-v0/I2PFDpRQMCS91p6l"`
- `export SCHOOL_PPTX_PYTHON="$HOME/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3"`
- `export PYTHONPYCACHEPREFIX="/tmp/presto-pycache"`
- `"$SCHOOL_PPTX_PYTHON" -m py_compile skills/school-pptx/scripts/verify_school_pptx.py skills/school-pptx/scripts/verify_pptx_renderer.py skills/school-pptx/scripts/markdown_contract.py skills/school-pptx/scripts/template_report.py`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_school_pptx.py --self-test canonical`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_school_pptx.py --self-test stable-diagnostics`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_school_pptx.py --self-test negatives-integrity`
- `skills/school-pptx/scripts/school-pptx.sh verify --workdir "$(mktemp -d /tmp/school-pptx-v44.XXXXXX)"`
- `"$SCHOOL_PPTX_PYTHON" skills/school-pptx/scripts/verify_pptx_renderer.py phase-43`
- `git diff --check -- skills/school-pptx/scripts/verify_school_pptx.py skills/school-pptx/scripts/verify_pptx_renderer.py skills/school-pptx/scripts/markdown_contract.py skills/school-pptx/scripts/template_report.py .planning/phases/44-verification-gate-runtime-notes-and-uat/44-02-PLAN.md`
</verification>

<success_criteria>
- Public verify 在两个 fresh roots 可重复 PASS，固定 8 gates 和固定 6 negatives 均 required==called、顺序一致、唯一、零 skip。
- Fresh canonical 13→32 的 pagination、contents、notes、media、table、code、groups、whole-slide-picture=0 和 transition=`none` 均有 hash-bound raw evidence。
- Phase 43 准确 21-gate aggregate 实际执行且 registry 复核通过。
- 六负例 exact stable codes、bounded output、隔离与 source preservation 全部通过；任一 integrity mutation 阻断总状态。
</success_criteria>
