# Phase 07 Plan: Jiaoan Jihua Skill

**Milestone:** v1.4 Jiaoan Jihua Skill
**Created:** 2026-05-31
**Mode:** mvp

## Goal

Create the next concrete portable skill in this repository: `jiaoan-jihua`, a markdown-first workflow for Chinese teaching-schedule and lesson-plan progress tables that render through the Presto jiaoan-jihua Typst template.

## Scope

- Add `skills/jiaoan-jihua/SKILL.md` as the canonical skill source.
- Add a reusable `jiaoan-jihua-full.md` Markdown template that demonstrates the supported intermediate structure.
- Add script code that calls the Presto `presto-template-jiaoan-jihua` binary, writes Typst output, compiles PDF with Typst, and can compare against reference Typst/PDF fixtures.
- Validate against the provided `jiaoan-jihua-full.md`, `jiaoan-jihua-full.typ`, and `jiaoan-jihua-full.pdf` fixture set.

## Tasks

- [x] Define the skill contract, trigger wording, inputs, outputs, verification, and safety boundaries.
- [x] Implement scriptable Markdown-to-Typst/PDF rendering and fixture comparison.
- [x] Verify generated Typst and PDF against the provided fixture artifacts.

## Validation

The phase is complete when the script can generate Typst identical to the provided reference and compile a PDF whose stable content matches the provided reference after normalizing volatile PDF metadata.
