# Phase 48: 确定性定向渲染与干净批量交付 - Discussion Log

> **仅供审计参考。** 研究、规划和执行代理不得以本文件为输入。
> 锁定决定位于 CONTEXT.md；本日志只保留讨论过的替代方案。

**Date:** 2026-07-18
**Phase:** 48-确定性定向渲染与干净批量交付
**Areas discussed:** 定向事实策略、硬条件四态、版本生成矩阵、批量发布结构

---

## 定向事实策略

| 问题 | 备选方案 | 用户选择 |
|------|----------|----------|
| 定向理由记录粒度 | 逐事实记录 / 按模块记录 / 只记录最终顺序 / 由实现决定 | 逐事实记录 |
| 定向依据保存位置 | 完整写入 YAML / YAML 简要索引 + 隐藏详细证据 / 全部隐藏 | YAML 简要索引 + 隐藏详细证据 |
| 可省略事实范围 | 核心必保留、其余按相关性取舍 / 只排序不省略 / 大幅精简 | 核心必保留、其余按相关性取舍 |
| 人工覆盖方式 | CLI 按事实 ID 覆盖 / target 正文写指令 / 不提供覆盖 | CLI 按事实 ID 覆盖 |

**Notes:** 覆盖参数要冻结进版本 metadata，不能污染 canonical 事实源。

---

## 硬条件四态

| 问题 | 备选方案 | 用户选择 |
|------|----------|----------|
| 四态判定方式 | 严格证据 / 关键词推断 / 全部人工标注 | 严格证据 |
| 缺口放行粒度 | 按目标单独确认 / 整批一次确认 / 长期允许 | 按目标单独确认 |
| 未知是否阻断 | 默认阻断可放行 / 默认允许并警告 / 等同缺口 | 默认允许并警告 |
| 结果呈现 | CLI 表格 + 隐藏 JSON / 完整写入正式 YAML / 只在终端显示 | CLI 表格 + 隐藏 JSON |

**Notes:** 未知与缺口保持语义区分；任何输出都不得暗示未知条件已经满足。

---

## 版本生成矩阵

| 问题 | 备选方案 | 用户选择 |
|------|----------|----------|
| batch 默认集合 | 通用 + 全部 confirmed targets / 仅目标版 / 仅显式选择 | 通用 + 全部 confirmed targets |
| 每版本主题数量 | 正式一个 + 三主题预览 / 三主题全部正式 / 通用三主题、目标单主题 | 三主题全部正式 |
| 1/2 页对照交付 | 只发布主版本 / 两种页数都正式 / 整批统一页数 | 只发布主版本 |
| 正式 Markdown 形态 | 独立可审阅最终文档 / 轻量引用清单 / 完整事实源副本 | 独立可审阅最终文档 |

**Notes:** 用户明确指出 PDF 离线生成开销不大且不消耗 AI，因此三个主题都直接生成正式 PDF。

---

## 批量发布结构

| 问题 | 备选方案 | 用户选择 |
|------|----------|----------|
| 投递根组织 | 平铺全部正式三件套 / 每版本子目录 / 按单位分目录 | 平铺全部正式三件套 |
| history 粒度 | 完整 current 集合 / 仅变化版本 / batch 与 render 不同 | 更新了谁，归档谁 |
| 同单位多岗位命名 | 冲突时加岗位 / 始终包含岗位 / 失败要求改标签 | 始终包含岗位 |
| batch 目标减少 | 新矩阵收敛并归档移除项 / 保留旧版本 / 失败要求另行清理 | 新矩阵收敛并归档移除项 |

**Notes:** 每个被归档版本仍必须保存完整 Markdown、Typst、PDF 三件套；整批预检失败时 current 完全不变。

---

## Claude's Discretion

- 定向评分权重、稳定同分排序和模块相关性词表。
- CLI 参数拼写、metadata 字段名、隐藏 JSON schema 与终端表格样式。
- history 序号目录内多个更新版本的具体组织形式。

## Deferred Ideas

- Phase 49 的跨 runtime、聚合故障注入、PDF 深度验证与跨环境字体 UAT。
