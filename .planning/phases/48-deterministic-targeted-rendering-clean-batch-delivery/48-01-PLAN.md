---
phase: 48-deterministic-targeted-rendering-clean-batch-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/graduate-resume/templates/targeting-policy.json
  - skills/graduate-resume/scripts/graduate_resume_targeting.py
  - skills/graduate-resume/scripts/test_targeting_contract.py
  - skills/graduate-resume/fixtures/targeting/multi-state-targets.md
autonomous: true
requirements:
  - TARGET-01
  - TARGET-02
  - TARGET-03
must_haves:
  truths:
    - "D-01/D-03：每个通用版或目标版都由稳定事实 ID 组成不可变投影；核心事实始终保留，非核心事实只能按离线相关性与冻结页数预算选择、排序、强调或省略。"
    - "D-04：retain/exclude/pin 覆盖只作用于一个派生版本并冻结进 metadata；排除核心事实、未知 ID 或互相冲突的覆盖稳定失败，canonical 与 target brief 字节不变。"
    - "D-05/D-07：硬条件仅由受控谓词和已核实证据得出 meets/gap/unknown/not-applicable；相似词不能证明满足，unknown 默认允许但保留警告。"
    - "D-06/D-08：每个 gap 目标单独记录本次 allow；完整逐事实 trace 与四态矩阵只进入隐藏 evidence，公开投影只含摘要、digest 和 allow 状态。"
  artifacts:
    - path: skills/graduate-resume/templates/targeting-policy.json
      provides: "带版本、精确字段集和 SHA-256 绑定的离线定向策略"
      contains: "policy_version"
    - path: skills/graduate-resume/scripts/graduate_resume_targeting.py
      provides: "VersionProjection、FactDecision、ConditionResult、OverrideSnapshot、PageBudgetRequest/LayoutFeedback 与确定性 resolver"
      exports: ["resolve_version_projection", "evaluate_hard_conditions", "load_targeting_policy"]
    - path: skills/graduate-resume/scripts/test_targeting_contract.py
      provides: "稳定排序、四态、覆盖冲突、gap allow 与不可变性回归"
  key_links:
    - from: skills/graduate-resume/scripts/graduate_resume_targeting.py
      to: skills/graduate-resume/templates/targeting-policy.json
      via: "严格字段加载、policy_version 与原始 bytes SHA-256"
      pattern: "targeting-policy\\.json|sha256"
    - from: skills/graduate-resume/scripts/graduate_resume_targeting.py
      to: "graduate-resume/v2 verified facts"
      via: "稳定 ID resolver，不修改输入 dict"
      pattern: "fact_id|selected_fact_ids"
---

<objective>
建立 Phase 48 的确定性目标解析与四态条件契约。

Purpose: 先冻结“哪些已核实事实进入哪个版本、为什么、硬条件处于什么状态”，使后续布局、Markdown 和交付只消费可验证投影，不再各自推断目标语义。
Output: 版本化离线策略、不可变目标投影模块、代表性 fixture 与窄回归测试。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-CONTEXT.md
@.planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-RESEARCH.md
@.planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-PATTERNS.md

<interfaces>
现有 `graduate_resume_cli.validate_document(document) -> dict[str, Any]` 在进入目标解析前完成 `graduate-resume/v2` 校验；`graduate_resume_layout.build_frozen_resume_plan(...)` 当前消费完整 facts，48-02 会把它收紧为本计划输出的 version projection。

新模块必须提供冻结、JSON-safe 的 `VersionProjection`、`FactDecision`、`ConditionResult`、`OverrideSnapshot`、`PageBudgetRequest` 与 `LayoutFeedback`。`resolve_version_projection(...)` 返回三主题共享的 selected ordered facts、逐事实动作/理由、预算反馈摘要/digest、condition summary/digest、override snapshot、target source/as_of 与 policy hash；48-02 将实现真实 Phase 47 布局反馈 adapter，本计划先用纯 fake callback 锁定算法。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 冻结目标策略与不可变版本投影</name>
  <files>skills/graduate-resume/templates/targeting-policy.json, skills/graduate-resume/scripts/graduate_resume_targeting.py, skills/graduate-resume/scripts/test_targeting_contract.py</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_layout.py（冻结 dataclass、validate、to_projection 与稳定容器排序 analog）
    - skills/graduate-resume/scripts/graduate_resume_cli.py（CliError、validated facts 与 target 结构）
    - skills/graduate-resume/templates/layout-measurement.json（版本化小型 JSON 配置与 bytes hash analog）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-CONTEXT.md（D-01..D-05）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-RESEARCH.md（Pattern 1、safe component、strict hard-condition fallback）
  </read_first>
  <behavior>
    - 同一 canonical facts、target、policy 与 overrides 重复解析得到完全相同的 JSON projection 和 digest，正文顺序变化不改变同分稳定顺序。
    - `candidate/profile` 与 `education` 永远 selected；非核心事实依据受控模块顺序、整数 score、稳定事实 ID tie-break 生成 select/sort/emphasize/exclude/pin trace。
    - `auto`（最多 2 页）、强制 1 页或强制 2 页预算通过同一个三主题 feedback callback 评估共享 selected ID 集合；任一主题溢出时每轮只移除一个最低优先级可省略事实，三主题永远得到相同 selected IDs。
    - core、retain、pin 均不可因预算移除；只剩不可移除事实仍溢出时返回 `PAGE_BUDGET_UNSATISFIABLE`，不得缩字、截断、产生第 3 页或为某主题单独删内容。
    - 未登记字段、未知事实 ID、核心事实 exclude、retain+exclude 或 pin+exclude 冲突返回稳定 CliError，且输入 canonical/target 对象序列化前后相同。
  </behavior>
  <action>先写失败测试，再创建 `targeting-policy.json`，固定 `policy_version`、整数权重、受控模块顺序、exact-term expansions 与 predicate registry 的精确字段集；predicate registry 按已解决研究结论只允许 `education_level`、`major_exact`、`certificate_exact`、`fresh_graduate_status`，其他条件必须落为 unknown。加载器读取原始 bytes、拒绝额外/缺失字段并冻结 SHA-256。实现 frozen dataclass `FactDecision`、`OverrideSnapshot`、`PageBudgetRequest(requested_pages=auto|1|2)`、`LayoutFeedback(theme_key, fits, page_count, overflow_container_ids)`、`VersionProjection` 及 JSON-safe validate/project 方法。`resolve_version_projection()` 先按 D-01/D-03 生成 core + 稳定排序非核心候选，再把同一 selected ID tuple 交给注入的三主题 layout feedback callback；若任一主题不 fit，则确定性移除排序最末的非 core/retain/pin 事实并重试，直至三主题都 fit，或以 `PAGE_BUDGET_UNSATISFIABLE` 失败。最终 projection 冻结 requested pages、三主题 page-count/fit 摘要、selected/elided IDs 与 feedback digest，禁止 theme-specific selected IDs。按 D-04 应用 retain/exclude/pin，禁止修改 `document.data` 或策略文件；tie-break 只用稳定事实 ID，不使用正文顺序、当前时间、随机数、网络或模糊模型。</action>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_targeting_contract.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 源码导出冻结 `VersionProjection`、`FactDecision`、`OverrideSnapshot` 和 `resolve_version_projection`，projection 中可检查 policy version/hash、ordered selected IDs、trace 与 overrides。
    - 测试用 fake conservative/modern/expressive feedback 断言超量 fixture 每轮只移除一个最低优先级可省略事实，三主题 selected IDs 完全相同，core/retain/pin 始终保留；不可满足时精确报 `PAGE_BUDGET_UNSATISFIABLE`。
    - 测试分别覆盖 auto/1/2 页、正文重排和重复运行，断言 feedback 调用序列、最终 projection bytes/digest 相同，且不存在第 3 页或 theme-specific 内容分支。
    - 测试断言核心事实不可排除、同分按稳定 ID、canonical/target 输入不变、冲突错误码稳定。
    - `targeting-policy.json` 不含具体公司、岗位、用户 override、当前时间或外部 URL。
  </acceptance_criteria>
  <done>确定性事实投影可独立生成和自校验，D-01/D-03/D-04 的选择边界与追溯信息由一个模块统一拥有。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现严格四态条件与逐目标缺口放行</name>
  <files>skills/graduate-resume/scripts/graduate_resume_targeting.py, skills/graduate-resume/scripts/test_targeting_contract.py, skills/graduate-resume/fixtures/targeting/multi-state-targets.md</files>
  <read_first>
    - skills/graduate-resume/references/schema-and-review-contract.md（target source/as_of/confirmed 与稳定 ID 事实契约）
    - skills/graduate-resume/fixtures/valid-multi-target.md（多目标 fixture analog）
    - skills/graduate-resume/scripts/graduate_resume_cli.py（target normalization 与稳定 JSON 错误边界）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-CONTEXT.md（D-05..D-08）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-RESEARCH.md（Pattern 2、Security Domain false qualification claim）
  </read_first>
  <behavior>
    - 受控谓词有明确 verified evidence 时分别产生 meets 或 gap；缺字段、只能靠相似关键词或无 predicate 时一律 unknown。
    - 受控谓词范围精确为 education_level、major_exact、certificate_exact、fresh_graduate_status；年限、地域、年龄和技能熟练度稳定为 unknown。
    - not-applicable 只接受 CLI 已解析的 `(target_id, condition_id, reason)` 本次显式 override；reason trim 后 1..200 Unicode code points 且无控制字符，不能由词表自动产生，也不能回写 target brief。
    - gap 目标未出现在本次 allow target ID 集合时阻断；unknown 不阻断但投影保留 warning；每个目标的 allow 状态互不继承。
  </behavior>
  <action>在同一不可变模型中实现 `ConditionResult` 与 `evaluate_hard_conditions()`，状态字面值固定为 `meets/gap/unknown/not-applicable`。按已解决研究结论，registry 只解析 education_level、major_exact、certificate_exact、fresh_graduate_status 并只读取已核实结构化字段；任何其他条件、缺字段或相似关键词均为 unknown。not-applicable 只接受 CLI 传入的本次 `(target_id, condition_id, reason)` override：ID 必须精确匹配目标与条件，reason trim 后 1..200 Unicode code points 且拒绝 NUL/CR/LF/控制字符；重复 pair 即使理由相同也稳定失败，不能回写 target brief。为 D-06 实现 `allowed_gap_target_ids` 的逐目标 gate，不得提供持久化或全局 allow-all；为 D-07 保留 unknown warning；为 D-08 生成完整隐藏 matrix、稳定 evidence digest、公开四态 counts/summary 与 `gap_allowed`。新增字面 fixture 覆盖四个谓词、未支持条件、generic、多 target、重复/未知 override，招聘来源/日期只来自用户输入。</action>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_targeting_contract.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 测试逐一断言 meets、gap、unknown、not-applicable 的来源规则，并证明“相似词但无明确证据”得到 unknown。
    - 测试断言四个允许 predicate 之外的年限/地域/年龄/技能条件均为 unknown；duplicate pair、未知 target/condition、空白/超长/控制字符 reason 返回稳定错误。
    - 未放行 gap 返回非零稳定错误；只放行 target A 不会放行 target B；unknown 无需 allow 仍可生成 projection 且带 warning。
    - 完整 matrix 包含 condition ID、state、evidence IDs/reason，公开 summary 不含候选人正文、联系方式、绝对路径或完整理由。
  </acceptance_criteria>
  <done>D-05..D-08 的四态判定、逐目标 gap allow 与隐藏证据边界可由测试确定证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate Markdown/YAML → target resolver | 候选人可控文本、稳定 ID、target requirements 与 override 参数进入策略判定。 |
| targeting-policy.json → projection | 版本化本地策略决定排序和受控谓词，策略篡改会改变正式版本语义。 |
| full trace/matrix → public summary | 完整事实理由和证据 ID 从隐藏 evidence 投影为公开 metadata 摘要。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Spoofing | `evaluate_hard_conditions` | mitigate | 仅受控 predicate + verified evidence 可产生 meets/gap；无证据或相似词回退 unknown，逐状态单测。 |
| T-48-02 | Tampering | policy/override/projection | mitigate | 精确字段集、原始 bytes SHA-256、frozen dataclass、自校验、核心事实与冲突 override fail closed。 |
| T-48-03 | Information Disclosure | trace/matrix projection | mitigate | 完整理由仅写 owned hidden evidence；公开 projection 只含 counts/digest/allow，测试拒绝联系方式、路径和正文泄露。 |
| T-48-04 | Repudiation | per-target gap allow | mitigate | allow 绑定本次 target ID 与 projection metadata，不提供全局开关，测试跨目标不继承。 |
| T-48-SC | Tampering | package supply chain | accept | 本计划仅使用 Python stdlib 与仓库已有 PyYAML，不新增 npm/pip/cargo 安装；无 package mutation。 |
</threat_model>

<verification>
运行 `python3 -m unittest skills/graduate-resume/scripts/test_targeting_contract.py -v`；确认无网络访问、无 canonical 写入、所有 projection/digest 重跑一致，四态与 gap gate 负例均为稳定非零错误。
</verification>

<success_criteria>
- 同一已核实资料可确定生成 generic 或指定 target 的不可变事实投影，且每个事实动作可追溯。
- 四态条件严格区分满足、缺口、未知、不适用，缺口按目标一次性放行且不制造资格暗示。
- policy、target 来源/日期、overrides 与 evidence 摘要具备版本/hash 绑定，完整敏感理由留在隐藏 evidence。
</success_criteria>

<output>
Create `.planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-01-SUMMARY.md` when done.
</output>
