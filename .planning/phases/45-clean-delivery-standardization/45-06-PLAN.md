---
phase: 45-clean-delivery-standardization
plan: 06
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - skills/gongwen/scripts/gongwen_lib/delivery.sh
  - skills/gongwen/scripts/gongwen_lib/commands.sh
  - skills/gongwen/scripts/gongwen.sh
  - skills/gongwen/tests/test_clean_delivery.sh
  - skills/gongwen/SKILL.md
  - skills/gongwen/references/format-and-rendering.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, REV-01, REV-02, REV-03, REV-04, SAFE-01, VERIFY-01, DOCS-01, RUNTIME-01]
must_haves:
  truths:
    - "gongwen 保持 Bash-only Markdown→Typst 转换，不引入 Python/Node renderer 或仓库根运行时依赖。"
    - "`render --input --typ [--pdf] [--expected-typ]` 先在 candidate 完成 expected/PDF gate，再发布 md+typ[/pdf] exact set。"
    - "所有 shell cleanup 只使用 validated quoted fixed-name arrays 和本 run 记录，绝不 glob/delete 用户根。"
  artifacts:
    - path: "skills/gongwen/scripts/gongwen_lib/delivery.sh"
      provides: "Bash-only bundle lock/history/rollback adapter"
    - path: "skills/gongwen/tests/test_clean_delivery.sh"
      provides: "真实 shell public CLI transaction matrix"
  key_links:
    - from: "skills/gongwen/scripts/gongwen_lib/commands.sh"
      to: "skills/gongwen/scripts/gongwen_lib/delivery.sh"
      via: "candidate md/typ[/pdf] explicit names"
      pattern: "render_markdown_to_typst|publish"
---
<objective>
在不改变 gongwen Bash-only 转换栈的前提下增加 candidate、history、no-op 和 rollback。

Purpose: 消除 expected mismatch/PDF compile 发生在 final Typst 已写之后的污染路径。
Output: shell transaction library、public command 接线、skill-local tests 和契约说明。
</objective>
<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/45-clean-delivery-standardization/45-01-SUMMARY.md
@.planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
@docs/clean-delivery-directory-contract.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 Bash-only explicit-set transaction</name>
  <files>skills/gongwen/scripts/gongwen_lib/delivery.sh, skills/gongwen/tests/test_clean_delivery.sh</files>
  <read_first>
    - skills/gongwen/scripts/gongwen_lib/common.sh
    - skills/gongwen/scripts/gongwen_lib/commands.sh
    - skills/gongwen/scripts/gongwen_lib/body.sh
    - skills/gongwen/tests/test_heading_normalization.sh
    - docs/clean-delivery-directory-contract.md
  </read_first>
  <behavior>
    - first/change/same 对 md+typ 与 md+typ+pdf exact set 正确，optional PDF 增减归档旧 whole bundle。
    - 001/003 后 next=004；七 fault、INT/TERM、lock conflict 后 current/history/sources 不变。
    - symlink/traversal/cross-root/different-stem/unknown/legacy media 在 mutation 前失败。
  </behavior>
  <action>新增 `delivery.sh`，以 `--typ` 的 validated dirname/stem 为 authority，`--pdf` 若存在必须同 root/stem；使用 `set -euo pipefail`、exclusive `.work/lock`、owned run candidate/rollback/evidence、`cmp -s`（不可用时回退既有 same_file_shell）、exact fixed-name arrays、max+1 history 和 trap rollback。用 `test -L`/`lstat` 等价检查拒绝 symlink，核验 root/work/history 同 device；mutation 不消费 `find`/glob 输出，不执行 `rm "$root"/*`，只清记录的本 run 名。测试使用 deterministic Typst fixture/fake 时必须产生 `%PDF-` 可读最小文件，不得 dynamic skip 缺失依赖。</action>
  <verify><automated>bash skills/gongwen/tests/test_clean_delivery.sh</automated></verify>
  <acceptance_criteria>
    - shell test 覆盖所有标准 fault、history gap、optional-set mismatch、concurrency 和 signal rollback。
    - 静态 scan 不存在 root wildcard rm/find-delete，trap 不清 unrelated stale work。
    - `delivery.sh` 不执行 Python/Node，也不 source sibling skill/root helper。
  </acceptance_criteria>
  <done>Bash transaction 独立证明 first/change/no-op/failure/unknown/lock invariants。</done>
</task>

<task type="auto">
  <name>Task 2: 将 render/expected/PDF 与文档接入安全交付</name>
  <files>skills/gongwen/scripts/gongwen_lib/commands.sh, skills/gongwen/scripts/gongwen.sh, skills/gongwen/SKILL.md, skills/gongwen/references/format-and-rendering.md</files>
  <read_first>
    - skills/gongwen/scripts/gongwen_lib/commands.sh
    - skills/gongwen/scripts/gongwen.sh
    - skills/gongwen/scripts/gongwen_lib/body.sh
    - skills/gongwen/SKILL.md
    - skills/gongwen/references/format-and-rendering.md
    - skills/gongwen/scripts/gongwen_lib/delivery.sh
  </read_first>
  <action>dispatcher 仅 source 新 library；`cmd_render` 把 reviewed Markdown copy、Typst、optional PDF 全部生成到 candidate，expected compare 先于 publish，PDF 必须从 candidate Typst 编译并验证 regular/non-empty/`%PDF-`。保持 `example` 为显式 authoring output，不自动触发 history；保持 public flags/help。文档精确声明 2/3 文件 exact sets、same-root/stem、support dirs、candidate/no-op/history/rollback、unknown confirmation、shell-only 栈；六 runtime notes 覆盖 whole-folder、explicit gongwen.sh fallback、Bash/Typst/cmp 依赖、execute/read/write、sandbox/allowlist、delivery/.work，OpenClaw/Hermes installation-time verified。</action>
  <verify><automated>bash -n skills/gongwen/scripts/gongwen.sh skills/gongwen/scripts/gongwen_lib/*.sh &amp;&amp; bash skills/gongwen/tests/test_heading_normalization.sh &amp;&amp; python3 test/clean-delivery/verify_clean_delivery.py --skill gongwen</automated></verify>
  <acceptance_criteria>
    - expected mismatch/Typst missing/compile failure 不改变 prior current/history。
    - normal root 仅 md+typ[/pdf] 和允许支持目录，无 diff/log/staging。
    - canonical/docs 与实际 optional PDF、Bash-only 和六 runtime 边界一致。
  </acceptance_criteria>
  <done>gongwen public render 安全发布且转换能力无回退。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI path strings → shell filesystem ops | shell expansion、traversal、跨根和 symlink 风险 |
| Typst subprocess → candidate | tool failure/partial PDF 不得进入 current |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06-01 | Elevation of Privilege | shell path handling | mitigate | quoted arrays、component validation、same-root/stem、no symlink |
| T-45-06-02 | Tampering | cleanup trap | mitigate | owned-name registry、无 glob/find-delete |
| T-45-06-03 | Denial of Service | concurrent publish | mitigate | exclusive same-root lock and bounded conflict exit |
| T-45-06-SC | Tampering | package installs | accept | 不新增包管理器依赖 |
</threat_model>
<verification>
- `bash skills/gongwen/tests/test_clean_delivery.sh`
- `python3 test/clean-delivery/verify_clean_delivery.py --skill gongwen`
</verification>
<success_criteria>
- shell-only renderer preserved；expected/PDF gates pre-publication。
- 2/3 exact bundle history/no-op/rollback 全覆盖。
- 无 broad shell cleanup 或跨 skill runtime dependency。</success_criteria>
<output>Create `.planning/phases/45-clean-delivery-standardization/45-06-SUMMARY.md` when done.</output>
