---
phase: 45-clean-delivery-standardization
plan: 04
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - skills/school-pptx/scripts/pptx_render.py
  - skills/school-pptx/scripts/verify_markdown_contract.py
  - skills/school-pptx/scripts/verify_pptx_renderer.py
  - skills/school-pptx/SKILL.md
  - skills/school-pptx/references/renderer-and-pagination.md
  - skills/school-pptx/references/verification-contract.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, REV-01, REV-02, REV-03, REV-04, SAFE-01, VERIFY-01, DOCS-01, RUNTIME-01]
must_haves:
  truths:
    - "validated Markdown+PPTX pair 通过同根锁、exact equality、whole-pair history 和 handled-failure rollback 发布。"
    - "parser/plan/runtime 非零的 structurally valid best-effort deck 只作 work evidence，绝不成为 current。"
    - "PowerPoint/WPS 人工 UAT 仍为人工边界；目录层不签署视觉通过，也不承诺跨文件硬崩溃原子。"
  artifacts:
    - path: "skills/school-pptx/scripts/pptx_render.py"
      provides: "held-descriptor pair transaction with history/rollback"
    - path: "skills/school-pptx/scripts/verify_pptx_renderer.py"
      provides: "pair fault/no-op/history/unknown regression"
  key_links:
    - from: "skills/school-pptx/scripts/pptx_render.py"
      to: "skills/school-pptx/scripts/pptx_emit.py"
      via: "existing staged package gate before current publication"
      pattern: "validate_staged_package"
---
<objective>
将现有最强 secure pair publisher 扩展为有 history、no-op 和 rollback 的完整交付协议，并阻止失败 best-effort pair 伪装 current。

Purpose: 关闭两次 replace 中间混版窗口，同时保持 editable PPTX renderer 与人工 UAT 边界。
Output: pair transaction、标准 fault 验证、canonical artifact/runtime 更新。
</objective>
<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/45-clean-delivery-standardization/45-01-SUMMARY.md
@.planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
@docs/clean-delivery-directory-contract.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 扩展 held-descriptor pair transaction 并隔离 best-effort</name>
  <files>skills/school-pptx/scripts/pptx_render.py</files>
  <read_first>
    - skills/school-pptx/scripts/pptx_render.py
    - skills/school-pptx/scripts/pptx_emit.py
    - skills/school-pptx/scripts/markdown_contract.py
    - docs/clean-delivery-directory-contract.md
    - .planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
  </read_first>
  <behavior>
    - first/changed/identical pair 分别表现为无 history、旧 pair 进 next history、current inode/mtime 不变。
    - 七 fault 与 INT/TERM 在任一 replace 后恢复旧 pair 并删除本次 history/run。
    - parser/plan/runtime error 返回非零，root current/history 不变，best-effort 仅在 owned evidence 生命周期内存在。
  </behavior>
  <action>保留 root O_DIRECTORY/O_NOFOLLOW、inode identity、owned O_EXCL temp、descriptor-relative replace/fsync 和 `validate_staged_package()`；把 destination 扩为 `.work/&lt;run-id&gt;/{candidate,rollback,evidence}`、exclusive lock、root unknown gate、exact pair bytes equality、max+1 history、verified rollback snapshot 和 completed-name restore。`render --input --out-dir [--stem]` candidate 完整且 parser/plan/runtime diagnostics 为零才允许 publish；异常 editable deck 可以生成供 bounded diagnostic，但 cleanup 前不得 replace current。legacy `media/`、unknown、duplicate stem、symlink 和 unsafe stem fail closed，不在普通 render 静默改写 Markdown；confirmed media→assets 迁移留给 SAFE-02 流程。不要声称 SIGKILL/power-loss 或多路径原子。</action>
  <verify><automated>python3 skills/school-pptx/scripts/verify_pptx_renderer.py --self-test delivery-transaction</automated></verify>
  <acceptance_criteria>
    - 原 between-replace hook 被标准七 fault 覆盖，after_publish_file_1 后旧 pair hash 可恢复且无 mixed pair。
    - no-op 不触碰 current；001/003 后 next=004；pre-existing history hash 不变。
    - root 仅 pair 与允许支持目录，无 best-effort/manifest/status/log/staging 泄漏。
  </acceptance_criteria>
  <done>Markdown+PPTX pair 对所有 handled failure 保持上一套成功版本。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 pair 黑盒矩阵并同步 artifact/runtime 边界</name>
  <files>skills/school-pptx/scripts/verify_markdown_contract.py, skills/school-pptx/scripts/verify_pptx_renderer.py, skills/school-pptx/SKILL.md, skills/school-pptx/references/renderer-and-pagination.md, skills/school-pptx/references/verification-contract.md</files>
  <read_first>
    - skills/school-pptx/scripts/verify_markdown_contract.py
    - skills/school-pptx/scripts/verify_pptx_renderer.py
    - skills/school-pptx/SKILL.md
    - skills/school-pptx/references/renderer-and-pagination.md
    - skills/school-pptx/references/verification-contract.md
    - skills/school-pptx/references/visual-uat.md
    - skills/school-pptx/scripts/pptx_render.py
  </read_first>
  <action>在现有固定 registry 中加入真实 public render 的 first/change/same、generation/validation failure、七 fault、history gap、unknown/symlink/traversal、lock、work cleanup、sources/assets hash 断言，并保留原 21-gate renderer authority。文档声明 current exact pair、支持目录生命周期、verify caller workdir 非 delivery root、best-effort evidence、no-op/history/rollback、media 确认迁移和真实 viewer UAT；六 runtime notes 保持 whole-folder、显式 shell fallback、Python packages、execute/read/write、sandbox/allowlist、delivery/.work，OpenClaw/Hermes installation-time verified。不得把自动目录验证写成 PowerPoint/WPS PASS。</action>
  <verify><automated>python3 skills/school-pptx/scripts/verify_pptx_renderer.py &amp;&amp; python3 test/clean-delivery/verify_clean_delivery.py --skill school-pptx</automated></verify>
  <acceptance_criteria>
    - 原 renderer/package validation 与新 publication gates 均 required==called、零 dynamic skip。
    - 文档准确区分 current delivery、work evidence、verify workdir、人工 UAT。
    - whole-folder isolated copy 能执行 help/轻量 self-test，无 sibling/root runtime import。
  </acceptance_criteria>
  <done>school-pptx 行为、证据与六 runtime 契约全部对齐。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| media/Markdown → PPTX ZIP | hostile paths/invalid media 进入 renderer |
| staged pair → root/history | 两次单路径 replace 可能中断 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | Tampering | root/media paths | mitigate | held fd、no-follow、bounded reads、unknown gate |
| T-45-04-02 | Denial of Service | between-replace fault | mitigate | verified rollback snapshot + completed-name restore |
| T-45-04-03 | Repudiation | best-effort false current | mitigate | diagnostics non-zero blocks publish; evidence-only cleanup |
| T-45-04-SC | Tampering | package installs | accept | 不新增包管理器依赖 |
</threat_model>
<verification>
- `python3 skills/school-pptx/scripts/verify_pptx_renderer.py`
- `python3 test/clean-delivery/verify_clean_delivery.py --skill school-pptx`
</verification>
<success_criteria>
- pair history/no-op/rollback 在真实 render 上通过。
- best-effort 非零结果不再成为 current。
- 人工 UAT 与硬崩溃边界陈述真实。</success_criteria>
<output>Create `.planning/phases/45-clean-delivery-standardization/45-04-SUMMARY.md` when done.</output>
