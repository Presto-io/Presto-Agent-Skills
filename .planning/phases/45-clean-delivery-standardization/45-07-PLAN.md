---
phase: 45-clean-delivery-standardization
plan: 07
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - skills/teaching-design-package/scripts/delivery-transaction.js
  - skills/teaching-design-package/scripts/test-delivery-transaction.js
  - skills/teaching-design-package/scripts/package-model.js
  - skills/teaching-design-package/scripts/teaching-design-package.sh
  - skills/teaching-design-package/SKILL.md
  - skills/teaching-design-package/references/format-and-orchestration.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, REV-01, REV-02, REV-03, REV-04, SAFE-01, VERIFY-01, DOCS-01, RUNTIME-01]
must_haves:
  truths:
    - "`render-package --pdf --input --out-dir` 不再在 candidate 前或 failure path 清空 public root。"
    - "model-derived `expected_public_filenames` 是唯一 dynamic 1+1+N mutation authority；course-name 变化可归档一个完整旧 group，多 group ambiguity fail closed。"
    - "`.teaching-design-package/` 被 `.work/<run-id>` 取代；model/status/debug/merge plan 仅 evidence，不是 current。"
  artifacts:
    - path: "skills/teaching-design-package/scripts/delivery-transaction.js"
      provides: "Node dynamic bundle transaction"
    - path: "skills/teaching-design-package/scripts/package-model.js"
      provides: "run-relative evidence paths and explicit public filename authority"
    - path: "skills/teaching-design-package/scripts/teaching-design-package.sh"
      provides: "candidate orchestration without destructive cleanup"
  key_links:
    - from: "skills/teaching-design-package/scripts/package-model.js"
      to: "skills/teaching-design-package/scripts/delivery-transaction.js"
      via: "public_delivery.expected_public_filenames"
      pattern: "expected_public_filenames"
---
<objective>
把 teaching-design-package 的 dynamic 1+1+N staging 改造成 `.work` candidate 与可回滚 whole-bundle 发布。

Purpose: 移除本阶段最危险的生成前/失败时 public-root 清空和逐文件 copy 污染路径。
Output: Node transaction、run-relative model/orchestration、真实模块/merge regression 与契约更新。
</objective>
<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/45-clean-delivery-standardization/45-01-SUMMARY.md
@.planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
@docs/clean-delivery-directory-contract.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 model-driven dynamic bundle transaction</name>
  <files>skills/teaching-design-package/scripts/delivery-transaction.js, skills/teaching-design-package/scripts/test-delivery-transaction.js</files>
  <read_first>
    - skills/teaching-design-package/scripts/package-model.js
    - skills/teaching-design-package/scripts/teaching-design-package.sh
    - skills/school-pptx/scripts/pptx_render.py
    - docs/clean-delivery-directory-contract.md
    - .planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
  </read_first>
  <behavior>
    - exact model 1+1+N first/change/same 正确，course prefix 变化归档唯一完整旧 group。
    - multiple/partial old groups、unknown/hidden/media/symlink/traversal fail closed；001/003 后 next=004。
    - 七 fault、module/merge failure、INT/TERM、lock conflict 后 old current/history/sources hash 不变。
  </behavior>
  <action>创建 self-contained Node publisher，只消费 validated `expected_public_filenames` 和 candidate root；使用 `lstatSync`、安全 relative components、held dev/ino identity、同根 `.work` lock、candidate/rollback/evidence、explicit copy/rename/fsync、path-set+bytes equality、max+1 history 和 completed-name rollback。current discovery 可只读识别零套或恰好一个结构完整的旧 `1+1+N` group；不同 course prefix 是 changed，多个 prefix/partial group/legacy hidden/unknown 均 bounded fail closed，不能按 mtime 选组。缺少 no-follow/安全替换能力时 runtime capability error fail closed。禁止 `rmSync(root,{recursive:true})`、glob、find-delete 或 readdir-derived mutation list。</action>
  <verify><automated>node skills/teaching-design-package/scripts/test-delivery-transaction.js</automated></verify>
  <acceptance_criteria>
    - test mutation guard 证明 publisher 不会硬编码 current group、Markdown-only equality 或 gap reuse。
    - publish_file_1/middle/post-verify faults 恢复 dynamic old group，移除本次 history/run。
    - unrelated stale work、sources、unknown user entries 不被清理或移动。
  </acceptance_criteria>
  <done>dynamic explicit-set transaction 独立通过完整 fault/unknown/history 矩阵。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 移除 destructive cleanup 并把 staging 映射到 owned run</name>
  <files>skills/teaching-design-package/scripts/package-model.js, skills/teaching-design-package/scripts/teaching-design-package.sh</files>
  <read_first>
    - skills/teaching-design-package/scripts/package-model.js
    - skills/teaching-design-package/scripts/teaching-design-package.sh
    - skills/teaching-design-package/scripts/teaching-plan-renderer.js
    - skills/teaching-design-package/scripts/teaching-design-renderer.js
    - skills/teaching-design-package/scripts/delivery-transaction.js
  </read_first>
  <behavior>
    - model/status/module Typst/PDF/merge plan/debug 均位于 `.work/<run-id>/evidence|candidate`，成功/失败不留 legacy hidden tree。
    - module registry、merge order、existing forced merge/missing/empty module faults 保持并在 publish 前生效。
    - `model`、`manifest`、non-PDF render/split commands 保留名称/flags，写显式 diagnostic workdir 而非 current root sidecar。
  </behavior>
  <action>把 `package-model.js` 的 work paths 改为 run-root 相对逻辑名，继续由 `safeCourseFilenamePrefix()` 与 `public_delivery.expected_public_filenames` 提供 authority。`teaching-design-package.sh` 创建 `.work/&lt;run-id&gt;` 后生成 model、modules、status、merge plan 和完整 public candidate；保留 registry 顺序、module compile/merge/reopen gates 与现有 failure env。彻底删除/断开 `cleanup_public_root()`、`record_failure()` 的 public deletion、candidate 前清空、逐文件 final copy 和 final-ready failure cleanup；完整 candidate 验证后一次调用 publisher。所有 diagnostics bounded，详细 evidence 在 rollback/finally 后清理。</action>
  <verify><automated>bash -n skills/teaching-design-package/scripts/teaching-design-package.sh &amp;&amp; node --check skills/teaching-design-package/scripts/package-model.js &amp;&amp; node --check skills/teaching-design-package/scripts/delivery-transaction.js &amp;&amp; python3 test/clean-delivery/verify_clean_delivery.py --skill teaching-design-package</automated></verify>
  <acceptance_criteria>
    - 静态 mutation guard 证明不存在 normal/failure public-root broad cleanup 和 direct sequential copy。
    - forced module/merge/final validation failure 均保持 old group/history，root 无 `.teaching-design-package`。
    - 原 1+1+N filenames、module registry/merge order 和公开 command/flags 无回退。
  </acceptance_criteria>
  <done>真实 render-package 只从完整 validated candidate 发布 dynamic bundle。</done>
</task>

<task type="auto">
  <name>Task 3: 同步 dynamic artifact 与六 runtime 契约</name>
  <files>skills/teaching-design-package/SKILL.md, skills/teaching-design-package/references/format-and-orchestration.md</files>
  <read_first>
    - skills/teaching-design-package/SKILL.md
    - skills/teaching-design-package/references/format-and-orchestration.md
    - skills/teaching-design-package/scripts/package-model.js
    - skills/teaching-design-package/scripts/teaching-design-package.sh
  </read_first>
  <action>记录 model-derived dynamic `1+1+N` exact current、diagnostic command/workdir、sources/assets/history/.work、candidate/module/merge/PDF gates、prefix change/ambiguity、no-op/history/rollback 和 legacy hidden confirmation；删除 `.teaching-design-package/` 成功根例外。六 runtime notes 覆盖 whole-folder、explicit shell fallback、Bash/Node/Typst/PDF merge 依赖、execute/read/write、sandbox/allowlist、delivery/.work；OpenClaw/Hermes 采用 installation-time verification。</action>
  <verify><automated>python3 test/clean-delivery/verify_clean_delivery.py --skill teaching-design-package --gate documentation_runtime_contract_gate</automated></verify>
  <acceptance_criteria>
    - 文档与 `expected_public_filenames`、public flags、evidence paths 一致。
    - 不把 model/status/manifest/debug 作为成功 current，也不声明自动治理 legacy/unknown。
    - 六 runtime 外部命令和权限/fallback 均明确。
  </acceptance_criteria>
  <done>TDP canonical 契约与 dynamic publisher 行为一致。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| course data/model → filenames | 动态 prefix/registry 可造成 traversal 或 ambiguous groups |
| shell module pipeline → Node publisher | partial modules/merge evidence 不得成为 current |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-07-01 | Tampering | dynamic filenames | mitigate | safe prefix + explicit model list + component validation |
| T-45-07-02 | Denial of Service | module/merge/publish faults | mitigate | candidate-first + registry gates + rollback matrix |
| T-45-07-03 | Elevation of Privilege | broad cleanup | mitigate | remove destructive functions; explicit mutation list only |
| T-45-07-SC | Tampering | package installs | accept | 不新增包管理器依赖 |
</threat_model>
<verification>
- `node skills/teaching-design-package/scripts/test-delivery-transaction.js`
- `python3 test/clean-delivery/verify_clean_delivery.py --skill teaching-design-package`
</verification>
<success_criteria>
- destructive cleanup 不再存在于 normal/failure path。
- dynamic group change/no-op/history/rollback/ambiguity 全覆盖。
- legacy hidden evidence 被 `.work` owned lifecycle 取代。</success_criteria>
<output>Create `.planning/phases/45-clean-delivery-standardization/45-07-SUMMARY.md` when done.</output>
