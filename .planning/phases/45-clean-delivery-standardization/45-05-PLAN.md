---
phase: 45-clean-delivery-standardization
plan: 05
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - skills/school-presentation/scripts/school_presentation/delivery.py
  - skills/school-presentation/scripts/school_presentation/test_delivery.py
  - skills/school-presentation/scripts/school_presentation/_engine.py
  - skills/school-presentation/SKILL.md
  - skills/school-presentation/references/verification-contract.md
  - skills/school-presentation/references/authoring-and-layout.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, REV-01, REV-02, REV-03, REV-04, SAFE-01, VERIFY-01, DOCS-01, RUNTIME-01]
must_haves:
  truths:
    - "normal render current 只包含 reviewed Markdown+offline HTML；manifest/verify evidence 不发布到 delivery root。"
    - "HTML size/offline/DOM/required asset gates 全部在 candidate 中完成，oversize 或 missing required asset 不覆盖 current。"
    - "legacy media 不静默迁移；确认后的 assets 引用在 history 中仍可解析，sources 永不被 normal render 修改。"
  artifacts:
    - path: "skills/school-presentation/scripts/school_presentation/delivery.py"
      provides: "Markdown+HTML transaction and asset reference gate"
    - path: "skills/school-presentation/scripts/school_presentation/_engine.py"
      provides: "in-memory render to candidate and evidence separation"
  key_links:
    - from: "skills/school-presentation/scripts/school_presentation/_engine.py"
      to: "skills/school-presentation/scripts/school_presentation/delivery.py"
      via: "rendered HTML + ephemeral manifest + reviewed Markdown"
      pattern: "cmd_render|manifest|max_size|publish"
---
<objective>
将学校演示 HTML 交付改为 Markdown+HTML candidate bundle，严格分离 manifest/verify evidence 与 current。

Purpose: 关闭先写 HTML/manifest 再 size fail 的污染路径，并落实 assets/history 引用边界。
Output: pair transaction、真实 render 矩阵、artifact/runtime 文档。
</objective>
<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/45-clean-delivery-standardization/45-01-SUMMARY.md
@.planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
@docs/clean-delivery-directory-contract.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 HTML pair transaction 与 persistent asset gate</name>
  <files>skills/school-presentation/scripts/school_presentation/delivery.py, skills/school-presentation/scripts/school_presentation/test_delivery.py</files>
  <read_first>
    - docs/clean-delivery-directory-contract.md
    - skills/school-presentation/scripts/school_presentation/_engine.py
    - skills/school-presentation/scripts/school_presentation/io.py
    - skills/school-presentation/scripts/school_presentation/html_assets.py
    - skills/school-pptx/scripts/pptx_render.py
  </read_first>
  <behavior>
    - first/change/same pair 与 history/no-op 规则正确，001/003 后 next=004。
    - oversize/offline token/missing required asset/七 fault/INT/TERM 后 current/history/sources 不变。
    - unknown、media、symlink、manifest-in-root、stale unrelated work 在 mutation 前保守处理。
  </behavior>
  <action>新增 self-contained `DeliverySession` 管理 stable Markdown+HTML 与显式 referenced `assets/`，使用同根 `.work` 锁、candidate/rollback/evidence、exact path-set+bytes、max+1 whole-bundle history、handled-failure restore。HTML gate 包含 UTF-8/non-empty、max bytes、offline single-file/required DOM tokens；解析 Markdown/HTML 中持续相对资源引用，只允许明确 managed `assets/`，归档时复制旧引用 assets 到 `history/&lt;seq&gt;/assets/` 并验证 archived reference。sources 不变；legacy `media/`、manifest target in delivery root、unknown/symlink 均 fail closed 并路由 confirmation cleanup。</action>
  <verify><automated>python3 -m unittest skills/school-presentation/scripts/school_presentation/test_delivery.py -v</automated></verify>
  <acceptance_criteria>
    - tests 证明 archived Markdown/HTML 的 assets 引用在 history 内可解析，unreferenced inputs 不被复制。
    - no-op 不 touch current；所有 fault 回滚无 mixed pair、新 history 消失。
    - helper 不 import sibling/root harness，不 broad-delete root 或 stale work。
  </acceptance_criteria>
  <done>HTML pair 与 referenced assets 的 transaction invariants 可独立验证。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 render/manifest/verify 接到 candidate 与显式 evidence root</name>
  <files>skills/school-presentation/scripts/school_presentation/_engine.py</files>
  <read_first>
    - skills/school-presentation/scripts/school_presentation/_engine.py
    - skills/school-presentation/scripts/school_presentation/verify.py
    - skills/school-presentation/scripts/school_presentation/pdf.py
    - skills/school-presentation/scripts/school_presentation/delivery.py
    - skills/school-presentation/scripts/school-presentation.sh
  </read_first>
  <behavior>
    - `render --input [--html] [--manifest] [--max-size-mb]` 保持参数，normal publication 仅 md+html。
    - manifest 仅允许 owned evidence/system temp/显式独立 verify workdir；`verify --workdir` 继续 caller-owned 且非 delivery。
    - bookmark-pdf 保持 postprocess helper，不因目录改造被误声明为自动最终 PDF。
  </behavior>
  <action>保持 in-memory `render_deck()` 与 deterministic verification；`cmd_render()` 先在 session candidate 写 reviewed Markdown/HTML/ephemeral manifest，完成 size/offline/DOM/resource gate 后仅发布 md+html。若 `--manifest` 指向 normal delivery root 或 root support dirs 外不安全路径，bounded 非零拒绝；允许显式独立 caller evidence path。required missing asset 必须 block publish，safe optional fallback 按现有 contract 保留。`cmd_verify()` 继续只写 `--workdir` 并新增 root pollution assertions；不把 verification-manifest.json 纳入 final set。</action>
  <verify><automated>skills/school-presentation/scripts/school-presentation.sh verify --workdir /tmp/phase45-school-presentation-verify &amp;&amp; python3 test/clean-delivery/verify_clean_delivery.py --skill school-presentation</automated></verify>
  <acceptance_criteria>
    - oversize HTML 与 manifest write failure 不改变 prior current/history。
    - public render/verify/bookmark flags 未删除，原 deterministic/interaction regression 通过。
    - delivery root 无 manifest、verification-manifest、截图、diff、日志或 failed HTML。
  </acceptance_criteria>
  <done>normal render 与 verification evidence 生命周期彻底分离。</done>
</task>

<task type="auto">
  <name>Task 3: 同步 HTML artifact、assets 与六 runtime 说明</name>
  <files>skills/school-presentation/SKILL.md, skills/school-presentation/references/verification-contract.md, skills/school-presentation/references/authoring-and-layout.md</files>
  <read_first>
    - skills/school-presentation/SKILL.md
    - skills/school-presentation/references/verification-contract.md
    - skills/school-presentation/references/authoring-and-layout.md
    - skills/school-presentation/references/playback-and-export.md
    - skills/school-presentation/scripts/school-presentation.sh
  </read_first>
  <action>精确记录 md+offline HTML current、manifest/verify/bookmark evidence 边界、sources/assets/history/.work 生命周期、confirmed media→assets 迁移、candidate gates、no-op/history/rollback 与人工浏览器/PDF export 边界。六 runtime notes 覆盖 whole-folder identity assets、explicit shell fallback、Python/browser/PDF external commands、execute/read/write、sandbox/allowlist、delivery/.work/verify workdir；OpenClaw/Hermes 只作 installation-time verified 声明。</action>
  <verify><automated>python3 test/clean-delivery/verify_clean_delivery.py --skill school-presentation --gate documentation_runtime_contract_gate</automated></verify>
  <acceptance_criteria>
    - 文档没有把 manifest 或 browser printed PDF 默认列入 normal current。
    - legacy media 明确走确认式迁移且同步验证引用，不静默移动。
    - 六 runtime 与显式 fallback/权限边界齐全。
  </acceptance_criteria>
  <done>HTML 技能文档与实际 pair/evidence 行为一致。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Markdown/media → offline HTML | 不可信资源引用和尺寸进入单文件输出 |
| manifest/workdir → delivery root | evidence path 可能污染 current |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-01 | Tampering | asset references | mitigate | authorized assets only、no traversal/symlink、archive reference recheck |
| T-45-05-02 | Denial of Service | oversized HTML | mitigate | candidate size gate before publish |
| T-45-05-03 | Information Disclosure | manifest placement | mitigate | explicit non-delivery evidence root guard |
| T-45-05-SC | Tampering | package installs | accept | 不新增包管理器依赖 |
</threat_model>
<verification>
- `python3 test/clean-delivery/verify_clean_delivery.py --skill school-presentation`
- `skills/school-presentation/scripts/school-presentation.sh verify --workdir /tmp/phase45-school-presentation-verify`
</verification>
<success_criteria>
- normal root 仅 current pair 与允许支持目录。
- oversize/resource/evidence failures 不污染 current/history。
- archived assets 引用有效且 sources 不变。</success_criteria>
<output>Create `.planning/phases/45-clean-delivery-standardization/45-05-SUMMARY.md` when done.</output>
