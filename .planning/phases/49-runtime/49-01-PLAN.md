---
phase: 49-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/graduate-resume/scripts/graduate_resume_pdf_gate.py
  - skills/graduate-resume/scripts/graduate_resume_verify.py
  - skills/graduate-resume/scripts/graduate_resume_cli.py
  - skills/graduate-resume/scripts/test_phase49_verify.py
  - skills/graduate-resume/references/phase-49-verification-contract.md
autonomous: false
requirements:
  - VERIFY-01
  - VERIFY-02
must_haves:
  truths:
    - "用户运行唯一公开入口 `graduate-resume.sh verify --workdir <caller-owned-dir>` 后，验证器会重开最终 Markdown、Typst、PDF 与逐页 PNG，并独立确认真实 A4、1/2 页、主题、照片模式、模块边界、正式命名和 target metadata（D-05、D-06）。"
    - "顶层聚合按固定顺序调用且只调用 14 个字面 gate，`required == called`、无重复、无动态跳过，并从原始 stdout/stderr、退出码、重开产物和 SHA-256 重算结论（D-01、D-02）。"
    - "固定验收覆盖正常照片、无照片、多目标、资格缺口、内容压力与隔离故障注入，证明 schema、定向、分页、发布、history/no-op 与 rollback 边界（D-03）。"
    - "Phase 48 的 6 个 Blocker、3 个 Warning 未全部处置，或 `48-SECURITY.md` 缺失/不通过/stale 时，`P49-G01-release-prerequisites` 必须让最终状态失败，既有 `48-VERIFICATION.md` PASS 不能掩盖债务（D-04）。"
    - "JSON、Markdown、PNG 与 raw logs 只写入调用方授权 verify workdir；正式 delivery root 仍只含 same-stem Markdown/Typst/PDF triples（D-07）。"
    - "每条验收链记录固定 fixture、Typst 0.15.0、受控字体清单及 hash、PDF/PNG hash 和运行环境身份（D-08）。"
    - "首次 public verify 在 caller-owned workdir 创建唯一 `runs/<active_run_id>/` 并原子写入根级 `active-run.json`；后续 continuation 必须以 `--resume <active_run_id>` 重开同一 run，不能静默创建新 run 或跨 run 消费证据（D-01、D-02、D-07）。"
  artifacts:
    - path: skills/graduate-resume/scripts/graduate_resume_pdf_gate.py
      provides: "独立重开 PDF/PNG 并交叉复算页面、字体、像素、照片和模块边界的 observer"
      exports: ["verify_rendered_layout"]
    - path: skills/graduate-resume/scripts/graduate_resume_verify.py
      provides: "固定 14-gate registry、原始证据模型、三态裁决与 caller-owned evidence transaction"
      contains: "P49-G14-evidence-mutation-guards"
    - path: skills/graduate-resume/scripts/test_phase49_verify.py
      provides: "固定 fixture、故障注入、required/called equality 与 producer mutation guards"
      contains: "PHASE49_GATE_ORDER"
    - path: skills/graduate-resume/references/phase-49-verification-contract.md
      provides: "公开 verify workdir、14-gate、证据 schema 与状态语义契约"
      contains: "P49-G01-release-prerequisites"
  key_links:
    - from: skills/graduate-resume/scripts/graduate_resume_cli.py
      to: skills/graduate-resume/scripts/graduate_resume_verify.py
      via: "`command_verify()` 将显式 workdir 交给固定聚合器并只输出其重算状态"
      pattern: "command_verify|run_phase49"
    - from: skills/graduate-resume/scripts/graduate_resume_verify.py
      to: skills/graduate-resume/scripts/graduate_resume_pdf_gate.py
      via: "G03/G04/G05/G07/G09/G10 重开固定 fixture 产物并消费 observer fields"
      pattern: "verify_rendered_layout"
    - from: skills/graduate-resume/scripts/graduate_resume_verify.py
      to: .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-REVIEW.md
      via: "G01 读取最新 review finding disposition 与对应修复复核证据"
      pattern: "P49-G01-release-prerequisites"
    - from: skills/graduate-resume/scripts/graduate_resume_verify.py
      to: caller-owned verify workdir
      via: "首次运行创建 `runs/<active_run_id>/` 与根级 `active-run.json`；后续 `--resume <active_run_id>` 只重开该 run"
      pattern: "active_run_id|active-run.json|--resume"
---

<objective>
建立 Phase 49 的 PDF/PNG 深度观察器和不可自证的固定发布聚合权威。

Purpose: 把 Phase 48 已完成的渲染与投递能力转化为可重复、可审计且不能被 producer 常量、动态 skip 或旧审查 PASS 欺骗的发布证据。
Output: 独立 artifact observer、固定 14-gate aggregator、公开 `verify --workdir`、故障注入与 mutation guard 回归、可审阅验收契约。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-runtime/49-CONTEXT.md
@.planning/phases/49-runtime/49-RESEARCH.md
@.planning/phases/49-runtime/49-PATTERNS.md
@.planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-REVIEW.md
@.planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-VERIFICATION.md

<interfaces>
执行顺序首先采用 `49-RESEARCH.md` 的 `Open Questions (RESOLVED)` checkpoint 契约：Phase 48 review/security 债务由独立 GSD 工作流关闭，本计划只验证其证据并在未齐时暂停，绝不把 `P49-G01` 的拒绝结果冒充修复或安全审计完成。

固定顶层 gate ID 与顺序为：`P49-G01-release-prerequisites`、`P49-G02-schema-baseline`、`P49-G03-normal-photo`、`P49-G04-no-photo`、`P49-G05-multi-target`、`P49-G06-qualification-gap`、`P49-G07-content-pressure`、`P49-G08-publication-faults`、`P49-G09-pdf-png-physical`、`P49-G10-triple-metadata-name`、`P49-G11-six-runtime-install`、`P49-G12-canonical-docs`、`P49-G13-human-uat`、`P49-G14-evidence-mutation-guards`。任何实现、reference 和测试必须逐字使用这一 tuple。

固定 fixture 来源为 `fixtures/layout/standard-photo.md`、`fixtures/valid-no-photo.md`、`fixtures/valid-multi-target.md`、`fixtures/targeting/multi-state-targets.md`、`fixtures/layout/pressure-two-pages.md`；故障注入只在每次 run 的 owned copy tree 内生成，不回写 canonical fixture。

唯一 active-run 数据契约：首次 `graduate-resume.sh verify --workdir <root>` 仅在 `<root>/active-run.json` 不存在时创建不可猜测、有界 `active_run_id`，原子写入 `{"active_run_id":"<id>"}`，并把 acceptance、raw、artifacts、runtime、uat 全部限定到 `<root>/runs/<active_run_id>/`。一旦 locator 存在，任何后续 public verify/49-02 continuation 都必须显式传入 `--resume <active_run_id>`；实现需同时复验 CLI 值、locator 值、run directory identity 与 run-local `acceptance.json.active_run_id` 精确相等。不得静默创建新 run、自动选择“latest”、回退旧 run 或跨 run 合并 evidence。

顶层状态只有 `passed | human_needed | failed`。本计划阶段 G11/G13 缺少合法外部记录时必须产生结构化失败证据；只有 49-02 完成后，G01-G12/G14 全通过且 G13 是唯一合法 PENDING 才允许 `human_needed`。
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: 确认 Phase 48 REVIEW 与 SECURITY 前置证据已由独立工作流关闭</name>
  <files>.planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-REVIEW.md, .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-SECURITY.md</files>
  <read_first>
    - .planning/phases/49-runtime/49-RESEARCH.md（source of truth：`Open Questions (RESOLVED)` 第 1 项阻塞 checkpoint 契约）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-REVIEW.md（待复核证据路径；当前 CR-01..CR-06、WR-01..WR-03 债务基线）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-VERIFICATION.md（analog/反例：功能 PASS 不能替代后续 review/security）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-08-SUMMARY.md（source of truth：Phase 48 最终实现文件与 source hash 复核范围）
    - .planning/phases/49-runtime/49-CONTEXT.md（D-04：review/security 是发布前置，不属于本阶段功能修复）
  </read_first>
  <what-built>尚未开始 Phase 49 实现；该 checkpoint 只确认 Phase 48 的独立 code-review fix 与 security audit 已产生可复算前置证据。</what-built>
  <action>先读取两个固定证据路径，不修改 Phase 48 产品代码，也不把当前 review 的 `issues_found` 或缺失 security 报告记录为“已关闭”。若 `48-REVIEW.md` 不是 hash-fresh `status: clean`，也不是带明确 `skip_reason`、reviewed source hashes 与批准依据的 `status: skipped`，暂停本计划并提示操作者先在 fresh GSD continuation 运行 `/gsd:code-review 48 --fix`；其后再运行 `/gsd:secure-phase 48` 生成 `48-SECURITY.md`。恢复时重新读取两个文件：REVIEW 必须 clean/skipped 且 CR-01..CR-06、WR-01..WR-03 没有 unresolved disposition；SECURITY 必须 `status: passed`/`PASS`、覆盖 Phase 48 修改文件与威胁，并列出与当前 checkout 逐一重算一致的 source SHA-256。任一报告缺失、stale、仍有 finding、非 PASS 或 hash 不一致时继续停在 checkpoint；通过后仍保留后续 `P49-G01` 机器复验，不能因本次人工确认跳过。具体执行已 RESOLVED checkpoint 契约与 D-04。</action>
  <how-to-verify>
    1. 检查 `48-REVIEW.md` 的状态为 clean 或有明确批准依据的 skipped，且 9 个原 finding 均无 unresolved。
    2. 检查 `48-SECURITY.md` 存在、结论为 PASS，并覆盖 Phase 48 source files、信任边界与 High 风险缓解。
    3. 对两个报告声明的 source paths 重新计算 SHA-256；全部与报告一致才恢复 49-01。
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-REVIEW.md &amp;&amp; test -f .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-SECURITY.md &amp;&amp; rg -n '^status: (clean|skipped)$' .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-REVIEW.md &amp;&amp; rg -ni '^status: (pass|passed)$' .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-SECURITY.md</automated>
    <human-check>逐项确认 review 的 9 个 finding disposition 与 review/security source SHA-256 均对应当前 checkout；未满足时运行指定独立 GSD 工作流后用 fresh continuation 恢复。</human-check>
  </verify>
  <acceptance_criteria>
    - REVIEW 固定路径存在且状态为 clean，或为带明确 skip_reason、批准依据与 hash-fresh source scope 的 skipped；CR-01..CR-06、WR-01..WR-03 均无 unresolved。
    - SECURITY 固定路径存在、状态 PASS、覆盖 Phase 48 修改文件/信任边界/High 风险，并且报告中的每个 source SHA-256 与当前 bytes 一致。
    - 缺失、stale、issues_found、未处置 finding 或非 PASS 时，49-01 没有执行任何实现任务；操作者收到 `/gsd:code-review 48 --fix`、随后 `/gsd:secure-phase 48` 与 fresh continuation 恢复指令。
    - 本 checkpoint 只证明外部关闭证据可用；最终 `P49-G01-release-prerequisites` 仍从 raw reports 机器复算，不能消费人工“approved”布尔值。
  </acceptance_criteria>
  <resume-signal>两个固定报告满足 clean/skipped + hash-fresh PASS 后输入“approved”；否则先完成指定 Phase 48 独立工作流。</resume-signal>
  <done>Phase 48 review/security 债务已由独立工作流关闭并由当前 hashes 复核，本计划才获准进入实现；拒绝门本身不计为债务关闭。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: 建立独立 PDF/PNG 重开观察与交叉重算</name>
  <files>skills/graduate-resume/scripts/graduate_resume_pdf_gate.py, skills/graduate-resume/scripts/graduate_resume_verify.py, skills/graduate-resume/scripts/test_phase49_verify.py</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_pdf_gate.py（被修改文件；复制现有 `verify_rendered_layout()` 的 PyMuPDF/Pillow 重开与 bounded failure 模式）
    - skills/graduate-resume/scripts/graduate_resume_final_markdown.py（source of truth：最终 Markdown schema、metadata 与重读投影）
    - skills/graduate-resume/scripts/graduate_resume_layout.py（source of truth：`FrozenResumePlan`、页数、模块容器和照片模式）
    - skills/graduate-resume/scripts/graduate_resume_render.py（source of truth：正式 stem、theme、generic/target 与 triple 生成规则）
    - skills/graduate-resume/scripts/test_render_contract.py（analog：真实 Typst 编译、三件套、照片与 clean publication 断言）
    - skills/school-pptx/scripts/verify_school_pptx.py（analog：producer/observer/arbiter 分离、artifact reopen、hash 与 mutation guards）
    - .planning/phases/49-runtime/49-CONTEXT.md（D-05..D-08）
  </read_first>
  <behavior>
    - PDF 每页尺寸从 `page.rect` 复算为 210mm × 297mm（使用锁定小容差），页数只能为 1 或 2；证据保存实际 pt/mm，不只保存布尔值。
    - 从最终 Markdown 重建 theme、photo mode、page count、container/page assignment、generic/target metadata 与正式 stem；再与 `.typ` markers、PDF 逐页文本/字体、每页 PNG 尺寸/像素/安全区独立交叉核对。
    - 照片版必须观察到 contain 槽位和预期重排；无照片版必须观察不到照片像素区域/装饰并验证正文重排；PNG 数必须严格等于 PDF 页数。
    - 受控字体 manifest bytes/hash、Typst snapshot version/hash、fixture source hash、Markdown/Typst/PDF/PNG hash 与环境信息进入同一 observer record；未知字体、缺页 PNG、hash 漂移或 metadata/stem 不一致均稳定失败。
  </behavior>
  <action>先在 `test_phase49_verify.py` 写真实编译与 copied-artifact mutation 用例，再扩展 `graduate_resume_pdf_gate.py`：将结构化 observer result 与裁决分开，新增对最终 Markdown/Typst/PDF/PNG 的单次有界 no-follow 读取，使用 PyMuPDF Page API 复算 A4、页数、逐页文本与字体，使用 Pillow 重开逐页 PNG 并复算尺寸、背景、安全区与照片 contain/absence；从 final Markdown 与 canonical naming functions 独立重建 theme、photo、container/page、generic/target stem 和 metadata，禁止读取 renderer 自报 `passed/count/hash` 作为事实。创建 `graduate_resume_verify.py` 的 evidence model 和 G03/G04/G05/G07/G09/G10 observer adapters，把固定 fixture、Typst 0.15.0 snapshot、font manifest 及所有原始 artifact hash 绑定为同一 record；所有公开错误使用稳定 code 与有界相对 workdir 路径，不泄露绝对路径或 canonical PII。具体落实 D-05、D-06、D-08。</action>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_phase49_verify.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 真实 normal-photo、no-photo、multi-target、content-pressure 产物均由 Markdown/Typst/PDF/PNG 四层重开得出一致观察值，A4 和 1/2 页以实际数值记录。
    - 修改 producer JSON 的 `passed/count/hash/theme/photo/page_count` 不改变 observer 输入；篡改 PDF 页面尺寸/页数、删除 PNG、交换 theme/stem/target metadata、改变模块页归属或注入未知字体均使对应稳定 gate 失败。
    - evidence 记录包含 fixture、source、Typst、font manifest、每个 triple 与逐页 PNG 的 SHA-256，且 artifact path 全部为 verify workdir-relative。
    - 测试证明 observer 不通过文件存在、非空、magic bytes 或 `pdfinfo` 文本单独放行。
  </acceptance_criteria>
  <done>VERIFY-01 的所有物理、结构、照片、命名与 metadata 事实均由独立重开观察获得并绑定原始 hash。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 固定 14-gate 聚合、发布前置门与 caller-owned 证据事务</name>
  <files>skills/graduate-resume/scripts/graduate_resume_verify.py, skills/graduate-resume/scripts/graduate_resume_cli.py, skills/graduate-resume/scripts/test_phase49_verify.py, skills/graduate-resume/references/phase-49-verification-contract.md</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_verify.py（Task 1 新建的 observer/evidence interfaces）
    - skills/graduate-resume/scripts/graduate_resume_cli.py（被修改文件；现有 `command_verify()`、argparse 与稳定 JSON error boundary）
    - skills/graduate-resume/scripts/test_phase48_cli.py（analog：字面 registry、`loadTestsFromName()`、required/called equality 与 raw outcome）
    - skills/school-pptx/scripts/verify_school_pptx.py（analog：固定 registry、candidate evidence 原子发布、mutation guard）
    - skills/graduate-resume/scripts/graduate_resume_delivery.py（analog/source of truth：held directory、no-follow、overlap/unknown/symlink 拒绝与 owned cleanup）
    - .planning/phases/48-deterministic-targeted-rendering-clean-batch-delivery/48-REVIEW.md（source of truth：CR-01..CR-06、WR-01..WR-03）
    - .planning/phases/49-runtime/49-CONTEXT.md（D-01..D-04、D-07）
  </read_first>
  <behavior>
    - `PHASE49_GATE_ORDER` 恰好等于 context 中的 14 个字面 ID；runner 固定顺序追加 called，最终验证 tuple equality、unique 和 `dynamic_skips == []`。
    - G01 要求最新 Phase 48 review 的 CR-01..CR-06、WR-01..WR-03 均有处置与复核证据，且 `48-SECURITY.md` 存在、覆盖 Phase 48 files/threats、结论通过并与当前 source hashes 相符；缺失、issues_found、stale 或只读旧 `48-VERIFICATION.md` 均失败。
    - G02-G10/G14 覆盖固定 schema、正常、无照片、多目标、资格缺口、内容压力、publication fault/history/no-op/rollback、物理观察、triple 绑定和 mutation；Phase 48 registry 仅作为 raw 子输入并被顶层重新观察。
    - G11/G13 证据缺失时保留 gate 调用并失败关闭，不能删除、skip 或伪造通过；最终 arbiter 此时返回 `failed`，不能返回 `human_needed`。
    - 首次 verify 只在根 locator 不存在时创建 `runs/<active_run_id>/` 与原子 `active-run.json`；locator 已存在时必须给出精确 `--resume <active_run_id>`，否则 `ACTIVE_RUN_REQUIRED`/`ACTIVE_RUN_MISMATCH` 失败，绝不创建新 run。
    - 所有证据写入显式 caller-owned held workdir 的 `runs/<active_run_id>/`；拒绝 repository/delivery overlap、symlink/FIFO/unknown，cleanup 只删除该 active run owned path，delivery root 不出现 JSON/Markdown/PNG/log sidecar。
  </behavior>
  <action>先扩展 `test_phase49_verify.py` 锁死 14-ID tuple、required/called/unique/no-skip、六类 fixture 和 publication fault copied-tree cases，并加入 producer mutation guards。新增 active-run cases：首次运行创建 locator/run；locator 存在却未传 resume、错误/旧 run ID、重跑企图新建 run、跨 run runtime record、跨 run UAT record 均稳定失败；只有同一 active run 内与 acceptance artifacts exact hashes 绑定的记录可被 G11/G13 消费。随后在 `graduate_resume_verify.py` 实现 `run_phase49_acceptance()` 与 held active-run transaction：首次仅在无 locator 时生成有界 `active_run_id`，原子写 `<workdir>/active-run.json`，全部内容写 `runs/<active_run_id>/{raw,artifacts,runtime,uat}`；locator 已存在时强制 `--resume <active_run_id>` 并复验 CLI/locator/run-local acceptance 三处字段与目录 identity，禁止 latest fallback、静默新 run 或跨 run 查找。G01 读取 review/security raw evidence；G02-G10/G14 重开 raw/artifacts；G11/G13 仅读取当前 active run。修改 `graduate_resume_cli.py` 固定首次 `--workdir` 与续跑 `--workdir ... --resume <active_run_id>` 语义；更新 verification contract 的 locator schema、路径、错误 code 与三态规则。具体落实 D-01..D-04、D-07。</action>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_phase49_verify.py skills/graduate-resume/scripts/test_phase48_cli.py skills/graduate-resume/scripts/test_render_contract.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `graduate-resume.sh verify --workdir <temp>` 的 `required` 与 `called` 精确等于 14-ID tuple；删除、重复、重排、动态 skip 或按环境缩减任一 gate 均失败。
    - 当前 48-REVIEW 仍为 6 Blocker/3 Warning 或 48-SECURITY 缺失时，G01 和最终状态稳定为 failed；伪造顶层/子层 passed、counts 或 hash 不能改成 PASS。
    - 六类 fixture、Phase 48 nested registry 与故障注入 raw outcome 均被重新观察；candidate/no-op/history/unknown/symlink/fault/rollback 的任何 false-green producer mutation 被 G14 拒绝。
    - workdir 外文件、delivery triples 与 repository source hashes 在成功和故障路径均不变；delivery root 中不出现 acceptance JSON/Markdown、PNG 或 logs。
    - G11 或 G13 未完成时仍出现在 called 中且最终 failed；`human_needed` 尚不能由本计划产生。
    - 首次运行只创建一个 locator/run；旧 run、错误/缺失 `--resume`、重跑新建 run、跨 run runtime/UAT record 全部 stale/failed，且不会改变当前 `active_run_id`。
    - `active-run.json.active_run_id`、`runs/<active_run_id>/acceptance.json.active_run_id`、CLI `--resume` 与 held directory identity 精确相等时才可续跑。
  </acceptance_criteria>
  <done>VERIFY-02 由固定、观察式、失败关闭的 14-gate public authority 证明，Phase 48 债务与安全报告不能被顶层 PASS 掩盖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted Input / Abuse Path | Structural Control |
|----------|------------------------------|--------------------|
| producer subprocess → aggregator | producer 可伪造 passed、计数、hash、runtime/UAT 状态或省略失败 | 固定 registry、raw stdout/stderr/exit capture、artifact reopen、独立 SHA-256/结构重算、mutation guards |
| caller path → verify workdir | path traversal、source/current overlap、symlink/FIFO、预置 unknown 文件可污染仓库或 delivery | held caller-owned root、descriptor-relative no-follow、bounded relative paths、overlap/unknown rejection、owned cleanup |
| active-run locator → resumed evidence | stale/错误 run ID、silent new run 或跨 run record 可把人工/runtime 证据绑定到错误 artifacts | 根级原子 `active-run.json`、显式 `--resume`、CLI/locator/run-local field/held identity 四重复验、G11/G13 run-local-only |
| final Markdown/Typst → PDF/PNG observer | 共享 parser 错误或产物替换可让 metadata 与物理输出共同漂移 | 单次有界读取、独立物理页面/像素/字体观察、前后 identity/hash 绑定、四层交叉重算 |
| Phase 48 evidence → release gate | 历史 verification PASS、stale review/security 或伪造 disposition 可掩盖高危债务 | G01 精确要求 CR-01..06/WR-01..03 disposition、复核 hash 与 `48-SECURITY.md` PASS，缺失即失败 |
| candidate evidence → published acceptance | 中途故障或同名覆盖可留下 partial/false-success evidence | unique owned run、candidate-first、同源 JSON/Markdown、fsync/atomic replace、最终重读验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01-01 | Tampering | 14-gate aggregate | High | mitigate | 字面 `PHASE49_GATE_ORDER`、required/called exact equality、零 skip、raw evidence 重算与 G14 mutation guards；任一不一致强制 failed。 |
| T-49-01-02 | Elevation of Privilege | verify workdir filesystem | High | mitigate | held directory + `O_NOFOLLOW` + relative bounded names + source/current overlap、symlink/FIFO/unknown 拒绝；cleanup 仅操作 owned run。 |
| T-49-01-03 | Spoofing | Phase 48 release prerequisites | High | mitigate | G01 绑定 9 个 finding 的 disposition/recheck hashes 与存在且 passing 的 `48-SECURITY.md`；旧 verification PASS 无放行权。 |
| T-49-01-04 | Tampering | PDF/PNG physical evidence | High | mitigate | 重开 Markdown/Typst/PDF/PNG，复算 A4/pages/text/fonts/pixels/stem/metadata/hash；不信任 producer status/count/hash。 |
| T-49-01-05 | Information Disclosure | evidence paths/logs/stems | High | mitigate | 只公开 bounded workdir-relative paths与稳定 code；G01 要求 Phase 48 CR-01/CR-02 已修复，禁止 URL/电话/邮箱/身份证进入 stem、Markdown 或 evidence。 |
| T-49-01-06 | Denial of Service | PDF/PNG/raw log parsing | Medium | mitigate | 限制文件类型、字节数、页数、PNG 数、文本与 stderr 长度；超限稳定失败，不做无界递归或动态发现。 |
| T-49-01-07 | Repudiation | acceptance publication | Medium | mitigate | 同源 JSON/Markdown、raw log hash、环境/fixture/tool/font/artifact hash 与原子 evidence transaction 提供可复核链。 |
| T-49-01-SC | Tampering | package supply chain | High | accept | 本计划不安装任何包；只使用仓库已锁定 Python 依赖、Typst 0.15.0 与受控字体。任何缺失直接失败，禁止联网补装。 |
</threat_model>

<verification>
先运行 `test_phase49_verify.py` 证明独立 artifact observer 与 14-gate integrity，再连同 Phase 48 registry/render contract 运行，确认聚合不破坏原有渲染和投递契约。最后用临时 workdir 调用公开 CLI，检查当前 Phase 48 债务/安全报告缺失会真实得到 failed，同时 workdir 外和 delivery triples 无变化。
</verification>

<success_criteria>
- VERIFY-01：真实 PDF/PNG 被重开并独立证明 A4、1/2 页、主题、照片、模块、命名和 target metadata。
- VERIFY-02：固定 14-gate 聚合覆盖六类 fixture、故障注入、原始证据重算和 mutation guards。
- D-01..D-08 全部有代码、测试与 reference 追溯；G11/G13 尚未完成时不能被动态跳过或产生 PASS/human_needed。
- Phase 48 的 6 Blocker、3 Warning 与缺失/失败/stale 安全报告构成不可绕过的 release prerequisite。
- 所有 evidence 只存在 caller-owned verify workdir，正式 delivery triples 与 repository source 不受污染。
- 首次运行与 49-02 continuation 共享唯一 `active_run_id`；旧 run、错误 run-id、新建重跑和跨 run runtime/UAT evidence 不可通过。
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | 重复证据确认真实稳定的 1/2 页 A4 PDF | 49-01 | COVERED | artifact reopen + fixed aggregate
REQ | VERIFY-01 | PDF/PNG 深度结构与命名验收 | 49-01 | COVERED | Task 1
REQ | VERIFY-02 | 六类 fixture、聚合与故障注入 | 49-01 | COVERED | Task 2
CONTEXT | D-01..D-04 | fixed registry、raw recompute、fixture coverage、Phase 48 debt | 49-01 | COVERED | Task 2/G01/G14
CONTEXT | D-05..D-08 | PDF/PNG reopen、workdir isolation、hash/tool/font chain | 49-01 | COVERED | Task 1 + Task 2
RESEARCH | — | 14-gate authority、producer/observer/arbiter、caller-owned tree | 49-01 | COVERED | 两任务与 threat model
CONTEXT | D-09..D-16 | six runtime 与 human UAT | 49-02 | COVERED | 下游计划，非缺口
</source_audit>

<output>
Create `.planning/phases/49-runtime/49-01-SUMMARY.md` when done.
</output>
