---
phase: 47-controlled-themes-photo-frozen-layout
plan: "04"
type: execute
wave: 4
depends_on: [47-01, 47-02, 47-03]
files_modified:
  - skills/graduate-resume/scripts/graduate_resume_layout.py
  - skills/graduate-resume/scripts/test_layout_contract.py
  - skills/graduate-resume/scripts/test_layout_fixtures.py
autonomous: true
requirements: [THEME-04, THEME-05]
user_setup: []
must_haves:
  truths:
    - "每个 FrozenResumePlan 容器必须恰好一次、以正确的 page_number 出现在一个冻结页面；删、重、跨页移动或伪造页码会在 Typst 发射和 PDF 门禁前失败关闭。"
    - "真实 PDF/PNG 固定矩阵在三主题下仍证明完整一/两页事实归属；把同一真实产物拿去核对遭篡改计划也必须拒绝，不能把已验证事实静默隐藏或重复。"
    - "教育、技能、证书、项目、实训和经历的每个已验证显示值仅出现一次；D-05 是 THEME-04/05 的同一事实展示不变量，不增加独立需求映射。"
  artifacts:
    - path: skills/graduate-resume/scripts/graduate_resume_layout.py
      provides: FrozenResumePlan 全量唯一页面归属、photo_mode 参数一致性和无重复显示字段
    - path: skills/graduate-resume/scripts/test_layout_contract.py
      provides: 冻结计划拓扑及结构化显示字段的单元回归
    - path: skills/graduate-resume/scripts/test_layout_fixtures.py
      provides: 三主题真实 PDF/PNG 对抗门禁回归
  key_links:
    - from: FrozenResumePlan.validate
      to: emit_typst / verify_rendered_layout
      via: 两个下游入口均先验证同一个冻结计划
      pattern: "LAYOUT_PLAN_INVALID"
    - from: project_containers._display_fields
      to: PDF text assertions
      via: 每个已验证字段值独立且只发射一次
      pattern: "education|skills|certificates|projects|training|experience"
---

<objective>
关闭最新 verifier 发现的 Phase 47 冻结计划完整性和 D-05 字段重复展示缺口，而不重做既完成的 47-01..03，也不进入 Phase 48/49。

Purpose: 让跨模块的 FrozenResumePlan 成为完整、唯一且页码正确的事实归属契约，使 Typst 发射器和真实 PDF/PNG 门禁不能通过删失、重复、跨页或伪造页码的计划；同时恢复按字段语义独立显示的中文条目。
Output: 一份仅补 THEME-04/THEME-05 blocker 的可执行回归与实现闭环。
</objective>

<execution_context>@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-CONTEXT.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-VERIFICATION.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-REVIEW.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-01-SUMMARY.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-02-SUMMARY.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-03-SUMMARY.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-04-SUMMARY.md
<interfaces>
`FrozenResumePlan.validate(facts)` is the mandatory precondition already called by both `emit_typst(plan, facts)` and `verify_rendered_layout(plan, facts, pdf_path, png_paths)`.

`ContainerPlan.page_number` is the canonical container-level page assignment; `PagePlan.containers` is the canonical ordered per-page ID sequence. They must describe exactly the same complete allocation.

`compile_controlled(plan, document)` creates disposable real Typst/PDF/PNG evidence. It must remain a Phase 47 verification workdir, not a Phase 48 delivery root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 将冻结容器覆盖、页码归属和照片模式收紧为单一计划不变量</name>
  <files>skills/graduate-resume/scripts/graduate_resume_layout.py, skills/graduate-resume/scripts/test_layout_contract.py</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_layout.py（`FrozenResumePlan.validate()`、`_display_fields()`、`build_frozen_resume_plan()` 和 `_paginate()`）
    - skills/graduate-resume/scripts/graduate_resume_typst.py（`emit_typst()` 必须继续只消费并验证冻结计划）
    - skills/graduate-resume/scripts/graduate_resume_pdf_gate.py（门禁的 `plan.validate(facts)` 前置条件）
    - skills/graduate-resume/scripts/test_layout_contract.py（现有冻结计划、Typst 发射和安全回归模式）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-VERIFICATION.md（删、重、跨页、伪造页码和 D-05 的精确复现）
  </read_first>
  <action>先在 `test_layout_contract.py` 写红灯回归：从同一成功的双页计划以 `dataclasses.replace` 构造四种篡改，分别为从 `pages[*].containers` 删除一个已有 ID、重复一个已有 ID、将一个 ID 移到另一页但不改 `ContainerPlan.page_number`、以及仅伪造某个 `ContainerPlan.page_number`；四种均令 `plan.validate(facts)` 和 `emit_typst(plan, {"__verified__": True, **facts})` 抛出稳定 `LAYOUT_PLAN_INVALID`。再在 `FrozenResumePlan.validate()` 展平 `pages` 的 ordered ID 序列，要求它与 `self.containers` 的 ordered ID 序列完全相同、长度相同且无重复；逐页查出对应 `ContainerPlan`，要求其 `page_number == PagePlan.number`。保留已有未知容器、首页 profile、连续页锚点和 source_fact_ids 校验。将 `build_frozen_resume_plan()` 的入参收紧为 `photo_mode in {"photo", "no-photo"}` 且 `photo_mode == "photo"` 当且仅当 `photo is not None`，从已验证 `photo_mode` 写入 `FrozenResumePlan.photo_mode`；两个不匹配组合均为 `LAYOUT_PLAN_INVALID`。最后删除 `_display_fields()` 对非 candidate 字段把第一个字段作为 `context` 拼接到后续字段的重写；每个字段保留自己标签和值，仍经 `_display_string()` 验证，且不改变 canonical facts、容器 ID、分页高度计算、主题、照片 resolver 或 Phase 48 feedback 接口。为 education、skills、certificates、projects、training、experience 各构造/使用含多个字段的已验证条目，断言 `ContainerPlan.fields` 中每个字段文本仅出现一次、后续字段不再含首字段文本，并断言包含本任务前四种拓扑篡改及两种 photo_mode 不匹配的所有失败代码。</action>
  <verify><automated>python3 -m unittest skills/graduate-resume/scripts/test_layout_contract.py -v</automated></verify>
  <acceptance_criteria>对任一合法计划，`tuple(id for page in plan.pages for id in page.containers) == tuple(container.id for container in plan.containers)`，且每个引用容器的 `page_number` 等于所在页面号；四类计划篡改不会得到 Typst。六类显示模块的任何已验证字段值在其容器字段集中计数为 1，字段之间没有旧的首字段前缀。两种公开 `photo_mode`/照片对象不匹配组合稳定失败，正常 CLI 和照片安全边界保持原样。</acceptance_criteria>
  <done>冻结计划不能以遗漏、重复或错误页码传入下游，字段语义不再靠重复纯文本前缀表达。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用真实 PDF/PNG 固定矩阵证明发射器与门禁拒绝篡改计划</name>
  <files>skills/graduate-resume/scripts/test_layout_fixtures.py</files>
  <read_first>
    - skills/graduate-resume/scripts/test_layout_fixtures.py（`compile_controlled()`、固定 fixture/theme 注册表和真实 Typst 调用）
    - skills/graduate-resume/scripts/graduate_resume_pdf_gate.py（`verify_rendered_layout()` 的计划验证与实际 PDF 页文本检查）
    - skills/graduate-resume/scripts/graduate_resume_typst.py（`emit_typst()` 的验证入口）
    - skills/graduate-resume/scripts/test_layout_contract.py（Task 1 的计划篡改辅助模式）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-VERIFICATION.md（当前 verifier 对 PDF gate 可接受删容器的反例）
  </read_first>
  <action>在 `test_layout_fixtures.py` 提取一个只在测试中使用的 `replace`/重建辅助方法，基于 `pressure-two-pages.md` 的每个 `THEME_KEYS` 成功冻结双页计划产生删除、重复、跨页移动和伪造 `page_number` 四个变体。保持 `compile_controlled()` 对未篡改计划在 `TemporaryDirectory` 内实际生成受控字体 PDF 与每页 PNG，并让它可将已生成的 `pdf_path`、`png_paths` 暴露给同一测试回调或等效测试辅助函数，不能把证据写入 skill delivery root。对每个变体，先断言 `emit_typst(tampered, {"__verified__": True, **document.data})` 返回 `LAYOUT_PLAN_INVALID`；然后把同一份真实、未篡改 PDF/PNG 传给 `verify_rendered_layout(tampered, document.data, pdf_path, png_paths)`，断言它同样在提取/信任页面文本前返回 `LAYOUT_PLAN_INVALID`，从而证明 PDF gate 不能被伪造页面引用蒙混。保留并完整执行原有短内容、有照、临界、压力、不可满足 fixture 与三主题循环，新增 D-05 渲染级断言：对 pressure PDF 的所有页面拼接文本，education/skills/certificates/projects/training/experience 中每个 `ContainerPlan.fields` 的可见值经同一 `_compact` 规则恰好出现一次；断言时逐字段计数，不能仅搜索学校或模块标题。若 Typst 把同一字符串在合法不同字段中本就可见，测试 fixture 应选择互异的已验证值并在必要时按 `(container.id, field_key)` 建立确定映射，不能靠模糊全页 substring 放宽断言。不得修改主题、模板、schema、targeting、正式三件套、candidate/history/batch、照片读取边界或 Phase 49 UAT。</action>
  <verify><automated>python3 -m unittest skills/graduate-resume/scripts/test_theme_contract.py skills/graduate-resume/scripts/test_layout_contract.py skills/graduate-resume/scripts/test_layout_fixtures.py -v &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh verify</automated></verify>
  <acceptance_criteria>三主题压力样张仍实际生成精确两页 A4 PDF/PNG 并通过正常门禁；每个删除、重复、跨页、伪造页码变体均分别被 `emit_typst()` 和 `verify_rendered_layout()` 以 `LAYOUT_PLAN_INVALID` 拒绝；所有六类模块每个已验证可见字段值在真实 PDF 文本中恰好一次。完整既有 19+ 项主题、布局和固定 fixture 矩阵以及 skill-local `verify` 全部通过，临时证据目录外不出现 PDF/PNG 或交付/history 写入。</acceptance_criteria>
  <done>真实产物验证已从“页面引用存在”升级为“完整冻结计划不可被删、重、错页或伪造页码绕过”，并覆盖 D-05 的实际显示结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verified facts -> FrozenResumePlan | 已验证事实必须完整、唯一、按正确页码进入冻结页面，不能靠删除或重复调整版面。 |
| FrozenResumePlan -> Typst/PDF gate | 两个下游均先验证同一计划拓扑，不信任 `pages[*].containers` 的单方面声明。 |
| structured fields -> visible PDF text | 每个已验证字段独立显示一次，不能通过重复上下文前缀制造额外内容或隐藏语义。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-47-27 | Tampering | page container list | mitigate | ordered full-coverage、唯一性和 `ContainerPlan.page_number` 与 `PagePlan.number` 的同一验证。 |
| T-47-28 | Tampering | Typst emitter | mitigate | 对抗变体在 `emit_typst()` 前失败，不允许部分页面集合被发射。 |
| T-47-29 | Spoofing | PDF/PNG gate | mitigate | 用真实未篡改 PDF/PNG 对照每个篡改计划，门禁在文本检查前失败。 |
| T-47-30 | Tampering | display fields | mitigate | 移除 context 前缀重写，六类字段值的 source 与 PDF 文本逐项计数。 |
| T-47-31 | Tampering | public layout API | mitigate | `photo_mode` 与照片对象严格一致，保持既有安全 resolver 不变。 |
</threat_model>

<verification>
1. `python3 -m unittest skills/graduate-resume/scripts/test_layout_contract.py -v`
2. `python3 -m unittest skills/graduate-resume/scripts/test_theme_contract.py skills/graduate-resume/scripts/test_layout_contract.py skills/graduate-resume/scripts/test_layout_fixtures.py -v`
3. `skills/graduate-resume/scripts/graduate-resume.sh verify`
4. 在测试输出/实现中复核四类对抗变体分别触发 `LAYOUT_PLAN_INVALID`，且压力 fixture 的三主题均走真实 Typst -> PDF -> PNG -> `verify_rendered_layout()`。
5. `git diff --check -- .planning/phases/47-controlled-themes-photo-frozen-layout/47-04-PLAN.md skills/graduate-resume/scripts/graduate_resume_layout.py skills/graduate-resume/scripts/test_layout_contract.py skills/graduate-resume/scripts/test_layout_fixtures.py`
</verification>

<success_criteria>
Phase 47 的两项最新 verifier blocker 均关闭：冻结计划将每个容器完整、唯一、正确页码地分配到 1/2 页；发射器、PDF gate 和真实 PDF/PNG 回归均拒绝删、重、跨页和伪造页码；D-05 的六类字段在结构化字段与真实 PDF 中各仅显示一次。47-01..03 已完成的主题、照片、安全、布局、临时证据与文档边界保持不变，正式交付和跨 runtime 验收仍留在 Phase 48/49。
</success_criteria>

## Source Coverage Audit

| Source | Gap / constraint | Coverage |
|---|---|---|
| 47-VERIFICATION.md | 容器可删、重、跨页、伪造 page_number，且发射/PDF gate 会接受 | Task 1 建立计划不变量；Task 2 用真实 PDF/PNG 对抗复现验证两个下游入口。 |
| 47-VERIFICATION.md | D-05 首字段前缀重复 | Task 1 删除前缀重写并锁定六类结构字段；Task 2 锁定真实 PDF 中每项字段值一次。 |
| 47-REVIEW.md | `photo_mode` 参数未被公开布局接口守住 | Task 1 只在建计划入口增加一致性校验与两条回归。 |
| THEME-04 | 原子条目、正确两页逻辑、失败关闭 | 本计划只补完整唯一页归属，因此不再允许无标题续接或静默删失事实。 |
| THEME-05 | 不得虚构或靠隐藏事实凑版 | 本计划禁止从冻结页集合删除/重复已验证事实，并将实物 gate 绑定该不变量。 |

未重新规划 THEME-01..03；D-05 作为 THEME-04/THEME-05 的事实展示不变量随本计划验收，而非新的 requirements 映射。Phase 48/49、canonical schema、target/delivery、主题/照片安全重构均明确排除。

<output>Create `.planning/phases/47-controlled-themes-photo-frozen-layout/47-04-SUMMARY.md` when done; preserve the existing `47-04-SUMMARY.md` until execution decides whether to update it as the gap-closure summary.</output>
