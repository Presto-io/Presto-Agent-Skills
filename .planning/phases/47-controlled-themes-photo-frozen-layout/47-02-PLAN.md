---
phase: 47-controlled-themes-photo-frozen-layout
plan: "02"
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - skills/graduate-resume/scripts/graduate_resume_layout.py
  - skills/graduate-resume/scripts/graduate_resume_typst.py
  - skills/graduate-resume/scripts/graduate_resume_cli.py
  - skills/graduate-resume/scripts/test_layout_contract.py
  - skills/graduate-resume/templates/layout-measurement.json
autonomous: true
requirements:
  - THEME-03
  - THEME-04
  - THEME-05
user_setup: []
must_haves:
  truths:
    - "用户可获得自动或强制的精确 1 页/2 页 A4 冻结布局建议，临界或强制模式保留另一页数的对照方案。"
    - "项目、实训、经历、职责和成果不会跨页拆开；第二页不会以孤立标题或无标题续接内容开始。"
    - "内容不足和分页压力只通过允许重排、空字段省略与间距阶梯处理，不会添加或改写事实。"
    - "无法在两页可读性边界内放入时，CLI 返回稳定失败而非发射不完整布局；有照片的冻结计划保留每主题已验证的 contain/crop 策略，禁止发射器拉伸或擅自裁切。"
  artifacts:
    - path: skills/graduate-resume/scripts/graduate_resume_layout.py
      provides: FrozenResumePlan、受版本/哈希约束的测量阈值、容器投影、三段式页数选择、已冻结 PhotoFitPolicy 与条目原子分页
      contains: FrozenResumePlan
    - path: skills/graduate-resume/scripts/graduate_resume_typst.py
      provides: 只消费完整冻结计划的安全 Typst 发射器
      contains: emit_typst
    - path: skills/graduate-resume/scripts/test_layout_contract.py
      provides: 页数、锚点、不可拆与失败关闭的纯逻辑测试
      contains: LAYOUT_UNSATISFIABLE
  key_links:
    - from: skills/graduate-resume/scripts/graduate_resume_cli.py
      to: skills/graduate-resume/scripts/graduate_resume_layout.py
      via: plan 命令从已校验 ResumeDocument 构建 FrozenResumePlan
      pattern: build_frozen_resume_plan
    - from: skills/graduate-resume/scripts/graduate_resume_layout.py
      to: skills/graduate-resume/scripts/graduate_resume_typst.py
      via: 发射器只接收 FrozenResumePlan 及已验证事实视图
      pattern: emit_typst
    - from: skills/graduate-resume/scripts/graduate_resume_typst.py
      to: skills/graduate-resume/templates/resume-themes.typ
      via: 受控模板填充、统一事实文本转义和固定 pagebreak 序列
      pattern: resume-themes.typ
  decision_trace:
    - "D-18: auto 按内容量选择合适页数，不硬编码一页或两页优先。"
    - "D-19: auto 使用明显适合 1 页、明显适合 2 页和临界区三段式判断。"
    - "D-20: 内容明显偏少直接一页、明显偏多直接两页，不为凑页或压页失真。"
    - "D-21: 临界区给出建议页数并生成两种页数版本，主产物采用建议版。"
    - "D-22: auto 尽量不失败，单页不合适转双页，仅双页超边界时失败关闭。"
    - "D-23: 强制一页允许受限激进压缩，同时提示推荐两页并生成两页对照。"
    - "D-24: 强制两页内容不足时仍生成两页，同时提示推荐一页。"
    - "D-25: 分页最小完整单元是单个条目。"
    - "D-26: 模块尽量不拆，必要跨页时仍保持单个条目不可拆。"
    - "D-27: 项目、实训、经历按单条严格不可拆。"
    - "D-28: 证书可整组续页，但同一证书条目内部不可拆。"
    - "D-29: 技能可按组续页，禁止页尾孤立技能组标题。"
    - "D-30: 个人信息冻结为首页首位锚点，教育保持前部稳定区域。"
    - "D-31: 为分页和阅读可受限重排，但不得破坏正常简历阅读习惯。"
    - "D-32: 分页困难时先在许可范围调模块顺序，再进行密度压缩。"
---

<objective>
实现冻结布局计划、条目级分页和 Typst 机械发射边界，使同一已验证资料能在受控主题下收敛为可审阅的 1 页或 2 页 A4 方案。

Purpose: 落实 D-18 至 D-32、THEME-03 至 THEME-05，并让布局逻辑在最终渲染前被完整验证。
Output: 不可变 `FrozenResumePlan`、页数建议/对照逻辑、不可拆条目与锚点验证、只消费计划的 Typst 发射器及单元测试。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-CONTEXT.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-PATTERNS.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-01-SUMMARY.md

<interfaces>
From plan 47-01 output, `graduate_resume_layout.py` owns `ThemeSpec`, `THEME_SPECS`, `resolve_theme()` and secure photo/font resolution. Its returned values must be passed through, not recomputed.

From skills/graduate-resume/scripts/graduate_resume_cli.py:
```python
def load_resume(path_value: str) -> ResumeDocument: ...
def validate_document(document: ResumeDocument) -> dict[str, Any]: ...
class CliError(Exception): ...
```
The CLI already normalizes v2 facts and emits JSON failures; Phase 47 may extend only `plan` and `verify`, leaving `render`/`batch` reserved for Phase 48.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 以不可变计划投影语义容器和三段式页数决议</name>
  <files>skills/graduate-resume/scripts/graduate_resume_layout.py, skills/graduate-resume/scripts/test_layout_contract.py, skills/graduate-resume/templates/layout-measurement.json</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_layout.py（47-01 的 ThemeSpec、字体/照片决议接口）
    - skills/graduate-resume/scripts/graduate_resume_cli.py（ResumeDocument、已验证 data 形状、CliError）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-CONTEXT.md（D-05a、D-07 至 D-11、D-18 至 D-32）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Frozen Layout And Pagination Contract、Typography、Page Geometry And Density）
    - skills/school-pptx/scripts/pptx_model.py（frozen dataclass 与 to_projection 范例）
    - skills/school-pptx/scripts/pptx_paginate.py（纯分页器与有界诊断范例）
  </read_first>
  <behavior>
    - 计划投影必须包含 theme、page_count、photo_mode、逐主题冻结的 `photo_fit` 与 `crop_policy`、font_manifest_hash、measurement_version、measurement_hash、pages、containers、entry_budget、reorder_reason 和 recommendation；任一必填字段缺失即拒绝计划。
    - profile 永远为第一页首位锚点，education 保持前部；缺失模块不生成空容器，未知模块只在默认白名单映射或显式容器覆盖存在时进入计划。
    - projects/training/experience/certificates 的每条条目、标题、元数据、职责和成果属于同一 page；技能组标题与至少一项内容同页。
    - measurement 配置是所有页数阈值的唯一来源：三主题均为 one_page_clear_max=0.88、one_page_critical_max=0.94、two_page_min_page_utilization=0.25、epsilon_mm=0.1；`two_page_min_page_utilization` 只约束 `--pages auto` 的“明显适合 2 页”推荐，绝不是所有双页计划的有效性门槛。auto 对明显少、明显多和临界内容作确定性三段决议；强制 1 页和强制 2 页各自保留推荐与对照计划。仅当请求页数在安全区、锁定字号/行距、条目完整性和有效页面开端等硬性规则下都不能生成时，才返回 LAYOUT_UNSATISFIABLE。
    - 预算只使用验证后的事实字段；允许顺序为省略空字段、受限模块重排、间距 token 阶梯，不得删改事实、缩小锁定字号或制造填充内容。
  </behavior>
  <action>先创建 versioned `templates/layout-measurement.json`，作为唯一测量阈值来源；三个 theme 都精确声明 `one_page_clear_max: 0.88`、`one_page_critical_max: 0.94`、`two_page_min_page_utilization: 0.25`、`epsilon_mm: 0.1`，并有非空 `measurement_version`。扩展布局模块，定义 `ContainerPlan`、`EntryBudget`、`PagePlan`、`PageRecommendation`、`FrozenResumePlan` 及其 `to_projection()`，均使用 frozen/slotted dataclass、tuple 和 JSON 原生值；计划记录 measurement_version 与配置文件 SHA-256，并从已解析 ThemeSpec 逐主题复制不可变的 `photo_fit`、`crop_policy`、主体安全区域合同和槽位上限，禁止在冻结后由发射器推断或改写。实现 `project_containers(verified_data, overrides)`：按 D-08/D-09 将个人信息、摘要、教育、技能映射为 `fact-block`，将项目、实训、经历、证书映射为 `list-entry`，保留原 section、事实 ID、字段和用户显式容器覆盖；空模块不渲染。实现无 I/O 的保守 CJK 预算器与 `build_frozen_resume_plan(...)`；每页 `utilization` 必须精确以 `(used_height_mm + epsilon_mm) / usable_height_mm` 计算，禁止从 PDF 像素、字号缩放或手工 pagebreak 改写。明确三条页数路径：`--pages auto` 同时评估合法 1/2 页候选，合法单页且 utilization <= 0.88 推荐 1 页；单页不合法且合法双页每页 utilization >= 0.25 推荐 2 页；合法单页且 0.88 < utilization <= 0.94 生成两份计划且推荐 1 页；合法单页且 utilization > 0.94 生成两份计划且推荐 2 页。`--pages 1` 在允许的密度阶梯内生成请求的一页计划，并同时生成推荐 2 页对照和 advisory（D-23）；`--pages 2` 必须在低于 0.25 的双页利用率下仍生成精确两页冻结计划，只要安全区、锁定字号/行距、完整条目、页首锚点和无虚构填充均满足，并同时写入 `recommendation=1` 的 advisory 与一页 comparison（D-24）。0.25 不得参与强制两页的拒绝判定。auto 仅在没有任何满足硬规则的 1/2 页候选时失败；强制模式仅在请求页数违反硬规则时返回 `LAYOUT_UNSATISFIABLE`，不得因 recommendation 或 comparison 不可用而拒绝已合法的请求计划。分页时只在完整条目边界续接模块，重排原因必须记录且不允许 profile 离开首页首位或 education 落入后部。以 `LAYOUT_PLAN_INVALID` 等稳定代码拒绝孤立标题、无标题续接、超出锁定字号/间距边界，或 `photo_fit`/`crop_policy` 与 ThemeSpec 不一致的计划。先写纯函数测试，再实现。此任务落实 D-07 至 D-11、D-18 至 D-32 和已冻结的临界测量规则。</action>
  <acceptance_criteria>
    - 单元测试精确覆盖 `layout-measurement.json` 的版本与 SHA-256、四个数值、`(used_height_mm + epsilon_mm) / usable_height_mm` 利用率，以及短资料自动推荐 1 页、超单页资料自动推荐 2 页、两个临界分支生成双方案、强制 1 页的推荐 2 页 advisory/对照及两页不可满足失败；并逐主题断言 FrozenResumePlan 明确记录与 ThemeSpec 完全一致的 `photo_fit=contain`、`crop_policy=forbid` 和槽位上限，篡改任一字段即拒绝。
    - 使用 `short-no-photo.md` 或等价固定短资料 fixture 的 `--pages 2` 测试必须成功：主计划 `page_count` 精确为 2，即使任一页 utilization 小于 0.25；`recommendation.recommended_pages` 为 1，包含一页 comparison 和 D-24 强制页数 advisory，且 pages/containers 只含源资料事实、没有空白占位或虚构填充。
    - 测试从每页 `containers` 断言 profile 的首页首位、education 前置、每个 list-entry 的 source fact ID 只出现于一个页面，以及第二页以语义模块标题开端。
    - 测试检索输入与投影，证明不存在凭空新增的经历、证书、成果、数字或空白占位容器；只出现经过验证的字段值。
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_layout_contract.py -v</automated>
  </verify>
  <done>布局规划器生成可 JSON 审阅、不可变、精确 1/2 页的候选计划；所有分页和事实边界在 Typst 发射之前被验证或失败关闭。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现只消费冻结计划的安全 Typst 发射器</name>
  <files>skills/graduate-resume/scripts/graduate_resume_typst.py, skills/graduate-resume/scripts/graduate_resume_layout.py, skills/graduate-resume/scripts/test_layout_contract.py</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_layout.py（FrozenResumePlan、ContainerPlan、计划验证和 ThemeSpec 接口）
    - skills/graduate-resume/templates/resume-themes.typ（47-01 的主题宏、照片和容器入口）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-PATTERNS.md（Typst 文本转义、模板/数据分离、受控编译范例）
    - skills/tiaokedan/scripts/tiaokedan_renderer.py（typst_content 与 subprocess 错误处理范例）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Photo Contract、Frozen Layout And Pagination Contract）
  </read_first>
  <behavior>
    - `emit_typst(plan, facts)` 只接受完整且已验证的 FrozenResumePlan；输入缺字段、主题不匹配、事实 ID 不匹配或未验证事实时返回稳定拒绝。
    - 所有候选人字符串经一个 Typst 转义函数处理；不得将 Markdown、照片路径或用户文本作为 Typst 源码拼接。
    - 发射器按 plan.pages/container page 归属输出固定序列，不重新测量、排序、选择事实或依剩余空间插入分页。
    - no-photo 输出不包含图像、文件路径、EXIF 字段或照片装饰；photo 输出只使用布局层已经验证的安全资产句柄和 FrozenResumePlan 中的 ThemeSpec PhotoFitPolicy，照片槽宽/高不超过 `35 mm x 49 mm`。
    - 默认 photo 分支必须 contain 式等比放置、不得拉伸；只有 FrozenResumePlan 已记录该主题显式 `allow_controlled_crop=True` 时才可进入受控 crop，未许可主题不得由 Typst 默认 `cover`、模板或发射器自行裁切。
  </behavior>
  <action>新建 `graduate_resume_typst.py`，实现单一 `typst_content(value)` 文本转义函数、`validate_emit_inputs(plan, facts)` 和 `emit_typst(plan, facts)`。发射器读取 skill-local `resume-themes.typ`，仅将完整计划投影的主题 key、页顺序、容器 section/id、冻结几何、照片槽尺寸、PhotoFitPolicy 与已验证事实视图填入模板；固定的 `pagebreak()` 只能来自 `plan.pages` 的边界。验证器必须拒绝缺失或不匹配的主题照片策略、超出 `35 mm x 49 mm` 的槽位、`allow_stretch=True` 或未许可裁切。它不得重跑 CJK 测量、模块重排、页数选择、空字段扩写或照片决议。对 no-photo 调用，完全排除 image/path/EXIF/边框/强调条字段；对 photo 调用，默认传入 contain/保持比例/禁拉伸指令，仅在 FrozenResumePlan 明确的 `allow_controlled_crop=True` 时传入受控 crop 指令，并仅接收布局层提供的非敏感、相对的媒体标识而非真实绝对路径。补充单元测试以拒绝计划篡改、未验证事实、缺失 font_manifest_hash、包含路径的 no-photo 投影、未经许可的 crop、任何 stretch 策略和未转义 Typst 特殊字符。此任务落实 D-05、D-05a、D-12 至 D-17a、D-25 至 D-32，并为 THEME-02/03/04 提供机械连接。</action>
  <acceptance_criteria>
    - 测试传入相同 facts 但改变 page/container 顺序的计划，发射器只接受已验证的原计划，不执行静默重排。
    - 测试包含 `#`、`[`、`]`、反斜杠和换行的资料文本，生成的 Typst 不执行用户插入的指令。
    - 测试 no-photo Typst 文本不含 fixture 媒体路径、`image(`、照片边框/强调条调用、`EXIF` 或绝对路径；photo 分支也不泄露原资料路径，并只发射不超过 `35 mm x 49 mm` 的 contain/等比/禁拉伸指令。测试构造未许可 crop、`allow_stretch=True`、超限槽位，均必须在生成 Typst 前稳定失败。
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_layout_contract.py -v</automated>
  </verify>
  <done>Typst 发射器成为冻结计划的纯下游消费者，无法把未验证事实、路径或新的分页决策带进排版文本。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 将 Phase 47 计划摘要接入 CLI 并保留后续交付边界</name>
  <files>skills/graduate-resume/scripts/graduate_resume_cli.py, skills/graduate-resume/scripts/test_layout_contract.py</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_cli.py（command_plan、command_reserved、main、JSON stderr 边界）
    - skills/graduate-resume/scripts/graduate_resume_layout.py（build_frozen_resume_plan、to_projection、稳定诊断）
    - skills/graduate-resume/scripts/graduate_resume_typst.py（validate_emit_inputs、emit_typst）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Interaction And Copywriting Contract）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md（Existing Extension Points And Gaps、Validation Evidence）
  </read_first>
  <behavior>
    - `plan --input` 在资料验证通过后输出 Phase 47 的 JSON 冻结计划摘要，含实际主题、精确页数、照片模式、字体清单哈希、recommendation、对照方案标识和受限重排原因。
    - 临界与强制页数输出 UI-SPEC 指定的建议/强制提示，且提示不含电话、邮箱、完整源事实、绝对照片路径或 EXIF。
    - `LAYOUT_UNSATISFIABLE`、字体/照片失败和计划篡改均为非零 JSON stderr；不会产出预览、正式 Markdown/Typst/PDF 或交付根文件。
    - `render`、`batch` 仍返回 NOT_IMPLEMENTED，不能实现 Phase 48 的三件套、批量、history、no-op 或 rollback。
  </behavior>
  <action>将 `command_plan` 从 Phase 46 baseline 升级为：读取资料、调用既有 `validate_document()`、应用 47-01 的参数覆盖、以 `--assets-root`（缺省输入 Markdown 父目录）执行安全照片决议、验证字体 manifest 后调用 `build_frozen_resume_plan()`，最后只输出脱敏 `to_projection()` JSON 摘要。摘要采用 UI-SPEC 的推荐文案，公开 theme/page_count/photo_mode/font_manifest_hash、measurement_version、measurement_hash、每页 utilization、容器 ID、页锚点和对照标识，但不得公开 profile 联系方式、完整正文、真实照片路径、EXIF 或任何原图字节。映射 `FONT_MANIFEST_INVALID`、`PHOTO_ASSET_INVALID` 与布局异常到 allowlisted JSON 错误码和无路径短消息；任何失败均不调用 Typst 编译、写出正式产物或改变输入。保留 `command_reserved` 对 render/batch 的现有行为，并用 CLI 级测试覆盖成功、临界、强制、两页失败、字体/照片边界和保留命令。此任务落实 D-06、D-18 至 D-24 和已冻结的字体/照片/测量边界。</action>
  <acceptance_criteria>
    - CLI 集成测试解析 `plan` JSON，断言 phase=47、theme/page_count/photo_mode/font_manifest_hash/measurement_version/measurement_hash/每页 utilization/recommendation 存在，且输出不含 fixture 的 phone/email/photo 原路径。
    - 临界与强制输出有 recommendation、actual 与 comparison 页数；CLI 测试对短资料执行 `plan --pages 2`，断言零退出、actual/page_count 精确为 2、recommendation 为 1、存在一页 comparison 和强制两页提示，且低利用率不被映射为 `LAYOUT_UNSATISFIABLE`。只有安全区、可读性或条目完整性等硬约束无法满足时才非零退出并含稳定代码 `LAYOUT_UNSATISFIABLE`。
    - `render` 和 `batch` 的调用仍以 `NOT_IMPLEMENTED` 非零退出；测试工作目录无新的正式三件套或 history 目录。
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_layout_contract.py -v &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh plan --input skills/graduate-resume/fixtures/valid-no-photo.md</automated>
  </verify>
  <done>用户可用既有离线 CLI 获得经过验证、脱敏的冻结布局方案与页数建议，而正式交付事务仍被明确隔离在 Phase 48。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已校验事实 -> 容器投影 | 语义事实进入布局预算，不能被主题或重排改写。 |
| FrozenResumePlan -> Typst 发射 | 不可变页面、容器、字体和照片决议被转换为 Typst 文本。 |
| CLI 参数 -> 页数/主题覆盖 | 用户偏好影响派生布局，但不得绕开 schema、照片或字体门禁。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-11 | Tampering | `FrozenResumePlan` | mitigate | frozen/slotted 计划、完整字段验证、事实 ID 交叉检查；发射器拒绝不完整或被篡改计划。 |
| T-47-12 | Information Disclosure | plan JSON / Typst | mitigate | 投影和错误只输出 allowlisted 布局元数据；照片绝对路径、EXIF、电话、邮箱和正文不进入输出。 |
| T-47-13 | Denial of Service | 分页预算 | mitigate | 限定为 1/2 页与锁定预算；两页仍不满足返回 `LAYOUT_UNSATISFIABLE`，不反复扩页或缩字。 |
| T-47-14 | Tampering | 主题/重排 | mitigate | ThemeSpec 不能携带事实选择；重排原因记录并验证 profile 首页首位、education 前部和条目原子性。 |
| T-47-15 | Elevation of Privilege | Typst 文本 | mitigate | 统一转义所有用户事实；只读 skill-local 模板，拒绝把路径或原始 Markdown 拼接为 Typst 代码。 |
| T-47-16 | Spoofing | 未验证事实 | mitigate | CLI 在构建计划前强制 `validate_document()`；pending、未知或不可信事实不能进入容器。 |
| T-47-SC | Tampering | 受控 Typst/字体版本 | mitigate | 计划携带 font_manifest_hash；后续 fixture 编译必须使用锁定 Typst 0.15.0、font path 与 `--ignore-system-fonts`，缺失即失败。 |
</threat_model>

<verification>
1. `python3 -m unittest skills/graduate-resume/scripts/test_layout_contract.py -v` 覆盖计划字段、页数决议、锚点、条目原子性、Typst 安全和 CLI JSON。
2. `skills/graduate-resume/scripts/graduate-resume.sh plan --input skills/graduate-resume/fixtures/valid-no-photo.md` 输出脱敏 Phase 47 冻结计划。
3. 对布局无法满足的压力输入，确认非零 `LAYOUT_UNSATISFIABLE`，且没有写出正式投递文件。
</verification>

<success_criteria>
- 所有合法资料都先形成完整不可变计划，再进入任何 Typst 文字发射。
- 自动、临界和强制 1/2 页规则有可机器验证的建议、对照或失败结果。
- 条目完整性、页首锚点、可读性下限和不虚构事实均由规划器与发射器边界共同保证。
</success_criteria>

<output>
Create `.planning/phases/47-controlled-themes-photo-frozen-layout/47-02-SUMMARY.md` when done.
</output>
