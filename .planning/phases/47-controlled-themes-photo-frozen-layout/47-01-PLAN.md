---
phase: 47-controlled-themes-photo-frozen-layout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/graduate-resume/fonts/SourceHanSansSC-Regular.otf
  - skills/graduate-resume/fonts/SourceHanSansSC-Semibold.otf
  - skills/graduate-resume/fonts/manifest.json
  - skills/graduate-resume/fonts/LICENSE.txt
  - skills/graduate-resume/fixtures/layout/media/student-photo.jpg
  - skills/graduate-resume/scripts/graduate_resume_layout.py
  - skills/graduate-resume/scripts/graduate_resume_cli.py
  - skills/graduate-resume/scripts/test_theme_contract.py
  - skills/graduate-resume/templates/resume-themes.typ
autonomous: true
requirements:
  - THEME-01
  - THEME-02
user_setup: []
must_haves:
  truths:
    - "用户可在不改变 graduate-resume/v2 事实 schema 的情况下选择首批正式主题保守稳妥、现代简洁或个性设计，并能看到其稳定 key。"
    - "每个首批主题都有且只有一个不超过 35 mm x 49 mm 的固定照片槽位；ThemeSpec 与 FrozenResumePlan 均逐主题冻结 `photo_fit=contain` 和 `crop_policy`，默认等比放置、禁止拉伸，未显式许可裁切时不会裁切；无照片模式不会保留照片槽或装饰空位。"
    - "布局只接受经校验的 skill-local 字体和受限本地照片，失败时不泄露路径或图片元数据。"
  artifacts:
    - path: skills/graduate-resume/scripts/graduate_resume_layout.py
      provides: 可扩展 ThemeSpec 注册表、中文主题别名解析、每主题照片适配策略和照片路径决议
      contains: THEME_SPECS
    - path: skills/graduate-resume/templates/resume-themes.typ
      provides: 三个纯视觉 A4 主题、fact-block/list-entry 与固定照片槽位宏
      contains: conservative
    - path: skills/graduate-resume/fonts/manifest.json
      provides: 两个 Source Han Sans SC 字重的相对路径、SHA-256 与许可来源身份
      contains: Source Han Sans SC
  key_links:
    - from: skills/graduate-resume/scripts/graduate_resume_cli.py
      to: skills/graduate-resume/scripts/graduate_resume_layout.py
      via: plan 子命令在资料校验后，以 --assets-root 调用主题和照片决议
      pattern: resolve_theme|resolve_layout_photo|assets_root
    - from: skills/graduate-resume/scripts/graduate_resume_layout.py
      to: skills/graduate-resume/fonts/manifest.json
      via: 构建布局前验证字体清单与文件哈希
      pattern: font_manifest_hash
    - from: skills/graduate-resume/templates/resume-themes.typ
      to: skills/graduate-resume/scripts/graduate_resume_layout.py
      via: ThemeSpec 稳定 key 和 PhotoFitPolicy 选择对应的唯一主题、照片槽及适配方式
      pattern: conservative|modern|expressive|contain
  decision_trace:
    - "D-01: 主题仅产生样式差异，不能改变事实、模块语义或内容强调。"
    - "D-02: 三个主题可采用不同页面构图，而非同骨架换皮。"
    - "D-03: 首批正式主题固定为保守稳妥、现代简洁、个性设计。"
    - "D-03a: 三个首批主题完整可见可选；ThemeSpec renderer-owned registry 可扩展，新增主题不得改写 graduate-resume/v2 或已验证事实。"
    - "D-04: 主题保持职业、可打印、可投递，避免海报化。"
    - "D-05: graduate-resume.md 是所有主题、页数和照片派生物的唯一可编辑事实源。"
    - "D-05a: 保持 v2 混合格式：profile/photo/preferences 在 frontmatter，事实模块在 Markdown 正文，正文顺序不赋予事实语义。"
    - "D-06: 资料修改回写唯一事实源；页数、主题和照片偏好通过 frontmatter 或 CLI 参数表达。"
    - "D-07: 个性化模块默认由 agent 判定容器归属，并保留用户显式覆盖。"
    - "D-08: 用 fact-block 承接结构化信息、list-entry 承接条目型模块。"
    - "D-09: 通用容器不抹平教育、项目、证书等模块语义。"
    - "D-10: 缺失模块不渲染，不保留空白或编造内容。"
    - "D-11: 以标准模块集合、可选启用、受控顺序和自适应补位适配布局。"
    - "D-12: 照片是固定占位区域的可选元素。"
    - "D-13: 每个主题只定义一个固定照片位置，不派生位置变体。"
    - "D-14: 无照片时照片和装饰一起消失；仅挤占主内容流的主题补齐移动。"
    - "D-15: 对主内容骨架影响小的右侧、下部等照片位，无照片可保持整体布局。"
    - "D-16: 照片形态和装饰随主题变化，但维持职业感。"
    - "D-17: 默认 contain 等比放置；裁切仅能由受控策略明确许可。"
    - "D-17a: 每个照片槽位上限为 35 mm x 49 mm，按主题固定等比放置且禁止拉伸。"
---

<objective>
为首批三种正式主题建立可扩展的纯视觉注册表、受控字体和照片/无照片边界，使后续冻结计划只接收可验证的主题与媒体派生值。

Purpose: 落实 D-01 至 D-17、THEME-01 与 THEME-02，防止主题篡改事实、系统字体漂移和本地照片路径泄漏。
Output: 受控字体清单与测试照片、ThemeSpec/照片安全决议、可由后续发射器消费的 Typst 主题骨架和单元测试。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-CONTEXT.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-PATTERNS.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md
@skills/graduate-resume/references/schema-and-review-contract.md
@skills/graduate-resume/references/phase-46-baseline.md

<interfaces>
From skills/graduate-resume/scripts/graduate_resume_cli.py:
```python
class CliError(Exception):
    def __init__(self, code: str, message: str, details: list[str] | None = None) -> None: ...

def load_resume(path_value: str) -> ResumeDocument: ...
def validate_document(document: ResumeDocument) -> dict[str, Any]: ...
def resolve_photo_mode(photo: dict[str, Any], preferences: dict[str, Any]) -> str: ...
```
`plan` 必须保持先 `load_resume()`、再 `validate_document()` 的输入边界；`render` 与 `batch` 继续返回 `NOT_IMPLEMENTED`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固定可验证字体和非敏感照片测试资产</name>
  <files>skills/graduate-resume/fonts/SourceHanSansSC-Regular.otf, skills/graduate-resume/fonts/SourceHanSansSC-Semibold.otf, skills/graduate-resume/fonts/manifest.json, skills/graduate-resume/fonts/LICENSE.txt, skills/graduate-resume/fixtures/layout/media/student-photo.jpg</files>
  <read_first>
    - skills/graduate-resume/references/phase-46-baseline.md（受控字体、--ignore-system-fonts 和后续交接约束）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md（Standard Stack、Environment Availability、Security Domain）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Design System、Typography、Photo Contract）
    - skills/school-presentation/references/identity/asset-manifest.json（相对路径与 SHA-256 资产身份范例；若文件不存在，记录该事实并仅复用其研究定义的字段形状）
  </read_first>
  <action>只从 Adobe 官方 `adobe-fonts/source-han-sans` release `2.005R` 的 `09_SourceHanSansSC.zip` 取得未修改的简体中文静态 OTF `SourceHanSansSC-Regular.otf`（400）与 `SourceHanSansSC-Semibold.otf`（600），存入上述固定 skill-local 文件名；不得改用系统安装、镜像站、变量字体或 HW 变体。纳入上游完整未修改的 SIL Open Font License 1.1 `LICENSE.txt`，并在 manifest.json 以相对文件名记录 family、weight、字节数、SHA-256、许可证文件、release tag、release asset URL 与下载日期；拒绝缺文件、清单字段不精确、哈希不符、额外字重或 fonts/ 外路径。调用 `typst fonts --font-path skills/graduate-resume/fonts --ignore-system-fonts --variants`，严格确认 Regular 400 与 Semibold 600 可见；此后布局层必须在生成 `font_manifest_hash` 前重复清单、文件、哈希和字体可见性校验，任一步失败返回 `FONT_MANIFEST_INVALID`，不产生计划。另在每个 layout fixture Markdown 的父目录创建 `media/student-photo.jpg`：它只可为非敏感、可公开测试的普通 JPEG，不含真人、证件、EXIF、GPS、姓名或可识别信息。该资产契约落实 D-04、D-12、D-16、D-17 和已冻结研究结论。</action>
  <acceptance_criteria>
    - manifest 中恰有 weight 400 与 600 两条字体记录，二者路径均为相对路径，且 `shasum -a 256` 与声明值一致。
    - manifest 精确记录 `2.005R`、`09_SourceHanSansSC.zip` 的上游 URL、下载日期、许可证、字节数和两个相对字体文件的 SHA-256；`typst fonts --font-path skills/graduate-resume/fonts --ignore-system-fonts --variants` 输出 Source Han Sans SC Regular 400 与 Semibold 600，缺失、哈希失配或不可见时以 `FONT_MANIFEST_INVALID` 失败。
    - `file skills/graduate-resume/fixtures/layout/media/student-photo.jpg` 识别为 JPEG，`exiftool` 可用时不输出 EXIF/GPS/Creator/Artist 字段。
  </acceptance_criteria>
  <verify>
    <automated>shasum -a 256 skills/graduate-resume/fonts/SourceHanSansSC-Regular.otf skills/graduate-resume/fonts/SourceHanSansSC-Semibold.otf &amp;&amp; typst fonts --font-path skills/graduate-resume/fonts --ignore-system-fonts --variants | rg 'Source Han Sans SC.*(Regular|Semibold)' &amp;&amp; file skills/graduate-resume/fixtures/layout/media/student-photo.jpg</automated>
  </verify>
  <done>两个受控字体、许可证、哈希清单和非敏感照片 fixture 均存在；系统字体没有成为任何成功路径的依赖。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现纯 ThemeSpec 注册表和失败关闭的本地照片决议</name>
  <files>skills/graduate-resume/scripts/graduate_resume_layout.py, skills/graduate-resume/scripts/graduate_resume_cli.py, skills/graduate-resume/scripts/test_theme_contract.py</files>
  <read_first>
    - skills/graduate-resume/scripts/graduate_resume_cli.py（parse_args、validate_preferences、validate_photo、resolve_photo_mode、command_plan、CliError）
    - skills/graduate-resume/references/schema-and-review-contract.md（唯一事实源和 Photo Contract）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-CONTEXT.md（D-01 至 D-17）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Theme Inventory、Page Geometry、Photo Contract）
    - skills/school-pptx/scripts/pptx_model.py（frozen dataclass/to_projection 范例）
  </read_first>
  <behavior>
    - `保守稳妥`、`现代简洁`、`个性设计` 及稳定 key 分别解析为 conservative、modern、expressive，未知主题返回稳定错误码且不修改资料；`--help` 与成功 `plan` 脱敏摘要均完整列出三项中文名、稳定 key 和 `--theme <key>` 选择方式。
    - 首批三个 ThemeSpec 具有不同骨架和唯一固定照片槽，但其允许模块集合、模块语义及事实 ID 投影相同；ThemeSpec/THEME_SPECS 是可扩展注册表，未来新增主题只能新增视觉注册项，不得改写 graduate-resume/v2 schema 或已验证事实。
    - 每个 ThemeSpec 和由它产生的 FrozenResumePlan 都逐主题记录可序列化的 `photo_fit` 与 `crop_policy`：首批主题的默认 `photo_fit=contain`、`crop_policy=forbid`、等比且禁止拉伸；只有注册项显式声明 `crop_policy=controlled` 并附带主体安全区域合同的主题才可受控裁切，其他主题遇到无法完整 contain 的填满请求必须保持留白而非静默裁切。
    - `command_plan --assets-root`（默认输入 Markdown 父目录）是唯一进行文件系统照片校验的边界；auto/photo 只接受相对该 root 的常规、可读、非 symlink 本地照片，绝对路径、父级逃逸、URL、Windows drive/UNC、目录、FIFO/设备、symlink、不可读或非图片文件返回 `PHOTO_ASSET_INVALID`，错误 JSON 不含绝对路径。
    - no-photo 或缺失 photo 时返回不含 path、图像字节、EXIF 或照片装饰标识的 no-photo 决议。
  </behavior>
  <action>新建纯 Python 模块 `graduate_resume_layout.py`，以 `@dataclass(frozen=True, slots=True)` 定义 `ThemeSpec`、固定照片槽位、不可变 `PhotoAsset`、`PhotoFitPolicy` 和 `THEME_SPECS`；注册首批 conservative/modern/expressive 三个 key，并映射 D-03 的中文名称。该注册表须可通过新增 ThemeSpec 扩展未来主题；扩展不得改写 graduate-resume/v2 schema、资料解析或已验证事实。ThemeSpec 只保存 UI-SPEC 冻结的 A4 安全边距、栏比、色板、四级字号、spacing token、唯一照片槽、无照片补位规则和照片适配策略，绝不保存候选人文本、事实选择或 target 强调。为每个首批主题固定宽/高均不超过 D-17a 的 `35 mm x 49 mm` 槽位，并以字段 `photo_fit="contain"`、`crop_policy="forbid"`、`preserve_aspect_ratio=True`、`allow_stretch=False` 冻结默认行为；`FrozenResumePlan` 必须逐主题复制这些字段，不能由 Typst 发射器或 CLI 默认值补推。未来主题只有在注册项显式设置 `crop_policy="controlled"`、`allow_controlled_crop=True` 且声明可验证的主体安全区域合同后才允许裁切；不得根据源图片、CLI 参数或发射器默认行为推断许可。实现 `resolve_theme(value)`、`resolve_layout_photo(input_path, assets_root, photo, preferences)` 与字体 manifest 完整性入口，均通过 `CliError` 暴露 allowlisted 稳定错误码。`validate` 保持 Phase 46 的纯 schema 校验；仅 `command_plan` 新增 `--assets-root`（默认 `input_path.parent`）并在创建任何 FrozenResumePlan 前调用照片决议。照片路径必须是相对 root 的 POSIX 路径，逐段拒绝空段、`.`、`..`、绝对、URL、Windows drive/UNC；逐段 `lstat` 拒绝 symlink，随后 strict resolve 的目标仍须在 strict resolve 的 root 内、为可读普通文件。成功只把无绝对路径的受控 `PhotoAsset` 和已冻结照片策略传给下游，Typst 发射器不得按 YAML 路径二次读取；失败统一为 `PHOTO_ASSET_INVALID`，诊断仅含逻辑相对路径或稳定原因。扩展既有 `plan` 参数以接受 `--theme`、`--pages {auto,1,2}`、`--photo-mode {auto,photo,no-photo}` 覆盖，不改 frontmatter schema；`--help`、`plan` 成功脱敏摘要必须显示 `保守稳妥 (conservative)`、`现代简洁 (modern)`、`个性设计 (expressive)` 及 `--theme <key>` 用法，但不得显示照片路径、EXIF 或 PII。每次覆盖仍在 `validate_document()` 成功之后才调用布局层。保持 `render`/`batch` 的 `NOT_IMPLEMENTED` 行为。先在 `test_theme_contract.py` 写上述失败与成功行为，再实现代码。此任务逐条落实 D-01 至 D-06、D-12 至 D-17a 和已冻结的照片边界。</action>
  <acceptance_criteria>
    - 测试逐个断言首批三个主题 key、中文别名、每主题不超过 `35 mm x 49 mm` 的照片槽位、`photo_fit="contain"`、`crop_policy="forbid"`、PhotoFitPolicy 和无照片补位规则，并断言同值逐主题进入 FrozenResumePlan；默认策略为 contain/等比/禁止拉伸/禁止裁切，未显式许可的主题不得形成 crop 决议，主题对象不得暴露 candidate、target 或正文事实字段。
    - 测试以复制并新增一个纯视觉 ThemeSpec 的方式断言 ThemeRegistry 可扩展，同时对比新增前后的 `graduate-resume/v2` schema 和已验证事实投影保持字节等价；本阶段的 `--help` 与成功 `plan` 摘要均以三个独立断言完整显示三种中文主题、conservative/modern/expressive key 和 `--theme <key>`，且不含 fixture 照片路径或 PII。
    - 测试构造绝对路径、`.`/`..`、Windows drive/UNC、symlink、远程 URL、目录、FIFO/设备、不可读文件和无照片输入；它们分别产生 `PHOTO_ASSET_INVALID` 或 no-photo 投影，stdout/stderr 不含测试目录的绝对路径。
    - `plan --assets-root`、`--theme`、`--pages`、`--photo-mode` 能覆盖派生值；`--assets-root` 缺省时精确为输入 Markdown 父目录，而 `validate` 输入与原始 Markdown 字节不被改写。
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_theme_contract.py -v &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh --help | rg '保守稳妥.*conservative' &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh --help | rg '现代简洁.*modern' &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh --help | rg '个性设计.*expressive' &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh --help | rg -- '--theme' &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh plan --input skills/graduate-resume/fixtures/valid-no-photo.md | rg '保守稳妥.*conservative' &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh plan --input skills/graduate-resume/fixtures/valid-no-photo.md | rg '现代简洁.*modern' &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh plan --input skills/graduate-resume/fixtures/valid-no-photo.md | rg '个性设计.*expressive'</automated>
  </verify>
  <done>CLI 与纯布局层可确定性决议首批三主题和安全照片模式；用户可在帮助和计划摘要中看见并选择全部主题，注册表保留未来仅增视觉项的扩展能力，所有不可信路径与未知主题均失败关闭且不泄露隐私数据。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 建立三主题 A4 Typst 容器与照片槽位骨架</name>
  <files>skills/graduate-resume/templates/resume-themes.typ, skills/graduate-resume/scripts/test_theme_contract.py</files>
  <read_first>
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Theme Inventory、Spacing Scale、Typography、Color、Accessibility And Print Acceptance）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-CONTEXT.md（D-08 至 D-11、D-13 至 D-17）
    - skills/end-of-term-teaching-materials/templates/typst/end-of-term-package.typ（Typst 模板结构范例）
    - skills/graduate-resume/scripts/graduate_resume_layout.py（ThemeSpec 稳定 key、照片槽及容器契约）
  </read_first>
  <behavior>
    - 模板声明 A4、14/14/15/15 mm 安全边距、四种锁定字号、Source Han Sans SC 400/600 与 UI-SPEC 的 spacing token。
    - conservative、modern、expressive 仅表现为构图/颜色/分隔/固定照片槽的差异；所有主题均提供语义 `fact-block` 与不可拆 `list-entry` 宏，且照片策略由冻结 ThemeSpec 传入而非模板猜测。
    - 首批三个槽位均不超过 `35 mm x 49 mm`；模板以默认 contain 保持源图等比、禁止拉伸，只有传入 ThemeSpec 中显式 `allow_controlled_crop` 的主题才可调用受控 crop 分支。
    - no-photo 分支不发射 image、照片边框或强调条；conservative/modern/expressive 分别遵循 UI-SPEC 固定补位，不产生第二个照片位置。
  </behavior>
  <action>新建 `resume-themes.typ`，以 ThemeSpec 的稳定 key 提供纯视觉 token 与宏：A4 页面、锁定安全边距、两字重与四个字号、xs/sm/md/lg/xl spacing token、三套色板和三个唯一照片槽。实现 `fact-block(section, body)` 与 `list-entry(section, id, body)`，后者必须使用 `block(breakable: false)`；宏参数保留 section 和 id，不能把模块变成匿名卡片。按 UI-SPEC 指定的 conservative 单栏右上、modern 左栏、expressive 顶部右端配置照片，每个槽位的宽/高均不得超过 `35 mm x 49 mm`，仅接受发射器传入的受控图像句柄和冻结 PhotoFitPolicy。默认分支必须以 contain 式等比适配图片并禁止宽高独立缩放；只有 policy 显式 `allow_controlled_crop` 时才能进入受控裁切分支，未许可时必须保留完整照片和槽内留白。无照片时连同边框/强调条移除，并只按主题合同补齐内容流。模板不得内嵌候选人事实、路径、任何系统字体名称、远程资源、自由坐标参数或发射时分页决策。将模板文本断言加入现有主题契约测试，确保不可拆宏、字体、三个 theme key、无照片条件分支、照片上限、contain/等比/禁拉伸和显式裁切许可安全边界存在。</action>
  <acceptance_criteria>
    - 文本测试确认 `block(breakable: false)`、A4 页面、四个唯一字号和三个主题 key 存在，且没有候选人电话、邮箱、测试照片路径或 `http`。
    - 主题测试确认每个主题的照片槽坐标/尺寸只定义一次且宽/高不超过 `35 mm x 49 mm`，默认渲染分支声明 contain/等比/禁止拉伸，裁切分支只能由显式许可开关到达，且 no-photo 分支不调用图像或照片装饰宏。
    - Typst 在受控字体环境中可解析最小的无照片模板调用；若字体不可见或模板不合法，命令非零。
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_theme_contract.py -v &amp;&amp; rg -n 'block\(breakable: false\)|conservative|modern|expressive' skills/graduate-resume/templates/resume-themes.typ</automated>
  </verify>
  <done>三套职业、可打印主题和每主题唯一照片/无照片变体已固定为纯视觉模板契约，能够被后续冻结计划机械消费。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 资料 YAML/Markdown -> 主题与照片决议 | 不可信偏好和可选照片路径变成派生布局输入。 |
| skill-local 字体/媒体 -> Typst | 二进制资产、字体身份和图像内容进入受控排版环境。 |
| 冻结计划 -> 模板 | 发射器只能使用已验证的视觉 token，不得解释事实文本或路径。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information Disclosure | `resolve_photo_asset` | mitigate | 仅允许输入文件父目录内的常规可读非 symlink 图像；拒绝绝对路径、逃逸、URL 和目录，诊断不回显绝对路径、EXIF 或 PII。 |
| T-47-02 | Tampering | ThemeSpec / Typst 模板 | mitigate | 主题注册表只含视觉字段；测试断言主题不持有事实选择，模板不含候选人内容或远程资源。 |
| T-47-03 | Tampering | 字体清单 | mitigate | manifest 以相对路径与 SHA-256 身份验证两个锁定字重；`--ignore-system-fonts` 下字体缺失或哈希失配立即失败。 |
| T-47-04 | Information Disclosure | 无照片分支 | mitigate | no-photo 决议、模板输入与测试均排除图像、装饰、路径和 EXIF。 |
| T-47-05 | Denial of Service | 恶意媒体/路径 | mitigate | 拒绝非常规文件和不可信路径；本计划不进行自动下载、解码修复或网络访问。 |
| T-47-06 | Spoofing | 未核实资料 | mitigate | CLI 在主题/照片派生前保留 `validate_document()` 门禁；未验证事实不进入布局。 |
| T-47-SC | Tampering | 字体与媒体资产引入 | mitigate | 记录来源、许可证、哈希并执行本地清单校验；不使用未审阅第三方模板或运行时下载资源。 |
</threat_model>

<verification>
1. `python3 -m unittest skills/graduate-resume/scripts/test_theme_contract.py -v` 通过主题别名、纯视觉隔离、照片路径拒绝和模板 token 断言。
2. `typst fonts --font-path skills/graduate-resume/fonts --ignore-system-fonts` 显示清单声明的两个字重。
3. `skills/graduate-resume/scripts/graduate-resume.sh verify` 仍通过 Phase 46 资料 fixture，证明 schema 未被主题支持改写。
</verification>

<success_criteria>
- 首批三个锁定主题可经 YAML 偏好或 plan 参数解析，且不会改变资料事实、模块语义或 target 逻辑；ThemeSpec 注册表可在未来新增视觉主题而不修改 graduate-resume/v2 schema 或既有事实。
- 照片仅从通过受限本地路径检查的文件派生，每主题槽位不超过 `35 mm x 49 mm`，默认 contain 等比且绝不拉伸，裁切仅在显式策略许可时发生；无照片变体在计划和模板层均没有照片残留。
- 受控字体、主题几何、照片槽位和 Typst 容器已经为冻结分页提供确定性输入。
</success_criteria>

<output>
Create `.planning/phases/47-controlled-themes-photo-frozen-layout/47-01-SUMMARY.md` when done.
</output>
