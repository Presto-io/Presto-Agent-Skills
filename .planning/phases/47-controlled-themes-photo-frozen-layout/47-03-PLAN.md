---
phase: 47-controlled-themes-photo-frozen-layout
plan: "03"
type: execute
wave: 3
depends_on:
  - 47-01
  - 47-02
files_modified:
  - skills/graduate-resume/fixtures/layout/short-no-photo.md
  - skills/graduate-resume/fixtures/layout/standard-photo.md
  - skills/graduate-resume/fixtures/layout/boundary-content.md
  - skills/graduate-resume/fixtures/layout/pressure-two-pages.md
  - skills/graduate-resume/fixtures/layout/error-unsatisfiable-two-pages.md
  - skills/graduate-resume/fixtures/layout/media/student-photo.jpg
  - skills/graduate-resume/scripts/graduate_resume_pdf_gate.py
  - skills/graduate-resume/scripts/test_layout_fixtures.py
  - skills/graduate-resume/scripts/graduate_resume_cli.py
  - skills/graduate-resume/SKILL.md
  - skills/graduate-resume/references/phase-46-baseline.md
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04
  - THEME-05
user_setup: []
must_haves:
  truths:
    - "每种首批主题的有照片、无照片、短内容、临界内容和压力内容都有固定可重复的布局证据；有照片样张证明槽位不超过 35 mm x 49 mm、等比 contain 且未拉伸，裁切只在主题明确许可时发生。"
    - "样张编译后仅为精确 1 或 2 页 A4；第二页有合法模块开端，条目完整且无第三页。"
    - "无照片样张在计划、Typst、PDF/PNG 证据中没有图像、路径或 EXIF 残留；有照片样张的 PDF/PNG 证据不泄漏照片路径或 PII。"
    - "两页仍不满足的样张稳定失败关闭，而不是生成截断或部分投递产物。"
  artifacts:
    - path: skills/graduate-resume/fixtures/layout/short-no-photo.md
      provides: 内容不足和无照片的一页样张
    - path: skills/graduate-resume/fixtures/layout/boundary-content.md
      provides: 临界区双方案页数样张
    - path: skills/graduate-resume/fixtures/layout/pressure-two-pages.md
      provides: 双页锚点、长字段和条目不可拆压力样张
    - path: skills/graduate-resume/scripts/test_layout_fixtures.py
      provides: 固定样张矩阵、受控 Typst 编译和逐页 PDF 语义/结构门禁
  key_links:
    - from: skills/graduate-resume/scripts/graduate_resume_cli.py
      to: skills/graduate-resume/fixtures/layout
      via: verify 使用显式固定 fixture 注册表调用 plan
      pattern: LAYOUT_FIXTURES
    - from: skills/graduate-resume/scripts/test_layout_fixtures.py
      to: typst compile
      via: `--font-path`、`--ignore-system-fonts`、`--creation-timestamp 0` 在临时 workdir 编译
      pattern: --ignore-system-fonts
    - from: skills/graduate-resume/scripts/test_layout_fixtures.py
      to: pdfinfo
      via: 检查精确页数和 A4 尺寸
      pattern: Page size|Pages
    - from: skills/graduate-resume/scripts/graduate_resume_pdf_gate.py
      to: FrozenResumePlan
      via: 从已编译 PDF 按页提取稳定文字/锚点，并以 plan 的 section、fact_id 与 page 归属进行语义对照
      pattern: LAYOUT_RENDER_MISMATCH
  decision_trace:
    - "D-33: 验证矩阵和 canonical 说明保证三种首批主题均完整可见，用户不会只受单一主题限制。"
    - "D-34: 当前阶段锁定多主题预览原则，但不决定预览产物格式、发布路径或 PDF/PNG/HTML 输出；这些工程细节边界保留给 Phase 48/49。"
---

<objective>
用固定的一页、两页、有照片、无照片、临界和内容压力样张锁定主题密度、双页锚点与失败关闭证据，并将该证据接入 skill-local verify。

Purpose: 把 THEME-01 至 THEME-05 从布局规则转化为可重复、离线、无正式发布副作用的证据。
Output: 显式 layout fixture 矩阵、受控 Typst/PDF/PNG 验证门禁、更新后的 verify 与 canonical 阶段边界说明。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-CONTEXT.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-01-SUMMARY.md
@.planning/phases/47-controlled-themes-photo-frozen-layout/47-02-SUMMARY.md
@skills/graduate-resume/SKILL.md
@skills/graduate-resume/references/phase-46-baseline.md

<interfaces>
From plan 47-02 output:
```python
def build_frozen_resume_plan(... ) -> FrozenResumePlan: ...
def emit_typst(plan: FrozenResumePlan, facts: ResumeFacts) -> str: ...
```
`FrozenResumePlan.to_projection()` supplies theme, page count, photo mode, font manifest hash, pages, containers, entry budgets and recommendation. Fixture code must treat it as immutable and must not produce Phase 48 delivery artifacts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 编写显式的主题、照片和页数 layout fixture 矩阵</name>
  <files>skills/graduate-resume/fixtures/layout/short-no-photo.md, skills/graduate-resume/fixtures/layout/standard-photo.md, skills/graduate-resume/fixtures/layout/boundary-content.md, skills/graduate-resume/fixtures/layout/pressure-two-pages.md, skills/graduate-resume/fixtures/layout/error-unsatisfiable-two-pages.md, skills/graduate-resume/fixtures/layout/media/student-photo.jpg, skills/graduate-resume/scripts/test_layout_fixtures.py</files>
  <read_first>
    - skills/graduate-resume/fixtures/valid-photo-single-target.md、skills/graduate-resume/fixtures/valid-no-photo.md（v2 资料 fixture 格式）
    - skills/graduate-resume/scripts/graduate_resume_cli.py（VALID_FIXTURES/INVALID_FIXTURES 与 verify 固定注册表范式）
    - skills/graduate-resume/scripts/graduate_resume_layout.py（ThemeSpec、FrozenResumePlan 与 recommendation 投影）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md（Validation Evidence And Commands 的必测矩阵）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Accessibility And Print Acceptance、Frozen Layout And Pagination Contract）
  </read_first>
  <behavior>
    - 短无照片资料对每主题验证 auto 推荐的一页和无照片去除；同一短资料在强制 `--pages 2` 时必须成功生成精确两页，即使低于自动两页利用率阈值，并提供推荐一页的 advisory 与 comparison（D-24）。标准有照片资料必须对首批每主题分别编译 Typst/PDF/逐页 PNG 样张，并验证 FrozenResumePlan 的 `photo_fit=contain`、`crop_policy=forbid`、固定照片槽与本地测试照片：槽位不超过 `35 mm x 49 mm`、默认等比放置且不得拉伸；只有 ThemeSpec 显式许可且主体安全区域完整保留的主题可执行受控裁切。
    - 临界资料在每主题产生推荐主方案与另一页数对照方案；压力资料验证两页、长姓名/机构/证书/成果与页面锚点。
    - 不可满足资料在任何主题/页数选择下产生 LAYOUT_UNSATISFIABLE，且不生成 Typst/PDF/PNG 文件。
    - 所有 fixture 只含已验证资料；内容不足通过较少的真实条目表达，不添加虚构经历、证书、成果或数字。
  </behavior>
  <action>在 `fixtures/layout/` 新建五个命名固定的 v2 Markdown fixture：`short-no-photo.md` 表达真实但较少的已验证资料并省略 photo；`standard-photo.md` 使用同级 `media/student-photo.jpg` 的相对路径 `photo: media/student-photo.jpg` 和普通内容；`boundary-content.md` 用真实可支撑字段构造 ThemeSpec 临界阈值；`pressure-two-pages.md` 覆盖长姓名、长单位名、长证书名、长成果、多项目/实训/经历；`error-unsatisfiable-two-pages.md` 只用已验证事实字段构造超过双页预算的压力。照片只能来自 47-01 创建的同级公开普通 JPEG；为该非敏感测试媒体定义与图像一同版本化的主体安全区域（像素坐标矩形）测试常量，而非任何真人/PII 元数据，使用其已知像素宽高、主体安全区域和非槽位比例作为正常照片样张，供后续 Typst/PDF/PNG 验证 contain 等比、无拉伸以及“受控裁切仍完整包含主体安全区域”。测试不得记录、输出或断言真实照片路径/EXIF/PII。与此同时在矩阵中加入缺失、越界、symlink、目录、FIFO/设备和不可读照片的负例，逐项断言 `PHOTO_ASSET_INVALID`。新增 `test_layout_fixtures.py` 中显式常量矩阵，逐个遍历首批 3 themes x 有/无照片、auto/强制 1/强制 2 页的预期；禁止用目录 glob 自动发现 fixture。对于每个主题照片计划，断言 FrozenResumePlan 记录的槽位宽/高不超过 `35 mm x 49 mm`、`photo_fit=contain`、`crop_policy=forbid`、保持比例、禁止拉伸；若该主题未明确许可，断言没有 crop 决议。对每个主题的 `short-no-photo.md --pages 2`，断言命令成功、主计划精确两页、至少一页 utilization 小于 0.25、recommendation 为 1、存在一页 comparison 和 D-24 advisory，并证明所有 containers/fact IDs 来自输入且没有空白占位或虚构填充；该低利用率绝不能被归类为 `LAYOUT_UNSATISFIABLE`。断言 source fixture 通过原有 validate，计划事实 ID 属于源资料，短内容无虚构补位，临界有 comparison，并精确断言 measurement_version、measurement_hash、分支名、recommended/comparison 页数和各页 utilization；压力计划严格两页且不可满足样张为稳定失败。此任务落实 D-01、D-03、D-03a、D-05、D-10、D-12 至 D-17a、D-18 至 D-29 与已冻结的照片/临界规则。</action>
  <acceptance_criteria>
    - 测试以字面固定列表包含五个 layout fixture 和 conservative/modern/expressive，删除或添加文件不会静默改变执行集合。
    - 每个成功 fixture 均能通过 `validate_document()`，每个投影事实 ID 均在对应 Markdown 源事实中存在；测试拒绝新增事实 ID 或空白占位容器。
    - `short-no-photo.md` 在三个主题下的 auto 均推荐 1 页；对同一 fixture 的 `--pages 2` 均零退出且精确产生两页，至少一页利用率低于 0.25，`recommendation.recommended_pages=1`、一页 comparison 与强制两页 advisory 齐全，且测试证明未增加任何事实或空白占位。
    - `standard-photo.md` 对每主题断言 FrozenResumePlan 的固定照片槽宽/高均不超过 `35 mm x 49 mm`、`photo_fit=contain`、`crop_policy=forbid`、保持比例/禁止拉伸，未经主题显式许可不出现 crop；另构造仅供未来显式 `crop_policy=controlled` 主题使用的受控裁切测试，断言其可见源图区域完整包含测试主体安全区域，裁切边界侵入该区域即失败；`boundary-content.md` 对每主题在 auto 下断言 measurement_version、measurement_hash、精确临界分支、recommendation、comparison 与每页 utilization；`error-unsatisfiable-two-pages.md` 断言 `LAYOUT_UNSATISFIABLE`。
    - 缺失、越界、symlink、目录、FIFO/设备和不可读照片的负例均在 `command_plan --assets-root <fixture-parent>` 返回 `PHOTO_ASSET_INVALID`，不生成 FrozenResumePlan 或临时渲染文件。
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_layout_fixtures.py -v</automated>
  </verify>
  <done>布局证据覆盖所有主题、照片变体、1/2 页决议、临界区、压力和两页失败，不依赖隐式目录内容或未验证资料。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对样张执行受控 Typst 编译、逐页 PDF 语义与裁切门禁</name>
  <files>skills/graduate-resume/scripts/graduate_resume_pdf_gate.py, skills/graduate-resume/scripts/test_layout_fixtures.py, skills/graduate-resume/scripts/graduate_resume_cli.py</files>
  <read_first>
    - skills/graduate-resume/scripts/test_layout_fixtures.py（Task 1 的固定矩阵与 plan 断言）
    - skills/graduate-resume/scripts/graduate_resume_typst.py（emit_typst、事实转义、no-photo 规则）
    - skills/graduate-resume/fonts/manifest.json（锁定字体身份）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md（Validation Evidence And Commands、Common Pitfalls、Open Questions (RESOLVED)）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（A4、双页锚点、无照片泄露与打印验收）
  </read_first>
  <behavior>
    - 每个成功 fixture 在调用者临时 workdir 生成 Typst、PDF 和逐页 PNG 证据，编译参数固定为 `--font-path`、`--ignore-system-fonts` 和 `--creation-timestamp 0`。
    - `pdfinfo` 证明 PDF 恰为计划的 1 或 2 页并为 A4；第三页、非 A4、编译失败、PDF header 无效、真实裁切/溢出检测失败或计划页数不符均返回 `LAYOUT_RENDER_MISMATCH`。
    - 门禁从已编译 PDF 按物理页提取稳定文本和锚点，并以 FrozenResumePlan 的 section/fact_id/page 归属对照：每页预期模块标题和事实文字存在；第二页首个语义锚点是该页计划模块标题；profile 位于第一页首位、education 位于前部；每个 list-entry 的标题、元数据、职责和成果完整地只存在于计划指定页。任何遗漏、跨页、次序或归属不符均返回 `LAYOUT_RENDER_MISMATCH`。
    - 对同一受控 Typst 源生成逐页 PNG，以物理 A4 像素边界和 14/14/15/15 mm 安全区换算后的容许 epsilon 扫描非背景像素：页面边缘、边距外或安全区外出现文字/线条/图像像素，或内容连通域触及图像裁切边界，均为真实 overflow/crop 并返回 `LAYOUT_RENDER_MISMATCH`。每个首批主题的有照片样张还以已知原图宽高、PNG 中照片内容边界和 FrozenResumePlan 的 `photo_fit`/`crop_policy` 验证缩放后比例等于源图比例（允许固定像素 epsilon）、宽/高不超过 `35 mm x 49 mm`，且 `crop_policy=forbid` 的主题不存在裁切；对未来显式 `crop_policy=controlled` 的测试主题，反向映射 PNG 可见区域到源图并断言完整包含版本化主体安全区域，任何主体损伤均返回 `LAYOUT_RENDER_MISMATCH`；无照片产物递归扫描不到 image/path/EXIF/照片装饰。
    - verify 只写调用者指定的临时证据根，不向 Phase 48 投递根、history 或 canonical 资料文件写入。
  </behavior>
  <action>新建标准库 `graduate_resume_pdf_gate.py`，实现 `verify_rendered_layout(plan, facts, pdf_path, png_paths)`，只接收不可变 FrozenResumePlan、已验证事实和当前受控 workdir 产物。该模块必须解析已编译 PDF 的 page tree、内容流和 ToUnicode CMap（含 FlateDecode/object stream），逐物理页抽取并 Unicode 规范化稳定文本/锚点；不得以 Typst 源、计划投影或“PDF 可打开”替代 PDF 语义证据。由 plan 的 `section`、`fact_id`、`page` 构造 expected-page map：断言每页模块标题和对应事实文本均在该 PDF 页，第二页首个计划容器的模块标题先于该页所有条目/元数据/职责/成果，profile 的姓名在第一页首位，education 在前部；每个 `list-entry` 的标题、日期/组织等元数据、职责和成果的所有非空事实片段都出现且只出现于其计划指定页。映射、抽取、页序或完整性任何不一致一律抛 `CliError("LAYOUT_RENDER_MISMATCH", ...)`。扩展 `test_layout_fixtures.py` 的 runner：为首批 `conservative`、`modern`、`expressive` 各自的有照片 FrozenResumePlan 调用 `emit_typst`，在 `tempfile.TemporaryDirectory()` 或明确 `--workdir` 下执行 `typst compile --font-path skills/graduate-resume/fonts --ignore-system-fonts --creation-timestamp 0 <plan.typ> <workdir>/resume.pdf`，再以相同源和参数导出 `page-{0p}.png`。用 `pdfinfo` 解析页数/页面尺寸、检查 `%PDF-` header，并调用 PDF 门禁。实现真实 raster overflow/crop 检测：按导出 PNG 的 PPI 将 A4 210x297 mm、14/14/15/15 mm 安全区和 measurement `epsilon_mm` 换算为像素；从四角采样页面背景，以颜色容差连接非背景像素，拒绝安全区外的内容像素、触及 PNG 物理边缘的内容连通域、或跨越安全区边界的文字/线条/图像连通域。对有照片的每个首批主题，从非敏感 fixture 原图的已知宽高和 PNG 可检测照片内容边界计算宽高比，断言 FrozenResumePlan 的 `photo_fit=contain`、`crop_policy=forbid`、缩放后比例在像素 epsilon 内等于源图、槽位物理宽/高不超过 `35 mm x 49 mm`，并将宽高不一致的非等比缩放、超限槽位和未许可裁切作为 `LAYOUT_RENDER_MISMATCH`；仅 FrozenResumePlan 显式 `crop_policy="controlled"`、`allow_controlled_crop=True` 且有主体安全区域合同的主题可额外验证受控裁切边界，必须将 PNG 可见区域反向映射到源图且完整包含该安全区域。对篡改 PDF 页数、移动模块标题、删除条目事实、把职责/成果移页、非等比拉伸照片、在未许可主题注入裁切、让许可裁切侵入主体安全区域，以及向边缘注入可见像素的测试样本，均断言 `LAYOUT_RENDER_MISMATCH`。同时以受控文本/依赖扫描确认无照片路径、媒体名、`image(`、EXIF 或照片装饰残留。把固定布局回归接入 `command_verify`，使现有 Phase 46 schema 8/6 fixture 先执行、Phase 47 layout 矩阵随后执行；布局失败返回原始 allowlisted 布局错误并保留 JSON。禁止在本任务实现正式 stem、三件套发布、batch、archive、history、no-op 或 rollback。此任务落实 D-12 至 D-17a、D-25 至 D-32、THEME-02/03/04。</action>
  <acceptance_criteria>
    - 测试捕获实际 Typst argv 并严格包含三项受控编译参数；font manifest 缺失/哈希不符或 Typst 版本不是 0.15.0 时失败关闭。
    - `pdfinfo` 输出的 Pages 只能为 1 或 2，Page size 匹配 A4；测试故意篡改预计页数或插入第三页时验证器以 `LAYOUT_RENDER_MISMATCH` 失败。
    - 门禁对每个已编译 PDF 页提取文本，逐页比对 FrozenResumePlan 的 section/fact_id/page：模块标题、条目标题、非空元数据、职责与成果都在计划指定页；第二页先有模块标题；缺失、错误页、重复页或无标题续接均为 `LAYOUT_RENDER_MISMATCH`。
    - 首批三个主题各自的有照片 Typst/PDF/逐页 PNG 样张均通过安全区/边缘连通域检测；每主题样张以原图比例和照片内容边界断言 `photo_fit=contain`、`crop_policy=forbid`、等比缩放、无拉伸且槽位不超过 `35 mm x 49 mm`。测试将可见文字、线条或图像像素注入安全区外或页面边缘、改变照片一侧尺寸造成拉伸、向未许可主题注入裁切，或让许可裁切侵入版本化主体安全区域时必须以 `LAYOUT_RENDER_MISMATCH` 失败，证明检查的是渲染物而非计划字段。
    - 无照片矩阵对 Typst、PDF 文本抽取（工具可用时）、PNG/依赖工作目录递归扫描，均没有测试照片名、相对/绝对照片路径、EXIF 和照片装饰标识。
    - 验证前后使用哈希断言 layout fixture 和 canonical 模板未被改写，且 workdir 之外没有新 PDF/PNG 或交付目录。
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest skills/graduate-resume/scripts/test_layout_fixtures.py -v &amp;&amp; skills/graduate-resume/scripts/graduate-resume.sh verify</automated>
  </verify>
  <done>固定样张已通过由受控字体、固定 Typst 和物理 PDF 页数驱动的证据门禁；任一布局违规均非零失败且不产生正式投递产物。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 canonical 工作流说明并明确 Phase 47 证据边界</name>
  <files>skills/graduate-resume/SKILL.md, skills/graduate-resume/references/phase-46-baseline.md</files>
  <read_first>
    - skills/graduate-resume/SKILL.md（Canonical Workflow、Runtime Adapter Notes、Outputs、Verification、Safety）
    - skills/graduate-resume/references/phase-46-baseline.md（CLI Boundary、Fonts And Dependencies、Next Phase Hand-Off）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-UI-SPEC.md（Design System、Interaction And Copywriting Contract、Accessibility And Print Acceptance）
    - .planning/phases/47-controlled-themes-photo-frozen-layout/47-RESEARCH.md（Planned File-Level Work 与 Phase 47/48/49 边界）
    - skills/graduate-resume/scripts/graduate_resume_cli.py（最终 plan/verify 命令和保留 render/batch 边界）
  </read_first>
  <action>更新 canonical `SKILL.md` 与 Phase 46 baseline 交接说明：在 canonical workflow 明确列出并说明选择首批正式主题的稳定 key：`保守稳妥 (conservative)`、`现代简洁 (modern)`、`个性设计 (expressive)`，以及 `graduate-resume.sh plan --theme <conservative|modern|expressive>`；要求 CLI `--help` 和成功 `plan` 脱敏摘要同时完整显示三项，用户不必猜测默认主题。说明 ThemeSpec/ThemeRegistry 是可扩展注册表，未来可新增纯视觉主题，但不得改写 graduate-resume/v2 schema 或已验证事实；不得称三种为永久上限。记录 `plan` 已输出受控主题、照片模式、冻结 1/2 页布局建议和脱敏摘要；记录照片槽位不得超过 `35 mm x 49 mm`、默认 contain 等比、不得拉伸、仅主题显式许可时才可受控裁切，字体必须 skill-local、锁定 Typst 0.15.0、用 `--ignore-system-fonts` 编译，照片只可使用显式本地文件且无照片去除全部残留；在 Verification 增加可复制的 `plan`/`verify` 命令与有/无照片样张证据范围。文档示例、帮助转录和计划输出不得暴露照片路径、EXIF 或 PII。明确 Phase 47 只输出调用方 workdir 中的布局验证证据，正式 Markdown/Typst/PDF 三件套命名、批量发布、history、candidate-first/no-op/rollback 仍属于 Phase 48，跨 runtime 与跨环境人工验收仍属于 Phase 49。canonical 主体不得写入 Codex/Claude 等私有命令或把主题、页数、照片布局回写为事实 schema；adapter notes 保持原有运行时差异位置。此任务落实 D-05、D-06、D-12 至 D-17a、D-33、D-34 与项目 AGENTS.md 的 canonical/adapters 边界。</action>
  <acceptance_criteria>
    - 文档包含可运行的 `graduate-resume.sh plan --theme <conservative|modern|expressive>` 与 `verify` 命令，并完整列出 `保守稳妥 (conservative)`、`现代简洁 (modern)`、`个性设计 (expressive)`；同时说明 --help/plan 摘要的主题可发现性、受控字体/本地照片/冻结计划门禁和 Phase 48/49 的明确边界。
    - 文档声明照片槽位上限 `35 mm x 49 mm`、默认 contain/等比/禁止拉伸及仅显式许可的受控裁切；示例和说明均不含照片路径、EXIF 或 PII。
    - 文档说明 ThemeSpec/ThemeRegistry 可新增未来纯视觉主题而不改写 graduate-resume/v2 schema 或已验证事实，且不把首批三种主题表述为永久上限。
    - `SKILL.md` 的 canonical workflow 不含 runtime 私有语法；六 runtime 行仍位于 Runtime Adapter Notes，且没有宣称本阶段完成正式交付或跨环境 UAT。
    - 文档不要求复制主题版事实 Markdown、不承诺预览格式/路径/发布事务，也不暴露照片绝对路径、EXIF 或 PII。
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'FrozenResumePlan|--ignore-system-fonts|35 mm x 49 mm|contain|Phase 48|Phase 49|graduate-resume.sh (plan|verify)' skills/graduate-resume/SKILL.md skills/graduate-resume/references/phase-46-baseline.md &amp;&amp; rg -n '保守稳妥.*conservative' skills/graduate-resume/SKILL.md &amp;&amp; rg -n '现代简洁.*modern' skills/graduate-resume/SKILL.md &amp;&amp; rg -n '个性设计.*expressive' skills/graduate-resume/SKILL.md &amp;&amp; ! rg -n 'Codex.*plan|Claude.*plan|永久.*三种|仅支持三种' skills/graduate-resume/SKILL.md</automated>
  </verify>
  <done>使用者能够从 canonical 文档理解并运行受控布局计划与样张验证，同时不会把当前阶段误认为已完成最终交付或跨 runtime 验收。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture 资料/媒体 -> layout verify | 受控样张与测试照片被编译成临时 PDF/PNG 证据。 |
| Typst/PDF 工具输出 -> 结构门禁 | 外部命令产物必须由页数、A4、锚点和隐私扫描共同验证。 |
| verify 证据 -> canonical 文档 | 测试结论被描述为阶段能力，不能扩大为正式发布或跨环境保证。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-21 | Information Disclosure | 无照片 fixture / workdir | mitigate | 对计划、Typst、PDF/PNG 与依赖清单递归负向扫描照片路径、图像、EXIF 和装饰；只使用非敏感测试媒体。 |
| T-47-22 | Tampering | layout fixture 注册表 | mitigate | 显式常量矩阵和必需测试数，拒绝 glob/动态跳过；每个资料先通过 Phase 46 validate。 |
| T-47-23 | Tampering | Typst 编译环境 | mitigate | 固定 Typst 0.15.0、font manifest hash、font path、`--ignore-system-fonts` 和创建时间；版本/字体漂移失败关闭。 |
| T-47-24 | Denial of Service | 超长内容/第三页 | mitigate | 压力样张与不可满足样张验证精确 1/2 页，超过预算返回 `LAYOUT_UNSATISFIABLE`，不截断或扩页。 |
| T-47-25 | Information Disclosure | CLI verify JSON / docs | mitigate | 验证输出只包含 allowlisted fixture、主题、页数和稳定错误码；不复制源事实、联系人、绝对路径或 EXIF。 |
| T-47-26 | Spoofing | 未验证事实 | mitigate | fixture runner 先调用 validate；测试断言计划 ID 皆源自已验证资料，禁止虚构填充。 |
| T-47-SC | Tampering | 受控资源与外部工具 | mitigate | 验证只使用 manifest 中审阅字体、固定本地 fixture 和已锁定 Typst；未知资源、字体失败或路径逃逸均停止。 |
</threat_model>

<verification>
1. `python3 -m unittest skills/graduate-resume/scripts/test_layout_fixtures.py -v` 执行固定主题/照片/页数矩阵和物理 PDF 门禁。
2. `skills/graduate-resume/scripts/graduate-resume.sh verify` 先保留 Phase 46 schema 夹具，再报告 Phase 47 layout 夹具结果。
3. 对无照片和不可满足 fixture 检查无媒体泄露、非零失败与 workdir 隔离。
</verification>

<success_criteria>
- 一页、两页、有照片、无照片、临界和压力样张共同锁定首批三主题的密度与双页语义，并以真实 PDF/PNG 证明照片槽位上限、等比 contain、无拉伸及显式裁切许可。
- 证据验证真实 A4 PDF 页数、字体隔离、条目完整、页面锚点和无照片隐私边界，而不是只验证文件存在。
- Phase 47 的 canonical 文档准确描述已具备的布局能力，并没有越界承诺 Phase 48 发布或 Phase 49 跨环境验收。
</success_criteria>

<output>
Create `.planning/phases/47-controlled-themes-photo-frozen-layout/47-03-SUMMARY.md` when done.
</output>
