---
patch: v1.7.1
base_milestone: v1.7
base_tag: v1.7
status: resolved
created: 2026-06-10
scope: post-close bugfix record
---

# v1.7.1 Post-Close Patch

## Purpose

This patch records bugs found after the v1.7 milestone was archived. The fixes stay inside the v1.7 End-of-Term Teaching Materials Skill delivery scope: score calculation, score views, deterministic review artifacts, and fixed-template PDF layout.

This patch does not reopen the v1.7 milestone and does not introduce a new feature milestone.

## Included Debug Sessions

| Debug | Scope | Resolution | Commit |
| --- | --- | --- | --- |
| `.planning/debug/resolved/end-of-term-uncertain-score-calc.md` | Preserve values such as `87?` in abnormal review while using the numeric part for provisional calculations. | Resolved | `35774e2` |
| `.planning/debug/resolved/end-of-term-score-views-and-summary-pagination.md` | Separate score-book and score-summary views, fix owned-student pagination, hide placeholder tasks, add deterministic score-list artifacts. | Resolved | `c344189` |
| `.planning/debug/resolved/end-term-layout-regression.md` | Align fixed-template PDF layout after user visual review: cover field spacing, red warning fills, analysis header/body, and handoff divider position. | Resolved | `5992207` |

## Verification Evidence

- `end-of-term-uncertain-score-calc` verified shell syntax, Python compilation, abnormal review rendering, calculated score evidence, normal final export verification, and `git diff --check`.
- `end-of-term-score-views-and-summary-pagination` verified Python compilation, shell syntax, full validation abnormal review rendering, Typst source checks, score-list Markdown/XLSX checks, built-in verification, and `git diff --check`.
- `end-term-layout-regression` verified Python compilation, shell syntax, `git diff --check`, full package verification, Typst source checks, a 49-student abnormal preview, PNG/PDF visual checks, and user-confirmed visual acceptance.

## GSD Handling

- v1.7 remains archived at the original milestone close.
- These fixes are managed as a post-close patch because they correct shipped v1.7 behavior instead of changing milestone scope.
- The next milestone should start from the patch commit/tag, not from the original `v1.7` tag, so the resolved debug work is preserved.

## Outcome

The v1.7 deliverable should be treated as patched by v1.7.1 after this record and the corresponding tag are pushed.
