---
phase: 45-clean-delivery-standardization
plan: 03
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - skills/tiaokedan/scripts/delivery_transaction.py
  - skills/tiaokedan/scripts/test_delivery_transaction.py
  - skills/tiaokedan/scripts/tiaokedan_renderer.py
  - skills/tiaokedan/SKILL.md
  - skills/tiaokedan/references/pdf-workflow.md
  - skills/tiaokedan/references/markdown-contract.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, REV-01, REV-02, REV-03, REV-04, SAFE-01, VERIFY-01, DOCS-01, RUNTIME-01]
must_haves:
  truths:
    - "`render --input --typ [--pdf] [--expected-typ]` 保持公开形状，md+typ 与 md+typ+pdf 是两个 exact managed sets。"
    - "旧 PDF 不再在 parse/expected/compile 前删除；所有候选与 diff evidence 先留在 `.work/<run-id>`。"
    - "跨 root 或不同 stem PDF、unknown、legacy `.tiaokedan/`、symlink 和 traversal 在 current mutation 前失败。"
  artifacts:
    - path: "skills/tiaokedan/scripts/delivery_transaction.py"
      provides: "skill-local optional bundle transaction"
    - path: "skills/tiaokedan/scripts/tiaokedan_renderer.py"
      provides: "candidate renderer、expected/PDF gate 与 public CLI 接线"
  key_links:
    - from: "skills/tiaokedan/scripts/tiaokedan_renderer.py"
      to: "skills/tiaokedan/scripts/delivery_transaction.py"
      via: "same-root/same-stem explicit candidate set"
      pattern: "expected_typ|pdf|candidate|publish"
---

<objective>
把调课单两/三文件输出改为 candidate-first、exact-set history 和可回滚发布。

Purpose: 防止旧 PDF 被提前删除、expected mismatch 或 Typst compile 失败污染 current。
Output: Python transaction、renderer 接线、局部矩阵、artifact/runtime 文档。
</objective>

<execution_context>
@/Users/mrered/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mrered/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/phases/45-clean-delivery-standardization/45-01-SUMMARY.md
@.planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
@docs/clean-delivery-directory-contract.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 optional PDF 的 safe transaction</name>
  <files>skills/tiaokedan/scripts/delivery_transaction.py, skills/tiaokedan/scripts/test_delivery_transaction.py</files>
  <read_first>
    - docs/clean-delivery-directory-contract.md
    - skills/tiaokedan/scripts/tiaokedan_renderer.py
    - skills/school-pptx/scripts/pptx_render.py
    - .planning/phases/45-clean-delivery-standardization/45-PATTERNS.md
  </read_first>
  <behavior>
    - md+typ 与 md+typ+pdf 的 relative path set 不相等，optional PDF 增减触发 changed history。
    - identical 不建 history、不触 current；001/003 后 next=004。
    - 七 fault、INT/TERM、lock conflict、unknown/symlink/traversal 后 current/history/sources hash 不变。
  </behavior>
  <action>创建 self-contained Python transaction：从安全 `--typ` basename 派生 root/stem 和 Markdown 名，若有 `--pdf` 必须同 canonical root、同 stem、`.pdf` suffix；拒绝绝对/`..`/separator/NUL/符号链接。使用 `.work/&lt;run-id&gt;/{candidate,rollback,evidence}`、同根锁、held identities、explicit names、path-set+bytes equality、max+1 history 和 completed-name rollback。current discovery 只接受同 stem 的完整 2/3 件套；partial、多 stem、`.tiaokedan/`、media 或任意 unknown 均 bounded fail closed。只清本 run inode，保留 unrelated stale work。</action>
  <verify><automated>python3 -m unittest skills/tiaokedan/scripts/test_delivery_transaction.py -v</automated></verify>
  <acceptance_criteria>
    - 测试 mutation guard 能击穿 Markdown-only equality 与 gap reuse 错误实现。
    - fault 在 publish_file_1/middle/post-verify 后均恢复 exact old set 并移除新 history。
    - transaction 模块无 sibling skill/root harness import，未使用 broad glob/rmtree 清理用户根。
  </acceptance_criteria>
  <done>两/三文件 transaction 的 first/change/no-op/failure/unknown/lock 行为可独立验证。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 expected 与 PDF gate 前移到 candidate</name>
  <files>skills/tiaokedan/scripts/tiaokedan_renderer.py</files>
  <read_first>
    - skills/tiaokedan/scripts/tiaokedan_renderer.py
    - skills/tiaokedan/scripts/tiaokedan.sh
    - skills/tiaokedan/scripts/delivery_transaction.py
    - skills/tiaokedan/references/markdown-contract.md
  </read_first>
  <behavior>
    - unresolved marker、expected mismatch、Typst missing/compile failure、empty/invalid PDF 均不改变 current。
    - expected mismatch 公开诊断有字节上限，详细 diff 只在 owned evidence 生命周期内存在。
    - PDF 从本次 candidate Typst 编译，成功 root 只含 stable md/typ[/pdf] 与允许支持目录。
  </behavior>
  <action>删除 render 开始时 stale PDF unlink 和 Typst root 直写；先 parse/review，再把 Markdown copy、Typst 和 optional PDF 生成到 candidate。`--expected-typ` 在 candidate 上比较，PDF compile 输入必须是 candidate Typst；验证 Markdown/Typst UTF-8/non-empty、PDF regular/non-empty/`%PDF-`。保持现有 public flags、解析内容和 exit codes；失败 stdout/stderr bounded 且无 false-success，finally 调 transaction cleanup/rollback。</action>
  <verify><automated>bash -n skills/tiaokedan/scripts/tiaokedan.sh &amp;&amp; python3 -m py_compile skills/tiaokedan/scripts/*.py &amp;&amp; python3 test/clean-delivery/verify_clean_delivery.py --skill tiaokedan</automated></verify>
  <acceptance_criteria>
    - 源码 scan 不再出现 render 前删除 final PDF 或先写 final Typst 再 expected compare。
    - 真实 CLI 覆盖 first/change/no-op/generation failure/validation failure/all faults/history gap/unknown/symlink。
    - 既有 Markdown contract 与 PDF 行为测试继续通过。
  </acceptance_criteria>
  <done>公开 render 在任一前置 gate 失败时不再破坏旧成功版本。</done>
</task>

<task type="auto">
  <name>Task 3: 同步调课单交付与 runtime 契约</name>
  <files>skills/tiaokedan/SKILL.md, skills/tiaokedan/references/pdf-workflow.md, skills/tiaokedan/references/markdown-contract.md</files>
  <read_first>
    - skills/tiaokedan/SKILL.md
    - skills/tiaokedan/references/pdf-workflow.md
    - skills/tiaokedan/references/markdown-contract.md
    - skills/tiaokedan/scripts/tiaokedan.sh
    - docs/clean-delivery-directory-contract.md
  </read_first>
  <action>记录 md+typ 与 md+typ+pdf exact set、同 root/stem 约束、support dirs 生命周期、candidate/expected/minimal PDF gate、whole-bundle history/no-op/rollback、unknown/legacy `.tiaokedan/` 确认门，并删除任何把 `.tiaokedan/` 当成功根长期例外的说明。六 runtime notes 覆盖 whole-folder、显式 `tiaokedan.sh` fallback、Python/Typst、读写/执行、sandbox/allowlist、delivery/.work；OpenClaw/Hermes 保守为 installation-time verified。</action>
  <verify><automated>python3 test/clean-delivery/verify_clean_delivery.py --skill tiaokedan --gate documentation_runtime_contract_gate</automated></verify>
  <acceptance_criteria>
    - 文档和脚本对 optional PDF、稳定文件名、证据位置一致。
    - 不声明 history 自动删除、普通 render 自动迁移 legacy 或 SIGKILL/power-loss 原子。
    - 六 runtime 与显式 fallback 可机械找到。
  </acceptance_criteria>
  <done>调课单 canonical 文档与实现安全边界一致。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI paths → delivery root | typ/pdf 参数可尝试跨根、不同 stem 或 traversal |
| candidate Typst/PDF → current | compile 与 expected gate 失败不得污染 current |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03-01 | Elevation of Privilege | output path validation | mitigate | same-root/stem、component validation、no-follow identity |
| T-45-03-02 | Tampering | expected/PDF publish | mitigate | candidate compare/compile before transaction mutation |
| T-45-03-03 | Information Disclosure | unbounded diff | mitigate | bounded public diagnostic，detailed evidence owned/cleaned |
| T-45-03-SC | Tampering | package installs | accept | 不新增包管理器依赖 |
</threat_model>
<verification>
- `python3 test/clean-delivery/verify_clean_delivery.py --skill tiaokedan`
- `python3 -m unittest skills/tiaokedan/scripts/test_delivery_transaction.py -v`
</verification>
<success_criteria>
- optional set equality/history 正确，失败不再删除旧 PDF。
- unknown/symlink/path mismatch 在 mutation 前失败。
- skill folder 自包含并保留原 public CLI。</success_criteria>
<output>Create `.planning/phases/45-clean-delivery-standardization/45-03-SUMMARY.md` when done.</output>
