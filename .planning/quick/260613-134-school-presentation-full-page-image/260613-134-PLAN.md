---
quick_id: 260613-134
slug: school-presentation-full-page-image
status: planned
date: 2026-06-12
---

# Quick Task 260613-134: school-presentation full_page_image 页面样式

## Goal

为 `school-presentation` 增加 `layout: full_page_image`，让 Markdown 逻辑页可被目录、页码和 manifest 管理，但页面本体只渲染一张全画布图片。

## Tasks

1. 更新渲染器：识别 `full_page_image` layout，保留 page metadata，输出无标题、无页脚、无交互的单图页面。
2. 更新技能说明和示例模板：记录 Markdown 写法，并加入可验证样例页。
3. 扩展验证：`verify` 必须证明 manifest 和 HTML 均包含 `full_page_image`，且该页没有 reveal steps。

## Verification

- 运行 `skills/school-presentation/scripts/school-presentation.sh verify --workdir /tmp/school-presentation-full-page-image-verify`。
- 检查 verification manifest 中 `status` 与 `full_page_image_verified` 均为 `passed/true`。
