# Examples

Examples are optional reference material for the canonical template.

Each example should be:

- small enough to review quickly
- complete enough to copy from
- explicit about only the runtime adapter notes it actually needs
- free of secrets and machine-specific paths
- never treated as a v1 requirement
